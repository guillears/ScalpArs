# SCALPARS — Current Live State

> Read at every session start (together with CLAUDE.md). **Edited in place** on every ship/revert (never appended — the archive grows, this stays small). Snapshot as of **2026-06-05**.

## Mode & capital
**⟲ PAPER RESET 2026-06-13** (operator, post ATR×Gap-LONG-block + Runner-Stretch-Trail-SHORT): clean baseline — short-exit revert tally (N≥8 armed shorts vs shadow_tight) AND ATR×gap-block revert tally (N≥8 would-be-blocked longs) both count from here. Previous resets 2026-06-12, 2026-06-10. Order IDs restart from 1 — **dedup strictly by (opened_at, pair, direction)**; pre-reset data: `reports/dedupe_pool_13batches_may26-jun13.csv` (362 trades) + `dedupe_pool_FULL.csv` (1,273) + batch CSVs (latest: orders_2026-06-13_4L_0S).
Paper. Balance **$2,500 + $500 BNB ≈ $3,000**. **5 max positions** (5→4 shipped 2026-06-09, **REVERTED to 5 on 2026-06-10**: with the 2× unmatched multiplier a single position reached ~$2,300 of $3k margin — two bad trades (MORPHO+CHZ −$434) had outsized equity impact; operator judged 4 too concentrated. Concentration-vs-deployment trade-off resolved toward diversification), equal-split, reserve $500 fixed. Leverage **20×** (both VERY_STRONG + STRONG_BUY).

## Strategy in brief
EMA5/8/13 + RSI + ADX scalper on 5m, BTC-macro-gated. Entry passes a BTC/pair filter stack → sized by liquidity caps × multiplier/pattern-cell rules → exited by a tiered ladder.

### Exit stack (current)
- **FAST_EXIT OFF** (both L1 +0.20%/2min and L2 disabled).
- **ATR-LOW Fixed TP (LONG) — DISABLED 2026-06-09** (`atr_low_fixed_tp_long_enabled=false`). Paired with "Keep Only Unmatched Longs" (below): the fix-TP caps the pop-and-fade (matched) cohort, but the unmatched longs we now keep are RUNNERS (54% peak ≥0.40) — capping them at +0.25 strangled the edge. OFF so unmatched longs trail. **Coupling: re-enable the fix-TP if `long_unmatched_only` is ever turned OFF (matched longs need the cap).**
- **🧬 KEEP ONLY UNMATCHED LONGS — SHIPPED 2026-06-09 (in-sample, forward-test)** (`long_unmatched_only=true`). Blocks any LONG matching ANY C or W pattern (counter `LONG_UNMATCHED_ONLY`); keep only truly-unmatched longs. **Evidence (10-pool, current-stack: fix-TP + EMA13-off + wide-SL + multipliers):** the LONG pattern library uniformly selects for LOSERS — EVERY C/W pattern net-negative (W6 −$574, W2 −$480, C7 −$261, C6 −$297 demux; multiplier *amplifies* the matched losses); truly-unmatched longs = N=39, **85% WR, +$471 demux / +$679 as-sized** (the UNMATCHED-LONG 2× sits correctly on them). Keep-only-unmatched flips the long book **−$1,515 → +$471 demux** (blocks ~80% of longs). With fix-TP off + trailing, the unmatched cohort models +$1,500–3,200 (heavy haircut on the trailing model). Mechanism: matched-long patterns = countertrend/exhaustion signatures (C7 bounce, W6 top); no-pattern = clean momentum runner. **FORWARD-TEST READ GATE: does the unmatched-long cohort hold ≥70% WR AND net-positive demux on N≥20 fresh full-stack? If yes → keep. If unmatched WR<60% OR net-negative on N≥20 → revert (toggle off + re-enable fix-TP).** In-sample + N=39 → forward-validate before calling it proven. SHORT side already mirror-optimal (truly-unmatched shorts blocked; matched shorts carry the edge).
- **Trailing**: arms at peak ≥ **0.45%** (tp_min), exits at peak − **0.25%** (pullback, ATR-floored ×0.5 + 0.1/level widen).
- **Trailing Min-Profit Gate (shipped 2026-06-08): `trailing_min_profit_to_fire = +0.10`.** The price-drop trailing only fires when its exit level (peak − pullback) is ≥ +0.10%; below that it's **suppressed/dormant** (rides the hard SL, re-arms on recovery). Fixes high-ATR L1 whipsaws where the ATR-widened pullback exceeds the peak (peak +0.45 − pullback 0.67 = −0.22 → exits red on a pair that recovers). Self-scopes to L1 (L2+ always clears). Engine: `indicators.py` price-drop fire gated on `(peak_pnl − pullback) ≥ +0.10`.
- **Runner Stretch-Trail** (LONG, high-ATR) **OFF (disabled 2026-06-06, `runner_trail_enabled`=false)** — high-ATR runners now exit via normal tight trailing. Live RUNNER_TRAIL exits were a wash (N=6, net +$30 vs tight, 3/6 better, and the bigger peakers FADED — stretch-trail gave back more than tight). UI toggle added (flip back on anytime). (The high-ATR ATR Multiplier sizing dimension that lived alongside this was REMOVED Jun 10 — see Active multipliers section.)
- **EMA13 strict cross exit — SHORT closes; LONG in PHANTOM-CF mode (Jun 7).** `ema13_cross_exit_long_enabled=false`: the cross no longer closes LONGs — it records a phantom (would-have-exited pnl, `phantom_ema13_cross_pnl`) and the long rides to its real exit. Tracked in the **🔀 EMA13 Cross — Disabled-Direction CF** table. SHORT still closes normally.
- **SL: signal-DEAD (STOP_LOSS) = −0.70% base; signal-ACTIVE (STOP_LOSS_WIDE) = −1.00% base (`signal_active_sl`, widened 2026-06-06 from −0.70).** Both ATR-widened (×1.5, floor −1.20%). The two labels differ only by whether the entry signal was still valid at the stop — now the signal-active path gets +0.30% more room (thesis still alive → ride; thesis dead → exit tight). VERY_STRONG + STRONG_BUY only.
- **Pattern fixed exits**: C1 SHORT fixed_sl −0.70%.
- **BE is OFF** (be_levels_enabled false). RSI-handoff OFF. Regime-change-exit OFF.

### Liquidity-aware sizing (LIVE, unproven forward — see Go-Live Watch)
① per-pair cap: notional ≤ min(0.10% × pair 24h vol, $500k). ② gross cap: Σ open notional ≤ balance × **30×**. ③ redeploy-leftover **OFF (2026-06-15 — operator: max-open must mean 5)**; effective max-open = `max_open_positions` 5 (was silently raised to `max_open_positions_hard` 10 while redeploy was on — the flip sleeve was piling up to 10 concurrent). `max_open_positions_hard` 10 left inert. min_investment_size $100 (skip if throttled below).

## Active entry filters
- **PAIR TREND FILTER — split per-direction + SHORT re-enabled 2026-06-13.** Was a single bidirectional toggle (was OFF). Now `pair_trend_filter_long_enabled=false` (gap<0 unmatched longs are N=67, 58% WR, −$27 ≈breakeven — not worth blocking) · `pair_trend_filter_short_enabled=true` with `pair_trend_short_gap_max=0.0`: **block SHORT when pair (EMA13−EMA50)/EMA50 ≥ 0** = shorting a pair not yet below its own 4hr trend (before the breakdown confirms → it bounces). Evidence: book shorts gap≥0 = 0% WR, −$247 (N=6 book / 39 all-pool, all May8–Jun1 while this filter was off). The SHORT mirror of the ATR×gap LONG finding — longs enter early (not extended up), shorts enter late (already below trend). Counter `PAIR_TREND_FILTER`. **WATCHLIST: tighten `pair_trend_short_gap_max` 0 → −0.2** (the −0.2..0 mild-downtrend zone = N=14, 43% WR, −$192) — ship when it confirms ≤35% WR on N≥30. **REVERT: re-disable short side if would-be-blocked (gap≥0) shorts ≥50% WR on N≥8 fresh.**
- **🚫 ATR×GAP LONG BLOCK — SHIPPED 2026-06-13 (DISCIPLINE-OVERRIDE, N=16 full/5 recent<30).** `atr_gap_block_long_enabled=true`, block LONG when `entry_atr_pct ≥ 1.0` AND pair `(EMA13-EMA50)/EMA50 ≥ 0.5%` (counter `ATR_GAP_LONG`). The "volatile-and-already-extended" quadrant = buying the exhaustion top of a move that already ran → mean-reverts (ENJ −$253 in 57s, peak 0.0%). **Resolves the ATR contradiction:** high-ATR is high-VARIANCE not bad — split by trend-extension, ATR≥1.0 × gap<0.5 = **64-75% WR POSITIVE** (the genuine runner, PRESERVED — do NOT lower the gap floor toward it); ATR≥1.0 × gap≥0.5 = 20-31% WR, −$414 to −$611 demux. Orthogonal to keep-only-unmatched (lives INSIDE the unmatched cohort; removing the quadrant rehabilitates ZERO banned C/W pattern — checked all 17 cohorts). Also note: the −1.20 ATR-widened SL is the RIGHT exit for the GOOD high-ATR runners (don't tighten — fix is at entry, this filter). Verified on the live batch: blocks ENJ (−$253), passes all 3 winners. **REVERT GATE: drop (`atr_gap_block_long_enabled=false`) if would-be-blocked longs show ≥50% WR on N≥8 fresh** (watch ATR_GAP_LONG counter + would-be-blocked record). Precision caveat: clips ~1 winner per 5 blocks (HOME +73 in recent set).
- **🛡️ JUN-10 GUARD SET (shipped post-crash-batch, operator-directed; in-sample-heavy — forward-validate):** ① **SHORT BTC 1h RSI floor `btc_rsi_1h_min_short=35`** (counter `BTC_1H_RSI_MIN_GATE`) — don't short when BTC's HOURLY RSI <35 = hourly bounce zone; cross-batch matched shorts: <30 −$940 · 30-35 −$382 · 35-40 +$651 (monotonic, helps 5/7 dates, NET +$1,322 incl 06-10). ② **LONG fan-window widened** `fan_ratio_block_long` `0.85-1.70`→**`0.85-3.00`**,5.0-99 — the 1.70-3.00 window was the spike-chase loser zone (N=13, −$1,043; all 06-10 long losers in it); keeps fan 0.6-0.85 edge (+$499) and 3-5 (+$102). ③ **LONG cross-filter += `70-100:40`** (PIPPIN at BTC RSI 71.6 leak) + **`pair_atr_max_long=2.5`** (counter `PAIR_ATR_MAX`) — distribution guard, historic max winner ATR 2.49, ESPORTS 4.68 DOA. ④ **LONG RSI-SPIKE GUARD `rsi_prev_min_long=50` AND `rsi_spike_min_jump_long=4`** (counter `RSI_SPIKE_GUARD`) — block when pair RSI one candle ago <50 **AND** the 1-candle jump ≥4 = momentum born from below the neutral line in a single candle (VVV 44.6→65, PIPPIN 45.5→58.3); complements fan (fan=candles 2-5, this=candle 1); kills only $60 of historic winners. **NOTE: jump SIZE alone does NOT separate** (winner avg jump +5.6 ≈ loser +5.8; pure jump≥5 = NET −$318 — kills 14 winners) — the signal is the SUB-NEUTRAL ORIGIN, not the delta; the jump≥4 condition is a formal-correctness refinement (historic blocks identical) excluding the theoretical 49.8→51 non-spike case. **GATES: ① revert 1h-floor→0 if would-be-blocked shorts ≥55% WR on N≥10 fresh · ② revert fan window→1.70 if blocked 1.70-3.00 longs ≥55% WR on N≥8 fresh · ④ DROP spike guard if it blocks ≥3 would-be winners with NO loser saves on fresh data.** Honest forward expectancy: ~+$30-50/batch ex-06-10 (the +$1,218 on 06-10 is hindsight-fit); UNMATCHED-LONG 2× kept (operator call — unfilterable DOA class stays at 2× size, accepted risk).
- **Global Volume Filter ON** (re-enabled 2026-06-02): Min L 0.70 · Min S 0.50 · Max S 1.10 · **capitulation override REMOVED 2026-06-05** (`..._override_enabled=false` — high-GV shorts always blocked; **no-op until Max S cap >0, currently the Max-S cap is the binding piece**) · LONG rescue $50M/ceiling 0.60 · **SHORT rescue $0 (load-bearing — do NOT add)**.
- **ADX Δ × BTC ADX ON**: SHORT block ΔADX≥2.0 & BTC ADX≥24 · LONG block ΔADX 1.0-2.0 & BTC ADX 18-30.
- **Pair ADX Direction: LONG=`both`, SHORT=`rising`** (LONG reverted to `both` 2026-06-03). SHORT blocks falling-ADX (7-pool falling-SHORT 45% WR/−0.24 vs rising 62%). LONG `rising` was REVERTED — on the proper 7-pool proxy it was backwards (cut falling-LONG breakeven +0.002 while keeping rising-LONG losers −0.174); the original LONG ship rested on the full pool's broken `adx_delta` field (6 negative/558 vs 59/222 in the 7-pool). Counter = PAIR_ADX_DIR.
- **BTC 1h Slope MIN floor (shipped 2026-06-03):** `btc_1h_slope_min_short = -0.60` (block SHORT when BTC 1h slope < -0.60 = shorting into a steep 1h crash/exhaustion). `btc_1h_slope_min_long = 0.0` (DISABLED — plumbed but off). 0 = disabled, negative = active. Counter = BTC_1H_SLOPE_MIN_GATE.
- **BTC-Accel Chase Filter (STATEFUL, LONG only) — RE-ENABLED 2026-06-05:** `evo_chase_filter_long_enabled=TRUE`, `evo_chase_window_min=30`. Blocks a LONG when live BTC EMA20 slope > the slope at the most recent LONG opened within 30min (chasing an accelerating BTC move = late). On the 6-05 batch it blocks 13 longs / −$853 of realized loss — the only lever that removes LONG losers. Engine tracks `_last_long_open_ts/_btc_ema20_slope`. SHORT side `evo_chase_filter_short_enabled=false`. Counter = BTC_ACCEL_CHASE_LONG.
- BTC RSI×ADX LONG `50-55:99-100,55-60:20-25,70-100:40` (Jun 10 final set — 55-60:20-25 added per band-watch evidence; the two 60-65 rules that existed as UI-only drift were DROPPED: net −$21, killed a +$58 winner. **UI must be synced after deploy: delete the two 60-65 rows**) · SHORT `30-35:30-32,35-40:20-26,45-50:25,0-30:25-30`.
- BTC ADX gate L/S [18,40] · BTC Gap×ADX LONG `0.10-0.20:0-22` · RngPos×ADXΔ LONG `90-95:0.0-0.3` / SHORT `5-10:1.0-2.0` · SHORT EMA20-slope-min 0.06 · Entry Quality Score filter ON.
- Full filter list = `trading_config.json`. **Pair blacklist = 10 pairs (trimmed from 23 on 2026-06-03; BTCUSDT re-whitelisted same day).**
  - **BLACKLISTED (15):** `ALLOUSDT, BNBUSDT, EIGENUSDT, ENAUSDT, ESPORTSUSDT, FILUSDT, MUSDT, RAVEUSDT, SYNUSDT, TRUMPUSDT, VELVETUSDT, VVVUSDT, XAGUSDT, XAUUSDT, ZECUSDT`. Of these: 4 evidenced losers (BNB/FIL/TRUMP/VVV, N≥15) + XAG/XAU (commodities) + ENA/MU/RAVE/ZEC (no-data, new-listing/illiquid) + ESPORTS/EIGEN/VELVET (DOA/meme) + **SYN (2026-06-22 — PAIR_RSI_OB 0W/5L/−$424 this batch, gap ~10%/ATR ~3% parabolic; N=5 single-batch, redundant with the PAIR_RSI_OB gap≥1.0 block below — belt-and-suspenders; re-whitelist if it doesn't repeat as a loser)** + **ALLOUSDT (2026-06-24 — the per-pair-concentration lever isolated by the BTC-ADX overlap audit; see FAN flip-short ship history below)**.
  - **🔓 RELEASED / WHITELISTED from blacklist (12, on 2026-06-03):** `ADAUSDT, ASTERUSDT, BCHUSDT, DOGEUSDT, ERAUSDT, HYPEUSDT, ICPUSDT, LABUSDT, LINKUSDT, PUMPUSDT, SKYAIUSDT, WLFIUSDT`. Released for thin pre-filter evidence (N<15). **REVERT GATE: re-blacklist any released pair at ≤35% WR on N≥10 fresh (current-stack) trades.**
  - **👁 NO-TRADE / TRACK-ONLY (`no_trade_pairs`):** `BTCUSDT, ETHUSDT`. Stays in the Top-Pair-by-Volume universe (subscribed, scanned, **displayed**) but entries are BLOCKED (counter PAIR_NO_TRADE). Third pair state, distinct from blacklist (removed from universe) and tradeable. **BTC** (Jun 3): edge<fee, reference only. **Re-validated Jun 10 via full offline backtest (51k 5m + 3yr 4h candles): 5m scalping rejected twice (fee=40-58% of median MFE); a 4h DIRECTIONAL Donchian module IS viable (+73%/yr @1×, all 24 cells positive over 3yrs) but PARKED — wealth-sleeve for the scaling phase. Full spec: `CLAUDE_ARCHIVE/BTC_MODULE_SPEC.md`.** **ETH** (added 2026-06-05): recurring short-squeeze pair — ETH shorts −$230 this batch (3 trades, squeezed on bounce) AND −$230 prior batch. Track-only (not blacklist) so its would-be record stays observable. Neither is in pair_blacklist, neither is tradeable.

## Active multipliers (target "both"; inv cap 2× · lev cap 2×)
- **BTC_60-65_22-25 LONG = 2.0×inv × 1.5×lev = 3.0× eff** (lev-stacked; cross-batch 73.7% WR / N=38). · BTC_60-65_28-30 LONG 2.0×.
- BTC_35-40_33-36 SHORT 2.0× · BTC_25-30_28-30 SHORT 2.0×.
- PAIR_35-40_30-35 SHORT = **1.0× (demoted 2026-06-02)**.

## Active Pattern Cell Ship rules
- **C1 SHORT** = 2.0×inv × **1.0×lev (2× eff) — lev DROPPED 1.5→1.0 on 2026-06-19** (operator; the −$239 fixed-SL hit this batch was a 3×-amplified loser → caps-for-losers, kill the lev on the fat-tail SL, keep inv 2×) + fixed_sl −0.70%. *(staged — pending push)*
- **W2+W1 SHORT** = 2.0×inv × **1.0×lev (2× eff) — lev DROPPED 1.5→1.0 on 2026-06-19** (operator; same caps-for-losers de-lever, inv 2× kept) *(staged — pending push)* — **refined Jun 10 from plain W2** (combo decomposition: W2+W1 = +$1,480/79% WR N=14 vs W2-alone = −$123 demux N=16 incl fresh XLM −$210 — the multiplier value lives in the COMBO; W2-alone drops to baseline 1×).
- **W6+!W1 SHORT = BLOCK (Jun 10)** — replaces the deleted W6 SHORT 2× rule. See gate entry below.
- **UNMATCHED LONG** = 2.0× (no fixed exits — deliberate, Jun 1; **✗-HARMFUL gate TRIPPED Jun 10, operator override** — see gate entry below).
- C4/C8 L+S, W1 L+S, W2 LONG, W4 L+S, W6 LONG, UNMATCHED SHORT = all baseline 1.0× (observation).
- **Extension multiplier (LONG) Ext0.4-0.6_L family = 1.0× (DEMOTED 2026-06-04 from 2×)** — tags kept for tracking, sizing killed. Cross-batch FULL turned ✗ HARMFUL (base N=5/−$235, +QuietVol N=5/−$252); +SlowADX N=3 noise. Long side has no gross edge → "caps for losers."
- **ATR Multiplier dimension REMOVED (Jun 10)** — was `Runner` ATR 1.1-99% 2.0× (Jun 5 ship). Refuted on the merits: all-time survivors of the Jun-10 guard stack in the 1.1-1.5 zone = N=14, **36% WR, −$328 demux** (worst WR bucket on the whole ATR axis); current-era survivors above ATR 0.8 = **N=0** (the ATR≤2.5 cap + fan windows + spike guard fence the zone). Also fully redundant under keep-only-unmatched (every long already gets identical 2×/1× from the UNMATCHED cell, max-wins). Feature deleted end-to-end: engine lookup + candidate, config field, both UI tables (rules + performance), perf compute, exports.

## 🚨 LOCKED GO-LIVE WATCH — liquidity sizing (gross 30× = biggest systemic knob)
Unproven forward; read before each live checkpoint. All signals in UI (Gross gauge, Filter Blocks, Liquidity Sizing table).
- **Revert 30×→25× if a correlated event causes ≥25% equity DD in <15min.** (Clean SLs ≈ 30 × 0.70% ≈ 21%; ≥25% means SLs gapped through — the tail the cap exists to bound.)
- **GROSS_CAP_SKIP > ~20% of entry attempts with Gross pinned red** → 30× is the binding limiter; decide raise vs accept. <5% = fine.
- **① slippage verdict (live, N≥10):** capped fills should show ≤ uncapped entry slippage. Clean read = same-pair (aggregate skews thin-pair, so raw capped>uncapped is EXPECTED — not a verdict).
- **REDEPLOY_OPEN must stay 0 at ~$3k balance** (① dormant here). If it fires → bug (equal-split divisor / margin gate).
- ① per-pair cap is dormant at this balance + paper-invisible (paper fills ~0 slippage); ② gross cap is the one that can bind now via multiplier stacks. ① verdict is **live-only**.

## 🧪 ACTIVE EXPERIMENT — Open run, deadlocked/thin filters OFF (Jun 6, ~24h)
**SHORT cross-filter RE-ADDED 2026-06-08** (`btc_rsi_adx_filter_short` restored to `30-35:30,35-40:20-26,45-50:25,0-30:25-30`) — abort gate tripped: 4/4 fresh shorts 0% WR / −$577, and **all 4 fell in removed-filter blocked cells** (oversold BTC RSI 28-35 × weak ADX 18-22 = short-into-exhaustion squeezes). Clean mechanism-consistent degradation on the money-maker side → honored the "re-add SHORT immediately" clause. **LONG cross-filter stays BLANK** (its arm unresolved; the 1 LONG loser that day shared its BTC cell with 2 winners — filter doesn't separate, so re-adding LONG would block winners; that loss was a high-ATR pair × ATR_Runner 2× event, not a BTC-cross issue).
**LONG cross-filter PARTIALLY re-added 2026-06-10:** `btc_rsi_adx_filter_long = "50-55:99-100"` (RSI-only 50-55 block — the watchlist's pre-committed band gate tripped on its 3rd consistent window: full-pool −, last-4 −, fresh unmatched N=12 −$396 incl. MORPHO −$160 + CHZ −$274 both at BTC RSI 53.6; mid-range/directionless BTC = no tailwind for momentum longs). **60-100 NOT re-added — its gate FAILED on fresh data:** unmatched longs at BTC RSI 60-65 are now +$368 / 86% WR (keep-only-unmatched fixed the overbought-long bleed; it was the matched longs). 55-60 stays open per watchlist. Ext floor stays off — PERMANENTLY moot for this strategy: 0 unmatched longs below ext 0.20 (momentum breakouts are extended by construction); raising it fails too (0.25 clips net-winners; the bad band 0.30-0.40 is a non-monotonic mid-hole flanked by 67%-WR bands = confound); winners avg MORE extended than losers (0.65 vs 0.56 — the edge IS extension). Do not re-propose while keep-only-unmatched is live. **Revert gate: re-blank 50-55 if would-be-blocked longs hit ≥55% WR AND net-positive on N≥8 fresh.**
**Blanked to break measurement deadlocks** (can't validate blocked cohorts while they're blocked; historical pools BE-contaminated): ① **BTC RSI×ADX cross-filter LONG 55-100 only** (`btc_rsi_adx_filter_long` 50-55 re-added Jun 10; SHORT re-added Jun 8) · ② **Pair-Extension floor DISABLED via master toggle** (`entry_dist_from_ema13_filter_enabled`=false; LONG min ext **0.20 value RETAINED** for one-flip re-enable — thin N=9 same as ship, zero data since May 22, rare/tiny-save cohort; the <0.20 band is the worst-per-trade but immaterial to LONG P&L). KEPT ON: BTC ADX [18,40] gate, **BTC ADX 24-30 short block (robustly validated, 3 windows — NOT opened)**, chase, ADX-delta, pair-ADX-dir, BTC-1h-slope, pair-level `rsi_adx_filter_*`, quality score → LONG+SHORT RSI surfaces open 40-100/0-60 + bottom-of-pullback longs now allowed.
- **ARCHIVED for instant re-add:** cross-filter LONG = `70-100:40,60-65:22-25,60-65:27-30,55-60:20-25,50-55:99-100` · cross-filter SHORT = `30-35:30,35-40:20-26,45-50:25,0-30:25-30` · LONG ext floor: just flip `entry_dist_from_ema13_filter_enabled` back to true (value 0.20 retained).
- **ABORT GATE (don't dogmatically run 24h):** re-add early if opened-band entries hit ≤30% WR on N≥20 that side, OR the side draws down past operator comfort. **SHORT is the money-maker → tighter watch:** if the open SHORT book degrades vs its filtered baseline (WR drop / net-neg on N≥15), re-add SHORT immediately (don't dilute a working side).
- **POST-RUN ANALYSIS (pre-committed):** ① bucket fresh longs AND shorts by BTC RSI band (×ADX); re-add the cross-filter rules ONLY for bands that confirm net-negative on the fresh data. ② check "does LONG pair_ext <0.20 still die?" (re-add the ext floor if it confirms loser; leave off if it surprises). Leave open any that surprise positive. This is the deliverable — judge on per-band breakdown, NOT the run's bottom line (LONG P&L expected worse; that's the cost of the data).
- **ATTRIBUTION CAVEAT:** stacks with the just-shipped SLWide −1.00. Separable (entry-mix vs exit) but flag when reading: "more/worse entries got in" (cross-filter) vs "stops gave room" (SLWide).

## Active watchlist & locked revert gates (pending — apply at next ≥30-trade checkpoint)

### 🗓 2026-06-21 watchlist (observation — no ship)
- **📋 NEXT-BATCH ANALYSIS QUEUE — 5 ATR/regime-exit tables to read on the fresh clean-config batch (2026-06-21, no ship; analytics-only).** Read all five together (the ATR→speed→regime-drift thesis chain + the exit-timing decision aid), SPLIT BY DIRECTION (mandatory — high-ATR≈FAN-shorts, low-ATR≈bull-longs → confounds as an ATR edge that's really a sleeve edge): ① **📊 Performance by Pair ATR(14)%** · ② **📊 ATR × Hold-Time Cross-Tab** · ③ **⏱ Hold-Time × ATR** (the EFFECT side: WR/RegChg%/avg-ATR by duration) · ④ **🎲 Regime-Drift × Outcome** (the decisive HELD-vs-DRIFTED "drift→random" test — early read says drift HURTS longs / NOT fade-shorts, confirm on clean data) · ⑤ **🔀 Regime-Change-Exit Counterfactual — PRIORITY DEEP-DIVE (operator 2026-06-22: "might cut losers earlier")** (exit@flip vs hold-to-SL; net-$ cols added 2026-06-21 — $ for magnitude WITHIN batch, Avg% cross-batch). GOAL: decide whether to flip `regime_change_exit` ON for a specific cohort = bail when BTC regime turns mid-trade instead of riding to SL. ⚠️ **CRITICAL CAVEAT (diagnosed 2026-06-22 — the table is NOT what it looks like):** it fires on the **3-value MACRO regime** (`determine_macro_regime`, EMA20-slope BULLISH/NEUTRAL/BEARISH) going NEUTRAL/opposite mid-trade — which is a DIFFERENT system than the FINE regime (STRONG_BULL→BULL_EXHAUSTED→CHOPPY) every other table uses. Consequences: (a) it's **structurally LONG-biased** — counter-trend shorts (PAIR_RSI_OB, adverse macro AT ENTRY) can never produce a favorable→neutral→opposite sequence, so they show **0 rows** even though they drifted in fine-regime terms (19 shorts drifted, 0 stamped); (b) `regime_neutral_pnl`/`regime_opposite_pnl` only populate for trades that lived through a macro-NEUTRAL window mid-trade. So the counterfactual currently answers "should LONGS bail when the macro TREND flips" — NOT a general loser-cut. For the analysis: split OPPOSITE(adverse) vs NEUTRAL(soft) — adverse mostly dies (exit wins), soft recovers ~⅓ (exiting chops comebacks); a positive Δ (hold−exit) means exiting earlier WOULD have helped. If the longs-only cut is strong, consider either shipping `regime_change_exit` long-only OR re-basing the trigger on the FINE regime to capture the shorts too (bigger change — flag before building). All pure read-only views (no engine/config). Early reads to date are on the MIXED pre-reset pool (N=278, scaffolding only) — judge on the fresh single-config data. Supersedes/consolidates the 06-20 'ATR-THEORY TABLES' build note.
- **📊 ENTRY-CONDITIONS BY STRATEGY TABLES — SHIPPED 2026-06-23 (analytics-only, use for future per-sleeve reads).** Two new report tables below "Entry Conditions by Outcome": **"Entry Conditions by Strategy"** (one row per sleeve×direction) and **"Entry Conditions by Strategy — Winners vs Losers"** (W/L profile within each sleeve). Same wide column set (RSI/ADX/ADXΔ/gaps/fan/stretch/slopes/BTC-RSI/ADX/ATR/RngPos/Breadth/GlobalVol/peak/avg%/Total$/SLProf/Duration). Groups by `entry_strategy` (FLIP: prefix stripped → FAN_RATIO_GATE/PAIR_RSI_OB/LONG_UNMATCHED_ONLY · BULL_LONG · BOUNCE_LONG · MOMENTUM). **This NATIVELY answers the per-sleeve winner-vs-loser separator question we kept hand-digging from the CSV (e.g. the FAN-flip pADX≥20 finding) — read the W/L table first each batch to spot a dimension where winners and losers diverge within a sleeve.** Server: nested `_ec_row(group, direction)` in `_compute_performance` (full field parity, SL-profile over the group's losers); payload `entry_conditions_by_strategy` + `_strategy_outcome`. D12 full (UI 2 tables + clipboard + saved-file exports; render wrapped in try/catch). Read-only, zero engine/risk. → DECISION_LOG 2026-06-23.
- **⚡ NEXT-BATCH — FAST-EXIT COUNTERFACTUAL **PER-SLEEVE** (operator 2026-06-23; analytics-only, no ship).** The 🔀 Fast-Exit Counterfactual is currently aggregate / direction-split only — re-read it (or re-run from the orders CSV) **split by `entry_strategy` sleeve: FAN_RATIO_GATE · BULL_LONG · MOMENTUM · PAIR_RSI_OB · BOUNCE_LONG.** GOAL: find WHICH sleeves benefit from a fast-exit (bank +X% within N-min of entry) vs which it just caps. Method per sleeve: for each closed trade, fast-exit fires if `peak_pnl ≥ threshold` AND peak hit within the window of `opened_at`; compare actual$ vs fast-exit$ → Δ$ + how many losers SAVED (peaked green then died) vs winners CAPPED. Grid 0.2/0.3/0.4% × 1/2/5min (0.2%/5min was best aggregate). EARLY READ (J22 hand-calc, FAN flips only): mild — +$246 on the losing batch but only +$58 on the winning batch while capping 3 winners → weak/asymmetric for FAN (we shipped the pADX floor instead). The OPEN question this answers: is fast-exit a real lever for the OTHER sleeves (esp. PAIR_RSI_OB straight-to-SL drainers, or BULL_LONG/BOUNCE long-side) — or universally a winner-capper? Decide per-sleeve, do NOT ship a global fast-exit. Pure read-only.
- **🔬 NEXT-BATCH DEEP-DIVE — the two 1× observation sleeves (2026-06-21, no ship; both running de-risked, hunting their entry condition).** Both are LIVE at 1×/0.05-lev OBSERVATION (not sized) precisely to collect this data — analyze each on the fresh batch against its pre-committed tracking gate (full evidence + revert clauses in the dated entries below; do NOT duplicate them here): ⓐ **🐂 BULL-LONG deep-dive** (entry above, ~line 91) — the cross-batch dig says the driver is **BTC momentum/extension, NOT fan/regime**: test the 2 tracking gates on fresh data ① BTC EMA13-EMA50 gap≥+0.06 (the −$430 loser zone → ship `bull_long_btc_gap_max≈0.06` if it stays ≤40% WR) · ② BTC 30m-RSI Δ≥+12 (the +0.149% winner band → becomes the entry condition if it holds ≥65% WR); SPLIT ×regime to test regime-invariance. N=52 was ~90% single-batch (J20) → the fresh batch is the confirmation. ⓑ **🐻↩ BOUNCE-LONG deep-dive** (entry below, ~line 96) — first 2 LIVE fires BOTH LOST (NEAR/JTO, falling knives mid-crash) → the open question is the **exhaustion/turn gate**: does the bounce only pay once BTC 30m-RSI Δ is RECOVERING (not still deeply negative)? Track btc_rsi Δ on each fresh fire + the <3-distinct-washout-EVENTS clustering test (the make-or-break the phantom couldn't answer). DO NOT widen `bounce_long_regimes` to STRONG_BEAR (more falling knives).
- **⏱ NEXT-BATCH — PAIR_RSI_OB ATR-DRIFT THEORY (2026-06-22, NO SHIP — cross-batch test FAILED, keep observing).** The hypothesis: after the gap≥1.0 filter, the residual PAIR_RSI_OB losers are **LOW-ATR majors** (BTC/LTC/LINK/XRP, ATR 0.18-0.29%) that grind 40-88min → BTC regime drifts from entry → straight-to-SL (peak ~0, never armed); winners are higher-ATR (0.26-0.49) that resolve fast + arm. This IS the operator's ATR→speed→drift theory (low ATR = slow → random) and is the LOW-side mirror of the gap filter's HIGH-side parabola cut. **06-22 in-sample looked clean (gap<1.0: ATR<0.25 = 1W/5L vs ATR≥0.25 = 7W/1L) — but the cross-batch test (deduped PAIR_RSI_OB N=136, gap<1.0 N=76, Jun17-22) does NOT support a 0.25 floor:** per-day WR is INVERTED on 06-18 (low 50% vs high 25%) and 06-21 (low 100% vs high 75%), flat 06-19/06-20, clean only 06-22 → 1-of-5 batches, fails the ≥3-direction-consistent bar. **A FLOOR at 0.25 fails cross-batch** (inverts 06-18 & 06-21, clean only 06-22) — partly because "≥0.25" dilutes the good band with a bad one. **Measured CORRECTLY on the gap<1.0 cohort (the live-filtered universe), the shape is a two-sided ATR WINDOW, not a floor:** <0.25 = 34/38%/−$546 (low tail, grinders) · **0.25-0.50 = 29/~48%/+$55 (the ONLY positive band)** · 0.50-1.0 = 10/20%/−$361 (high tail — volatile-but-not-parabolic pairs AVAX/HYPE/ORDI/VELVET that PASS the gap filter yet still whip). So both tails lose inside the filtered cohort, not just the gap≥1.0 parabolas. **DO NOT SHIP** — window is +$55/N=29, marginal + today-heavy. NEXT-BATCH TASK: on fresh PAIR_RSI_OB gap<1.0 survivors, re-check (a) does ATR<0.25 keep losing, (b) does 0.50-1.0 keep losing, (c) does 0.25-0.50 stay the positive band — if the WINDOW (not floor) holds ≥3 batches, consider a two-sided ATR gate. Full evidence → this session's analysis.
- **✅ KEY REFRAME (2026-06-21 filter-overlap audit): FAN flip-short is ALREADY net-positive under the CURRENT filter stack.** Survivor-sim across J17/18/20/21 (apply live filters to the historical fires): 141 fired → **68 blocked → 73 survive = +$1051**. The work is done by already-shipped filters: **BTC30_RISE blocks 42, RSI_MIN<55 blocks 21, U3/FAN_LOATR blocks 14.** So the raw −$471 figure is stale — DON'T re-filter a green sleeve. Only red survivor batch = J21 (4 trades, 0 blocked by current stack, −$287). **Watchlist plug (thin, N=5 marginal):** block FAN flip-short when BTC EMA13-EMA50 gap ∈ [−0.10,+0.05] × BTC ADX 30-35 ("flat-but-convicted bear chop"). On SURVIVORS it cuts only 5 (J21 −287→−118, J20 +146, J18 +29) for +$343 pooled → it's the one hole the current stack misses (J21's S.BEAR SYN/VELVET/EPIC). N=5 marginal = TRACK one more batch; ship only if strong-bear ADX 30-35 leaks again. (Slope×ADX = same cell via abs-slope; gap version is sign-clean.)
- **🔭 FAN flip MULTIPLIER candidate — pair EMA20 slope 0.20-0.30 (2026-06-21, TRACK, no live sizing).** `entry_ema20_slope` 0.20-0.30 = N=27/67%WR/+0.354%avg/+$1097, **positive 3/3** (J17 +572, J18 +48, J20 +477). FAILS the locked multiplier gate (N=27<30, WR=67%<70%) → not yet. **Caveats:** (a) loser $ is MET-concentrated (MET ×3 = ~52% of the cell's loss, a repeat gapper) → cap/blacklist MET before blanket-multiplying; (b) +$1097 is RAW — re-measure on CURRENT-filter survivors before sizing; (c) BE is OFF so the BE-compat rule is moot. **PROMOTE: at N≥30 (survivor-pool) + WR≥70%, ship 1.5× first (staging), step to 2× after +50 trades — never jump straight to 2×.**

### 🗓 2026-06-20 batch (NO ship — observation carries forward)
- **🔭 FAN FLIP-SHORT loser-separator HUNT — ALL 6 candidates REJECTED cross-batch (2026-06-20; NO change made, operator: keep collecting).** Re-evaluate the FAN flip-short disaster (last-6h −$937; batch 13-13-05) once **N≥2 fresh full batches accrue**, OR another low-BTC-vol grind episode occurs. **Candidates tested (FAN flip-short only, Jun17 +$1039 / Jun18 −$998 / Jun20 −$20):** ① **U1 vol<0.15 × BTC ADX>30** — cut +$335 of Jun-17 *profit* (over-blocks normal). ② **U2 pADX-fall × BTC-ADX-rise** — best precision live (5L/1W last-6h, −$566) BUT cut **2:1 winners** in Jun-17/18 (4W/2L, 8W/4L), Δ≈$0 — no edge out-of-sample; mechanism sound (divergence: pair losing downside momentum while BTC trend strengthens up → squeezes the fade). ③ **U3 BTC vol<0.10** (= "Performance by BTC Volatility Regime <0.10%" bucket = `entry_btc_atr_pct<0.10`) — **0 trades in both prior batches** (regime absent → unvalidatable, 1-episode), AND misses the 3 biggest last-6h losers (RIF/BICO/HEI at vol 0.103-0.107, just above the line); adjacent 0.10-0.15 band flips sign cross-batch (Jun17 bear +$876 / Jun18 −$344). ④ **BTC ADX 30-35** — non-monotonic mid-range hole flanked by winners (confound); threshold ≥30 flips sign (Jun-17 those were winners). ⑤ **pair ADX 22-25** — N=8-10/batch, already ~70% covered by U2∪U3; only incremental is ORDI −$128 at cost of 2 winners. **U2 ∪ U3 together (the idea we were about to wire):** current batch cut 24/50 (48% of sleeve, **11W/13L**) Δ+$906; last-6h cut 16/17 → leaves −$12; BUT prior batches Δ−$31 / +$13 cutting **mostly winners** → it flags the *regime*, not winners-vs-losers. **CORE FINDING (the reason all fail):** FAN flip-short is a **~zero-edge fat-tailed coin flip** — pooled +$1039 −$998 −$20 = **+$21 net / 126 trades**, avg ≈−0.005%/trade; winners & losers are indistinguishable at entry (bearish W-vs-L: BTC RSI 40.5 vs 35.8, ATR 0.96 vs 1.08 — no separator). Per-pair: loss diffuse (top-2 = 41% < 60% gate) AND no cross-batch-consistent loser (VELVET/JTO/XLM/XPL/SKYAI all won ≥1 batch; this batch's worst MET/HEI have 0 prior history). **The only thing consistent across all 3 batches = the 20× gap-through-SL mechanism, not any entry variable, regime, or pair.** Scope note: if any candidate IS ever shipped → **FAN-flip-only**, NOT all flips (U3 on PAIR_RSI_OB cuts 54% of that sleeve for ±$7; its 0.05× size has no gap-mechanism + its real losses were at vol 0.13-0.15). **DEFERRED real lever (NOT done — operator chose do-nothing/keep-collecting over the proposed 20×→1× de-lever): size the FAN sleeve down (de-lever is pure $ scaling, leverage-invariant — halves the −$937 to −$469 @10× / −$234 @5× without changing the ~0 expectancy; it's ruin-risk control, not an edge fix) OR pause the sleeve.** Full evidence + per-trade tables → DECISION_LOG 2026-06-20.
- **📊 ATR-THEORY TABLES — BUILT 2026-06-21 (3 tables; ANALYZE on the fresh batch).** Surfaces under 'Performance by Pair ATR(14)%': ① ATR × Hold-Time cross-tab (commit 80c4a20) · ② **⏱ Hold-Time × ATR** (duration-primary, the EFFECT side: WR / RegChg% / avg-ATR by duration band) · ③ **🎲 Regime-Drift × Outcome** (HELD entry==exit vs DRIFTED entry≠exit, × direction — the decisive "drift→random" test). EARLY READ on the mixed pre-reset pool (N=278, **scaffolding only**): avg-ATR falls monotonically with hold (1.13%→0.24%) & RegChg% climbs (26%→100%), BUT WR does NOT cleanly decay; **drift HURTS longs (HELD 55% vs DRIFTED 40%/−0.25%) but does NOT hurt fade-shorts (HELD 51% vs DRIFTED 53%/−0.00%)** — a fade WANTS the reversal, which IS a regime change → the "drift→random" thesis is **LONG-only**. Confirm on clean single-config data. ─── orig theory + validation: THEORY (operator): high ATR = fast-resolving trades (hit the fixed ±% stops quickly, while the entry thesis still holds); low ATR = slow trades where BTC conditions drift from entry → random outcome. VALIDATED on pooled N=278: **ATR→speed CONFIRMED monotonic** (median hold: <0.5% ATR = 28m → 0.5-1 = 11m → 1-1.5 = 6m → 1.5-2.5 = 4m → >2.5 = 2m); **regime-drift CONFIRMED monotonic by hold-time** (entry≠exit BTC regime: <10m = 26% → 10-20 = 45% → 20-40 = 67% → 40-90 = 74% → **>90m = 100%**); lowest-ATR <0.5 = 50%WR/−0.09 coin flip w/ 63% drift. **CAVEATS: (a) WR does NOT cleanly decay with hold (zig-zags 48→60→45→59→42%) — randomness shows at the EXTREMES, not monotonically; (b) STRATEGY CONFOUND — high-ATR ≈ FAN shorts (runner edge), low-ATR ≈ bull-longs/momentum → SPLIT BY DIRECTION is mandatory or it reads as an ATR edge that's really a sleeve edge.** BUILD: `_compute_atr_holdtime_crosstab(orders)` in main.py; rows = ATR bands (<0.5/0.5-1/1-1.5/1.5-2.5/>2.5) × dur cols (<10/10-20/20-40/40-90/>90m), cell = N/WR%/avg%, LONG & SHORT split in-table, marginals per ATR row = median-hold + regime-change%; placed under 'Performance by Pair ATR(14)%'. Data = existing Order columns (`entry_atr_pct`, `opened_at`/`closed_at`, `entry_btc_regime`/`exit_btc_regime`, `pnl_percentage`) → **ZERO new columns**. D12: UI table + BOTH text-report exports (grep-verify 2 export blocks). Pure read-only view (no engine/config). Review/build next batch.
- **🥇 TOP RE-EVAL CANDIDATE — ADXΔ < −0.5 ＞ U2∪U3 (2026-06-20).** Of every FAN flip-short separator tested this session, **block flip-shorts on collapsing pair-ADX (`flip_adx_delta_min=-0.5`) is the strongest — the ONLY one net-positive on all 3 batches INCLUDING the winning Jun-17** (Δ+$644 / +$527 / +$687; Jun-17 keep improves to +$1,683). No sign flip: both loser sub-buckets negative all 3 batches (<−1.0 = −$259/−$495/−$167 · −1.0..−0.5 = −$385/−$31/−$520); −0.5..0 is mixed (+$213/−$84/+$366) → cut at **−0.5 not 0**. Mechanism: flips BYPASS the live `Pair ADX Dir S: rising` short filter → enter on a collapsing move with no momentum → run over. **Ranks ABOVE U2∪U3** (which flagged regime-not-outcome / cut 2:1 winners out-of-sample). **CAVEATS (why still watchlist, not clean ship): (a) trips the locked "high-WR-but-net-losing → fix sizing, don't block" rule on Jun-18 (blocked cohort 63% WR, 12W/7L, −$527 — cuts many winners); (b) it IS the 2026-06-17 ADXΔ<−0.5 candidate that was DOWNGRADED — the BE-ratchet rescues the armed reversers, residual = never-armed straight-to-SL = the SAME 20× gap fat-tails (PORTAL/XLM/NEAR repeats), which ADXΔ flags alongside a pile of winners.** **GATE: at live N≥30 fresh — ship `flip_adx_delta_min=-0.5` (discipline-override + tight revert) ONLY IF the cut stays net-negative AND ≤50% WR AND its residual loss is NOT 1-2 repeat pairs; if it's high-WR-net-losing (the Jun-18 shape) → treat as a SIZING fix (de-lever the fat-tail), not a block.** Corroborates + supersedes the 06-17 ADXΔ<−0.5 entry below.
- **✅ FLIP_SHORT_PAIR_GAP — pair EMA13-EMA50 gap≥1.0 block SHIPPED 2026-06-21 (FAN-effective; N=16 DISCIPLINE-OVERRIDE; commit 62d2d3b).** `flip_short_pair_gap_max=1.0` — block flip-SHORT when pair (EMA13−EMA50)/EMA50% ≥ 1.0 (don't fade a parabola already steeply above its own 4h trend → never arms, 20× gaps the SL to ~−1.2%). **ONE-SIDED (positive tail only)**: a big NEGATIVE gap is with-trend momentum that WINS (≤−1.5 = +0.79%) → NOT blocked (symmetric |gap| tested + rejected: keeps less $, clips negative-side winners). Cross-batch FAN survivors (J17-21 deduped): **gap≥1.0 = 16/44%WR/−0.359%/Σ−$461, net-negative every batch**; 0–1.0 = the fade sweet spot (19/87%WR/+0.45%). Subsumes the ATR≥2.0 loser cohort and **dominates+REPLACES** the BTC-gap×ADX-30-35 plug (now shelved); lifts the survivor pool **68%/+0.095% → 74%/+0.220%/+$1,401**. Engine: shared flip-SHORT block in `_flip_filters` (counter `FLIP_SHORT_PAIR_GAP`; **`pair_gap` added to `_ff_in`** to avoid the silent no-op trap — same bug class as the PAIR_RSI_OB ADX floor; PAIR_RSI_OB returns early so it's unaffected). D11 full. **TIGHT REVERT: `flip_short_pair_gap_max`→0 if blocked gap≥1.0 flip-shorts hit ≥50% WR on N≥10 fresh.** Supersedes the prior 🔭 pair-gap watchlist obs. → DECISION_LOG 2026-06-21.
- **🩸 FAN flip-short negative-gap −1.2% bleed = VARIANCE, no filter (2026-06-21 dig, CLOSED).** The dominant remaining FAN pain (BTR/VELVET/EPIC straight-to-SL) profiled on the post-gap-filter kept pool (58/74%/+$1,401, 15 losers): 11/15 are `FLIP_STOP_LOSS` straight-to-SL gaps, **NOT pair-concentrated** (worst-2 = 38% < 60%), no clean dimension (pair-ATR Δ tiny; only a faint low-vol/high-BTC-ADX tilt), and VELVET is actually a net WINNER (4/75%/+$68). Conclusion: irreducible 20×-short tail → **de-leverage territory (deferred scaling roadmap), not an entry filter**. Do NOT re-hunt.
- **✅ U3 — FAN flip-short sub-0.10 BTC-ATR block SHIPPED 2026-06-20 (FAN-only; N=14/1-weekend DISCIPLINE-OVERRIDE) *(staged — pending push)*.** `flip_fan_btc_atr_min=0.10` — block FAN flip-SHORT when entry BTC ATR% < 0.10 (weekend thin-liquidity regime; 20× gap-through-SL fat tails). Gate in `_flip_filters` FAN/SHORT branch (counter `FLIP_FAN_LOATR`; added `btc_atr_pct` to `_ff_in`). EVIDENCE: Jun20 Sat sub-0.10 = N=14/36%WR/−$775, all straight-to-SL gaps; weekday Jun17(Wed,min0.109)/Jun18(Thu,min0.122) never dipped <0.10 → regime is **weekend-only** (day-of-week common cause). Meets Pattern-C→FILTER on WR/Avg%/NP, fails only N (14<30) + one weekend. **FAN-ONLY** (PAIR_RSI_OB's sub-0.10 losers were the ADX-floor bug, not clean evidence; different macro regime; LONG_UNMATCHED no data). D11 full (config.py + json + engine + UI cyan input/load/save). **TIGHT REVERT: set `flip_fan_btc_atr_min`→0 if next weekend's blocked sub-0.10 fades would be ≥45% WR on N≥6 (phantom tracker still observes the blocked side).** Widen to universal only on a future weekend's real (non-bug) sub-0.10 losses in other sleeves. Full evidence → DECISION_LOG 2026-06-20.
- **🐞 PAIR_RSI_OB ADX≥40 floor was a NO-OP until 2026-06-20 — FIXED *(staged)*.** The gate read `ind.get('adx')` but `_ff_in` never populated pair `adx` → always None → never blocked; the sleeve ran unfiltered at 20× since 840e913. Added `adx` to `_ff_in`. Now blocks pADX<40 (would have cut 35/43 shorts, −$427, this batch). **The 40+ cell's tight-revert clock (N≥15) effectively starts now** — first window it's the only thing firing.
- **🔓 PAIR_RSI_OB seed DECOUPLED from BTC_ADX_GATE_HIGH — SHIPPED LIVE (de-risked) 2026-06-20 *(staged — pending push)*.** `flip_pair_rsi_ob_btc_adx_high_mode=live`. The overbought-fade seed was accidentally choked above BTC ADX 40 (it inherited the long pipeline's `BTC_ADX_GATE_HIGH` veto, but the fade is a SHORT — the long's ADX ceiling is irrelevant, and overbought pairs are richest when BTC trends hardest). Recorder now seeds THROUGH a `BTC_ADX_GATE_HIGH`-only veto (phantom always; live when mode=live). The >40 cohort first ran de-risked at 1× (Jun20 batch: N=32/66%WR/−$9, de-risk saved ~$173). **PROMOTED to full 20× 2026-06-21** (de-risk removed in `_flip_filters`) after it held at the raised pADX≥45 floor (45+ = 17/82%WR/+$347/+0.20%, BE-compat 67%). Now all pADX≥45 STRONG_BULL fires at 20× regardless of BTC ADX; mode=live still gates the >40 SEED. N=17/one-batch override. **TIGHT REVERT: mode→off (stops the >40 seed) AND/OR floor→40 if the next ~6-8 fresh 45+ fires drop <65% WR, OR ≥2 gap straight to SL at 20×, OR BE-compat <60%.** Builds on the floor fix above.
- **🧱 PAIR_RSI_OB pair-gap≥1.0 PARABOLA BLOCK — SHIPPED 2026-06-22 (N=22 DISCIPLINE-OVERRIDE; staged — pending push).** `flip_pair_rsi_ob_pair_gap_max=1.0` — block PAIR_RSI_OB flip-SHORT when pair (EMA13−EMA50)/EMA50% ≥ 1.0 (counter `FLIP_PAIR_RSI_OB_GAP`, own field/clock — PAIR_RSI_OB returns early so it skipped the FAN `flip_short_pair_gap_max`; replicated inside its branch). The sleeve was the batch drainer (22/41%/−$983, all S.BULL). Per-trade dig: losers gap-DOWN (peak<0.20, never arm → 20× SL ~−1.2%), and the loss is dimensionally-INSEPARABLE per-pair (EIGEN 3W/5L overlap on every entry var; only outcome `peak` splits them) → so the lever is entry-extension, not exit. gap<1.0 = 3/100%/+$97 vs gap≥1.0 = 19/32%/−$1,080 (catches ALL 13 losers) → flips cohort **−$983 → +$97** (forfeits 6 inseparable winners +$288, shrinks sleeve to ~14% pass = near-shuts it in S.BULL, OK since net-losing). Mirrors the cross-batch FAN gap≥1.0 filter (N=16 J17-21, same anti-parabola mechanism) → extends a proven gate, not a fresh fit. Exit-tightening REJECTED (fast-exit 0.20/2min CF +$502 is 4/5 SYN → redundant with the blacklist; immediate-gap losers can't be exit-saved). D11 full. **TIGHT REVERT: `flip_pair_rsi_ob_pair_gap_max`→0 if blocked (gap≥1.0) PAIR_RSI_OB fades show ≥50% WR on N≥10 fresh.** SYN also blacklisted same day (redundant belt-and-suspenders, see pair_blacklist). → DECISION_LOG 2026-06-22.
- **🚫 PAIR_RSI_OB DISABLED — 2026-06-23 (removed from `flip_entry_sources` → `"FAN_RATIO_GATE:1.0"`).** Every lever now exhausted and the verdict converges: under the FULL current stack (gap<1.0 + pADX≥45 + STRONG_BULL) the survivors are a **zero-edge breakeven (N=22/64%WR/+0.003%)** — the filters extracted whatever edge existed, leaving nothing positive. No remaining lever recovers an edge: **fast-exit fails** (76% of losers go straight-to-SL / peak<0.2% so it can't fire; net +0.04%/trade = noise, caps as many winners as it saves); **dist-from-EMA13 is a real cross-batch separator but 74% redundant with the gap filter and adds 0 over the full stack** (only 1 survivor has dist≥0.7, a winner). Disabled rather than keep a breakeven sleeve on the monitor. Phantom seed stays decoupled (always fires) so it keeps tracking for a future re-enable. **RE-ENABLE only if the phantom shows a cross-batch-STABLE (≥3 batch) winner/loser separator the live filters don't already capture.** Config-only (existing field). → DECISION_LOG 2026-06-23. ─── prior de-lever context below:
- **🛑 PAIR_RSI_OB DE-LEVERED 20×→1× (lev 1.0→0.05) — 2026-06-22 (Option B retire-to-observation).** `flip_entry_sources` `PAIR_RSI_OB:1.0:1.0`→**`PAIR_RSI_OB:1.0:0.05`**. The sleeve **failed its locked source-revert gate decisively** (live 33/42%WR/−0.32%avg; deduped cross-batch N=136 net-negative) AND **every entry/exit lever is exhausted cross-batch**: gap≥1.0 (real, KEPT — removed the −$1,080 parabola tail) but the residual is still net-neg & unseparable; pADX≥45 decayed; RSI buckets all neg; regime already S.BULL-gated; ATR floor inverts cross-batch (window 0.25-0.50 marginal +$55/N=29, today-heavy); fast-TP only helps already-blocked SYN. Diagnosis = ~zero-edge fat-tailed coin flip (like FAN-flip-short); only constant across batches is the 20× gap-through-SL mechanism + broken R:R (wins +0.2-0.5 / losses full −0.7 to −1.2). Kept firing at **1× observation** (not disabled) so live+phantom data keeps accruing; STOPPED bleeding at 20×. **RE-LEVER only if a cross-batch-STABLE (≥3 batch) winner/loser separator emerges** — do NOT re-lever on another in-sample finding. Gap filter + the analytics tables stay. → DECISION_LOG 2026-06-22. ─── prior 20× ship context below:
- **🔀 PAIR_RSI_OB FLIP — pADX FLOOR now 45 (RAISED 33→40→45; 45 on 2026-06-21) + 20× SHIPPED *(staged — pending push)*.** [Jun21: floor 40→45 — 40-45 band = 17/47%/−$605 loser, 45+ = 17/82%/+$347 winner in the >40-BTC-ADX regime; see decouple line above + DECISION_LOG 06-21.] Original Jun-20 ship below: Two changes: ① `flip_pair_rsi_ob_adx_min=40.0` — fire the overbought-fade short ONLY when entry pair ADX≥40 (counter `FLIP_PAIR_RSI_OB_ADX`, engine gate in the PAIR_RSI_OB branch of `_flip_filters`, after the regime gate). ② `flip_entry_sources` PAIR_RSI_OB lev `0.05→1.0` = **full 20×** (was 1× de-risked obs). EVIDENCE: shipped first at ADX≥33 (then 9/89%/+$698), but the 33+ cell **decayed to 13/62%/+$431** AND split-bucket showed the edge is ADX≥40 only — **33-40 = N=5/20%WR/−$237 (loser band) vs 40+ = N=8/88%WR/+$668** → operator raised the floor 33→40. **⚠️ Still N=8 in-sample (<< N≥30 gate) + 20× (skipped the 1.5×-first staging) + this is a 2nd finer re-cut on a decaying cell (analyst flagged the re-overfit risk; operator chose 40 anyway).** RISK: counter-trend fade (short overbought pair in STRONG_BULL) at 20× = gap-through-SL profile. **TIGHT REVERT (N=8 + 20× counter-trend): set `flip_pair_rsi_ob_adx_min`→0 AND/OR lev `1.0→0.05` at live N≥15 NEW PAIR_RSI_OB shorts if the 40+ cell WR≤70% OR avg≤+0.05%; INSTANT de-lever (→0.05) if any single S.BULL fade gaps the SL past ~−1.0% OR 3 of the first 6 hit FLIP_STOP_LOSS.** D11 full (config.py + json + engine gate + UI input/load/save). Analytics: the 🎯 PAIR_RSI_OB×Pair ADX table now splits 33-36/36-40/40-45/45+, and the ADX≥45×RSI cross-bucket (retitled 40→45 on 2026-06-21 to mirror the live floor; key `adx45_rsi`) tracks the kept cohort. Lives alongside the [PAIR_RSI_OB RE-ENABLED 2026-06-19] entry below (regime gate STRONG_BULL unchanged).
- **🐂🎚 BULL-LONG — PER-REGIME FAN WINDOW SHIPPED 2026-06-24 (operator-directed; new field `bull_long_fan_by_regime`).** `STRONG_BULL:1.35-2.0,HEALTHY_BULL:2.0-3.0` — the gated regime's window OVERRIDES the global `bull_long_fan_min/max` (left 0.85/5.0 as a fallback for any allowed-but-unmapped regime; with both bull regimes mapped it's non-binding). EVIDENCE — cross-batch dig (N=106 deduped, current sleeve filters): the winning fan band is **regime-specific** — `STRONG_BULL × 1.35-2.0 = 11/73%/+5.1%` (3 dates), `HEALTHY_BULL × 2.0-3.0 = 24/67%/+3.9%` (6 dates), while `HEALTHY_BULL × 0.85-1.35 = 25/28%/−12.0%` ≈ the ENTIRE sleeve loss (the band the 06-23 widening imported). **Indicators do NOT separate within either regime** — BTC RSI flat (62.0 vs 61.9 W/L), ADXΔ & PairTrend *inverted* in HEALTHY (high-ADXΔ/above-trend tails = the losers), STRONG_BULL N=16 too small + PairTrend sign-flips vs HEALTHY = confound. So **fan×regime is the only axis**; this replaces the global-fan approach. Engine: `_maybe_open_bull_long` now resolves regime BEFORE the fan check, then applies the per-regime window. D11 full (config default+comment · json · engine · UI text input `config-bull-long-fan-by-regime` · load/save). **REVERT: drop a regime's entry if its windowed cell runs ≤45% WR on N≥10 fresh.** **2026-06-24 follow-ups:** global `bull_long_fan_min/max` set to the union **1.35 / 3.0** (was 0.85/5.0) so the inert fallback is consistent, not contradictory; and the report Bull-Long×Fan "LIVE ✓" marker is now **regime-aware** (`_bull_long_fan_live()` in main.py — union of per-regime windows, global fallback; replaces the old global-fan_max check in both the live fan-bucket table and the phantom curve). → DECISION_LOG 2026-06-24.
- **🐂🔺 BULL-LONG — RE-LEVERED 1×→20× 2026-06-24 (operator-directed, ON the cleaned per-regime fan cell). ⚠ DISCIPLINE-OVERRIDE.** `bull_long_lev_mult` 0.05→**1.0 (20×)**. Live cell now = S.BULL × fan 1.35-2.0 **OR** H.BULL × fan 2.0-3.0, @20×, flat −0.70 SL, cap 3. Below the locked re-lever gate (N≥30 + WR≥65-70%/avg≥+0.10% ×≥2 batches) — windowed cells N=11(73%)/N=24(67%), partly in-sample. Distinct from the 06-22 20× that tripped instantly: that was the OLD wide fan incl. the inverting 1.65-2.0/sub-1.35 bands; this ships only the cleaned cells. **TIGHT REVERT (override-grade): INSTANT de-lever →0.05 if 3 of first 6 new bull-longs hit SL OR any single gaps past ~−1.0%; de-lever at N≥10 fresh if windowed WR≤60% OR avg≤+0.05%.** Reset taken 2026-06-24 ~16:06 to apply (pre-reset snapshot: reports/orders_2026-06-24_prereset_preregimefan_20x_39trades_-178.csv). → DECISION_LOG 2026-06-24.
- **🐂🧪 BULL-LONG — REVIVED AS ZERO-RISK OBSERVATION TEST 2026-06-23 (operator).** `bull_long_enabled=true` @ **1× obs (`bull_long_lev_mult=0.05`)**, scoped **S+H BULL × fan 0.85-5.0** (operator widened via UI 2026-06-23 to collect data across the FULL fan range — incl. the <1.35 [25%WR] & ≥3.0 confirmed-loser tails; cheap at 1× obs, gives the complete live bucket picture), **cap 3** concurrent, and a NEW **flat fixed SL `bull_long_fixed_sl=-0.70`** (re-sim: flat −0.70 beat the live ATR-widened −1.20 for this cohort, −0.151→−0.078; stamped via the existing PATTERN_FIXED_SL exit path in open_position's bull_long block, fires before −1.20). PURPOSE: settle the bull-long question under the BEST exit at ~$0 cost — does fan 1.35-3.0 in bull flip viable once it has the tight flat SL? Excludes the confirmed losers (<1.35 = 25%WR, ≥3.0, CHOP, bear). **DECISION GATE (locked): N≥30 per regime (S.BULL/H.BULL tagged) across ≥2 batches; a cell graduates to consider-leverage ONLY at WR≥65% AND avg≥+0.10% cross-batch under the flat SL — NEVER re-lever on test data alone (the 06-22 mistake). Prior is negative (probably a non-edge); outcome is either a found long-sleeve OR the question closed for good → commit to Donchian.** D11 full (`bull_long_fixed_sl` new field + engine + UI + load/save). → DECISION_LOG 2026-06-23.
- **🔭 BULL-LONG separator candidate — pair-RSI ceiling ~61 (2026-06-24, WATCHLIST → REFUTED cross-batch; superseded by the per-regime fan ship above).** The single-batch RSI≥62 read did NOT survive the N=106 cross-batch dig: BTC/pair RSI is flat W/L within both regimes (no monotonic gradient; 55-60/60-65/65+ all ~50% in HEALTHY_BULL). The real, multi-date separator is fan×regime (shipped). Keep RSI as a dead lead, do not ship `bull_long_rsi_max`. ─── original watchlist: FIRST coherent bull-long winner/loser separator — readable now that the flat −0.70 SL normalizes losses (all 9 losers this batch capped at −0.70 via PATTERN_FIXED_SL, exit-variance no longer drowns the entry signal). On the 06-24 batch (N=21, 12W/9L, ~breakeven +0.001%/trade): **pair RSI ≥ 62 = 5L/0W** (all hit the SL); blocking it flips the cohort **57%→75% WR, +0.001%→+0.222%/trade** (removes 5 losers, ZERO winners). Mechanism: RSI≥62 bull-longs = overbought top-of-move chases. **⚠ range-position is NOT the cut** (winners reach rng 88-95 too; rngpos≥85 kills 6 winners — the W/L *average* diverged but the *threshold* test shows RSI is the clean knife, rngpos is not). Echoes the "don't chase extension" theme the pullback-long idea reached for, now visible. **SHIP a new `bull_long_rsi_max≈61` (block bull-long when entry pair RSI ≥ 61) ONLY IF RSI≥62 bull-longs stay ≤40% WR on N≥15 fresh across ≥2 batches.** ⚠ N=21 in-sample, only 5 losers caught, bull-long signals have sign-flipped batch-to-batch all week → confirm before shipping; do NOT act on this batch alone. → DECISION_LOG 2026-06-24. ─── prior DISABLE (now superseded):
- **🐂🚫 BULL-LONG — DISABLED 2026-06-23 (operator: focus the book on FAN + MOMENTUM; re-add later as a clean experiment). [SUPERSEDED by the revival test above.]** `bull_long_enabled=false`. The sleeve never found a stable edge — de-levered to 1× obs then the instant gate tripped same week (below); rather than keep a 1×-obs sleeve consuming book slots, turn it OFF and dedicate the max-5 to the proven FAN-flip + MOMENTUM/UNMATCHED longs. **Phantom Bull-Long Curve keeps tracking (independent of the toggle)** → re-enable as a fresh EXPERIMENT when a cross-batch-STABLE band/regime edge shows in the phantom. The de-lever (lev 0.05) + concurrency cap (3) stay set but are now inert. → DECISION_LOG 2026-06-23. ─── prior de-lever history below:
- **🐂🛑 BULL-LONG — DE-LEVERED 20×→1× obs — 2026-06-23 (INSTANT revert gate TRIPPED; operator-confirmed). [SUPERSEDED by DISABLE above.]** `bull_long_lev_mult` **1.0/20×→0.05/1×**. The 06-22 re-lever (below) failed its locked INSTANT gate on the VERY NEXT batch: **7 bull-longs / 29%WR / −$335, all HEALTHY_BULL; 4 of the first 6 hit SL (gate ≥3) AND two gapped past −1.0% (TNSR −1.19, ALLO −1.01).** The fan **1.65-2.0** sub-band inverted exactly as the cross-batch warning predicted (5/20%WR/−$264). Wins were +0.19/+0.27% vs losses −0.7 to −1.2% = broken R:R at 20×. ATR was NOT the separator (the 2 winners LINK/ONDO were the LOWEST-ATR 0.31/0.41; losers averaged 0.82 → argues against a low-ATR floor). Pre-committed gate = automatic, no debate. Band (S+H BULL × fan 1.35-2.0) + regime config UNCHANGED — only leverage cut. Now back to 1× observation collecting data. **➕ CONCURRENCY CAP added 2026-06-23: `bull_long_max_concurrent=3` (0=uncapped)** — reserve ≥2 of the max-5 slots for higher-conviction MOMENTUM/UNMATCHED longs; a 1×-obs bull cluster (06-23 opened 5-7 at once) must not monopolize the book. Engine gate in `_maybe_open_bull_long` (counts open BULL_LONG, skips if ≥cap, counter `BULL_LONG_MAX`). D11 full. Cheap insurance (only binds in clusters; watch the BULL_LONG_MAX counter for how often it fires). **RE-LEVER only at N≥30 cumulative + WR≥70%/avg≥+0.15% across ≥2 batches (cross-batch-STABLE) — never again on a single in-sample band.** → DECISION_LOG 2026-06-23. ─── SUPERSEDES the 06-22 re-lever below:
- **🐂🔺 BULL-LONG — RE-LEVERED to 20× + band restricted to fan 1.35-2.0 — 2026-06-22 (operator discipline-override, on a FRESH RESET batch). [REVERTED 06-23, see above.]** LIVE state: `bull_long_lev_mult` 0.05/1×→**1.0/20×**, `bull_long_fan_min` 1.0→**1.35**, `bull_long_fan_max`=**2.0**, `bull_long_regimes`=**STRONG_BULL,HEALTHY_BULL** (CHOP+bear excluded). So the live cell = **S+H BULL × fan 1.35-2.0 @ 20×**. EVIDENCE (06-22, bull-only this band): **8/88%WR/+0.554%avg** (the sweet spot; fan<1.35 = 5/60%/−$96 loser, fan>2 = SL losers). **⚠️ DISCIPLINE-OVERRIDE (acknowledged): N=8 < N≥30 gate + 0.05→1.0 is a 20× jump skipping the mandated 1.5× stage. Operator-directed; justified by FRESH-RESET = clean attribution (no old-config contamination) + strong in-band edge.** RISK: bull-long sub-bands have INVERTED cross-batch before (fan 2-3 = 90%/+$705 on 06-20 → −$452 on 06-22) → this 88%/N=8 could regress; 20× build-side long = gap-through-SL ruin-axis. **TIGHT REVERT (N=8 + 20× + cross-batch-unstable band): de-lever `bull_long_lev_mult`→0.05 at live N≥10 NEW bull-longs if fan 1.35-2.0 S+H-bull WR≤70% OR avg≤+0.15%; INSTANT de-lever if 3 of the first 6 hit SL OR any single gaps past ~−1.0%.** Config-only (existing UI). → DECISION_LOG 2026-06-22. ─── prior obs-arm history below:
- **🐂 BULL-LONG — converted to 1× OBSERVATION ARM 2026-06-21 (aperture-open to find the real driver); `bull_long_fan_min` 1.35→1.0 on 2026-06-22 (operator — widen the obs aperture; opened 1.0-1.35 band is a documented 20× loser [06-20: 14/36%/−$732], safe ONLY at 1×/0.05-lev obs).** `bull_long_lev_mult` 1.0/20×→**0.05/1×**, `bull_long_regimes` HEALTHY_BULL→STRONG_BULL,HEALTHY_BULL,CHOPPY_FLAT,CHOPPY_WEAK→**STRONG_BULL,HEALTHY_BULL (06-22 — dropped CHOPPY_FLAT,CHOPPY_WEAK: CHOP was the whole live drain 18/56%/−0.247%/−$482, >½ sleeve volume; bull regimes alone = 14/64%/+0.155%)** (NO bear — build-side long-into-bear is the opposite thesis; flip-LONG bear = 2/0%/−$220), `bull_long_fan_min` 1.65→1.35→**1.0 (06-22)**, `bull_long_fan_max` 3.0→5.0→**2.0 (06-22 — operator narrows LIVE aperture to fan 1.0-2.0, the current best-belief band; ⚠ fan 2-3 INVERTS cross-batch [06-20 winner 10/90%/+$705 → 06-22 loser], so this is in-sample, safe only at obs-lev 0.05× + phantom Curve still tracks 2-5)**. WHY: the cross-batch dig (N=52) proved **fan & regime are NOT the driver — BTC momentum/extension at entry is**: BTC 30m-RSI Δ≥+12 = 26/73%WR/+0.149% (winning band) · BTC EMA13-EMA50 gap≥+0.06 = 22/27%WR/−$430 (the whole loss). The 1.65-3.0/20× config was net-LOSING (J21 7/43%/−$192) → de-risk, open the aperture, TRACK the real cells before re-levering. **⏱ 2 TRACKING GATES (review EACH batch, split ×regime to test regime-invariance): ① BTC gap≥+0.06 — if stays ≤40% WR → ship `bull_long_btc_gap_max≈0.06` (Pattern-C, the −$430 zone). ② BTC 30m-RSI Δ≥+12 — if holds ≥65% WR → that becomes the entry condition.** N=52 is ~90% J20 (single-batch) → confirm on fresh batches first. **REVERT: any single bull-long gaps past ~−1.0% OR the arm <40% WR at N≥20 → cut back to HEALTHY_BULL / fan 1.65-3.0 / lev 0.05.** Config-only (existing UI). ─── SUPERSEDES the 20× ship (2026-06-20, N=15 DISCIPLINE-OVERRIDE) below: Three changes: `bull_long_fan_min` 1.35→**1.65**, `bull_long_fan_max` 10.0→**3.0**, `bull_long_lev_mult` 0.05→**1.0 (=20×)**. EVIDENCE (live Bull-Long × Fan): 0.85-1.0 5/0%/−$419 · 1.0-1.35 14/36%/−$732 · 1.35-1.65 10/50%/−$120 · **1.65-2.0 5/80%/+$175 · 2.0-3.0 10/90%/+$705** · 3.0-5.0 2/0%/−$206 · 5-10 1/0%/−$130 → **1.65–3.00 is the ONLY positive zone** (N=15 in the kept band). The fan restriction is itself a RISK REDUCER (cuts the documented losing bands, keeps only the two winners); the 20× then amplifies the now-filtered winner cell. **⚠️ N=15 < the N≥30 lock gate AND a 1×→20× jump skipping the 1.5×-first staging — same override profile as PAIR_RSI_OB; violates "ship one change at a time" (fan-min + fan-max + lev all at once). Operator-directed.** RISK: 20× build-side long; the sleeve was net-LOSING overall (47/49%/−$352) — the bet is that the 1.65-3.00 sub-band's 80-90% WR holds live at 20×. **TIGHT REVERT (N=15 + 20× + was-losing sleeve): de-lever `bull_long_lev_mult`→0.05 at live N≥15 NEW bull-longs if the kept 1.65-3.00 band WR≤70% OR avg≤+0.05%; INSTANT de-lever if 3 of the first 6 hit SL OR any single gaps past ~−1.0%. Widen fan back (min→1.35/max→10) only if 1.65-3.00 holds AND adjacent bands turn positive on N≥30.** Existing UI fields (no new D11). Supersedes the [BULL_LONG 1× OBSERVATION] gate below (that N≥15 revert already tripped → this is the operator's "hunt the 2nd variable" resolution, now at 20×).
- **🔭 BULL-LONG OBSERVATION-FOR-REVIEW — 2026-06-22 (NOT a rule; re-confirm with more data before any action).** Read on the 06-22 batch (N=32 bull-longs @1×/0.05-lev obs). TWO things to track forward, both small-N: ① **Both 06-21 BTC-momentum tracking gates FAILED/INVERTED this batch** — gate ① BTC EMA13-50 gap≥+0.06 (claimed −$430 LOSER zone) came back **4/100%WR/+0.745%** (winner, inverted); gate ② BTC 30m-RSI Δ≥+12 (claimed +0.149% WINNER band) came back **11/55%WR/−0.135%** (no edge). The N=52 dig was ~90% single-batch (J20) → first fresh confirmation read says **neither replicates**. Do NOT ship `bull_long_btc_gap_max` on this; keep both gates in the tracker but treat as un-confirmed. ② **The recurring shape is regime > fan, but it's the dimension we flagged as a confound** — live realized (avg% lev-invariant): **S+H BULL net +ve** (S.BULL 7/57%/+0.20, H.BULL 7/71%/+0.11 = combined N=14/64%/+0.155%) · **CHOP is the whole drain** (18/56%/−0.247%, >½ the sleeve's volume) · the **fan>2–3 tail gives bull-regime wins back** (S.BULL fan 2–3 & 3–5 = single SL losers). Tighter live cohorts trending up: **S+H BULL fan 1–3 = 13/69%/+0.262%** · **S+H BULL fan 1–2 = 10/80%/+0.422%** (the cleanest aperture). **⚠️ All sub-cells are N=1–4 = noise; phantom curve disagrees on SIGN (shows 1.65–2.0 negative, live shows it among the best) → fan is exit-mechanism-dependent, NOT a clean dial.** **FUTURE REVIEW (no action now): IF S+H BULL fan ≤2.0 reaches N≥30 cumulative AND holds WR≥70% / avg≥+0.15% across ≥2 batches → THEN consider re-lever (and/or dropping CHOP from `bull_long_regimes`, the one move live already supports). Until then: stay 1×/0.05 obs, keep collecting.** Operator-agreed framing: watchlist, not a hard gate. → DECISION_LOG 2026-06-22.
- **📊 NEXT-BATCH — BULL-LONG VOLATILITY/LIQUIDITY DIMENSIONS (2026-06-23, operator-requested; analytics-only, NO ship).** On the fresh single-config batch, split BULL_LONG **winners vs losers** by ① **pair ATR% (`entry_atr_pct`)** · ② **pair 24h volume (`entry_pair_volume_24h_usd`)** · ③ **BTC ATR% (`entry_btc_atr_pct`)** — **measured ON THE CURRENT-STACK COHORT ONLY** (regime ∈ STRONG_BULL/HEALTHY_BULL AND fan 1.35-2.0), NOT the raw pool. **⚠️ RAW-POOL READ WAS A CONFOUND — DO NOT USE IT.** The first dig (J20-23 pooled, N=101) flagged low-ATR/thin-vol/mid-BTC-ATR as losers, but re-simulating under the live filters (S+H BULL × fan 1.35-2.0 → N=35) **DISSOLVED or INVERTED every one**: thin vol <50M flipped 41%→57%WR/+0.29, BTC ATR 0.10-0.13 flipped 38%→80%WR, pair ATR <0.3 collapsed 18→4 trades. The raw losers were the CHOP-regime + out-of-band-fan trades the current stack already excludes (classic filter-overlap confound). **On the current-stack cohort (N=35, per-bucket N=1-13) there is NO clean separator in any of the three** — faint residuals only: pair-ATR 0.5-0.8 soft spot (43%WR, N=7) and BTC-ATR 0.13-0.16 soft spot (46%WR, N=13), both too thin to act on. MANDATORY method next batch: current-stack cohort, then ×regime (S.BULL vs H.BULL), N per bucket shown. **Decision rule: a dimension earns a gate ONLY if it separates W/L monotonically on the CURRENT-STACK cohort, N≥30, across ≥2 fresh batches.** Pure read-only; existing Order columns, zero schema.
- **👁 WATCHLIST — BULL-LONG pair ATR <0.3% (2026-06-23, operator-flagged; UNRESOLVED, track forward).** Raw-pool flagged sub-0.3% pair-ATR bull-longs as a loser (N=18/39%WR/−0.26) but the current-stack re-sim left only **N=4/50%WR/−0.07 = inconclusive, NOT a confirmed loser** (the rest were CHOP/out-of-band trades already excluded). Mechanism plausible (a "momentum" long on a near-dead-ATR pair = no expansion fuel → grinds/fades, mirror of the FAN-fade finding that low ATR helps a *fade*). **TRACK: on fresh CURRENT-STACK bull-long fires, does pair ATR <0.3% keep losing? SHIP a `bull_long_pair_atr_min≈0.3` floor ONLY IF sub-0.3% confirms ≤40% WR AND net-negative on N≥15 cross-batch (≥2 batches).** Do NOT ship on the raw-pool number — it's confounded. Until then observation-only. → DECISION_LOG 2026-06-23.

### 🗓 2026-06-19 batch (continuation — NOT reset; observation carries into next batch)
- **🔭 FAN FLIP-SHORT — STRONG_BULL UN-BLOCKED 2026-06-19 (operator; NEXT-BATCH REVIEW REQUIRED).** Reverted the Jun-17 B2 STRONG_BULL block: `flip_short_regime_block_any_adxd_regimes` `STRONG_BULL`→**`""`** (off) AND `flip_short_regime_block_regimes` `STRONG_BULL,HEALTHY_BULL,CHOPPY_FLAT`→**`HEALTHY_BULL,CHOPPY_FLAT`**. So FAN flip-shorts now FIRE in STRONG_BULL again (H.BULL + CHOP still block ADXΔ<0). **WHY:** the B2 block was shipped on PHANTOM evidence — there is **zero real live S.BULL FAN-short data** (Jun-17 prechange = 0 S.BULL fires [bear period]; Jun-18 = blocked). Cross-batch PHANTOM shows S.BULL is a **winner**: Jun18 9/78%/+0.46, Jun19 11/82%/+0.24 (two independent windows; H.BULL stays a real live loser 5/40%/−$82 Jun18). **Filter-overlap audit (why it's safe):** the dangerous S.BULL fades (parabolic rips) are already fenced by 3 INDEPENDENT regime-agnostic gates — `flip_short_stretch_block_max=2` (FLIP_SHORT_HISTRETCH), `flip_fan_spike_max=10` (FLIP_FAN_SPIKE), and BTC RSI≥60 & ADX≥30 — so the regime gate was redundant belt-and-suspenders that only killed the milder (winning) S.BULL fades. Fires at **full FAN size/leverage (20×)** — NOT de-risked (unlike the PAIR_RSI_OB S.BULL fade which runs the same mechanism at 1×). **TIGHT REVERT (N=9-11/batch phantom, NO live history, counter-trend @20×): RE-BLOCK STRONG_BULL (restore both fields) at live N≥10 S.BULL FAN-shorts if WR≤60% OR avg≤+0.05% OR any single S.BULL fade gaps the SL past ~−1.0%.** NEXT-BATCH: read the live FAN_RATIO_GATE SHORT × S.BULL cell in the FLIP TRADES × BTC-REGIME table — it should now POPULATE (was "−"). Compare live WR to the phantom 78-82%.
- **🔀 PAIR_RSI_OB FLIP — RE-ENABLED 2026-06-19 @1× size × 0.05 lev (= 1× live obs) / STRONG_BULL-only (operator).** *(Jun 19: switched from 0.5× size to `PAIR_RSI_OB:1.0:0.05` — 1× investment × ~1× actual leverage, matching the bull/bounce sleeves; the flip lev floor was scoped 0.5→0.05 to allow it.)* The overbought-fade short (a LONG blocked at pair RSI>65 → fade SHORT) is back in `flip_entry_sources` ("FAN_RATIO_GATE:1.0,**PAIR_RSI_OB:0.5**"), scoped to fire **ONLY in STRONG_BULL** via its OWN per-source gate `flip_pair_rsi_ob_short_regimes="STRONG_BULL"` (engine: a `source=='PAIR_RSI_OB'` branch at the top of `_flip_filters` that **returns early** → does NOT inherit FAN's flip-short gates, whose `flip_short_regime_block_any_adxd_regimes=STRONG_BULL` would otherwise block the exact regime we want). Counter `FLIP_PAIR_RSI_OB_REGIME`. EVIDENCE = the regime-confound fix: the source nets ~breakeven/negative ALL-regimes but that's H.BULL drowning a strong S.BULL cell — **cross-batch (Jun18 40/80%/+0.32, Jun19 33/76%/+0.20; two INDEPENDENT windows) S.BULL 76-80%WR vs H.BULL 29-47%WR/neg**. Kept it a FLIP (not a new sleeve) because the **flat flip exit IS the exit the phantom +0.20..+0.32 was measured under** → near-zero phantom→live haircut, and the flat mean-reversion exit is the RIGHT exit for an overbought snapback (the normal/momentum exit gives the reversion back). Started **0.5× size** (reduced exposure) to de-risk the one unknown — the counter-trend tail (shorting into a strong bull; the flat SL ATR-widens, can gap past −0.70). Tracking rides the Flip Trade Log × BTC-Regime (free). **TIGHT REVERT: restrict `flip_pair_rsi_ob_short_regimes`→'' (off) at live N≥15 if WR≤65% OR avg≤+0.05% OR the S.BULL losers gap the SL beyond ~−0.85%.** Full D11 (config.py + json + engine gate + UI per-source filter block + load/save + source-registry note).
- **🚫 BOUNCE-LONG DISABLED — 2026-06-23 (`bounce_long_enabled=false`).** Cross-batch N=8 = **2W/6L / −0.443% avg** — a no-edge counter-trend long, and BOTH entry-condition theses tested and FAILED to separate winners from losers: ① **dist-from-EMA13** — win mean −0.60 sits inside the loser cluster (−0.39 to −0.69); only the WU −1.80 outlier supports "too extended = bad"; 2 losers were *closer* to EMA13 than both wins. ② **BTC 30m-RSI Δ (the exhaustion-gate thesis) is INVERTED** — both winners fired at Δ≈−16 (deeply negative, still plunging), while the two *mildest* deltas (−7/−8) were losers → "fire only when decelerating" would have blocked both winners. (BTC 1h Δ doesn't separate either.) Same verdict as PAIR_RSI_OB: no separator, no edge → disabled rather than keep taking −0.44% trades. Phantom seed stays decoupled (always fires) → keeps tracking. **RE-ENABLE only if the phantom shows a cross-batch-STABLE (≥3 batch) separator.** NOTE: tracking-watchlist NOT added — the candidate fields already exist on every Order, and they've been falsified. Config-only. → DECISION_LOG 2026-06-23. ─── prior live history below:
- **🐻↩ BOUNCE-LONG — 2026-06-21 LIVE: first 2 fires BOTH LOST (NEAR/JTO, H.BEAR, straight-to-SL) = FALLING KNIVES** — both fired mid-crash (BTC 30m-RSI Δ −18.5/−25.5, still plunging), DOA (peak ≤0.08%). CONFIRMS the mirror of bull-long: the bounce needs the down-move **EXHAUSTING** (BTC 30m-RSI recovering), not just oversold. **WATCHLIST: add an exhaustion/turn gate (don't fire while BTC 30m-RSI Δ deeply negative); DO NOT widen `bounce_long_regimes` yet — STRONG_BEAR = MORE falling knives.** N=2, track btc_rsi Δ on next bounces. ─── orig ship: SHIPPED 2026-06-19 (operator, LIVE @ 1× observation). New dedicated sleeve (`entry_strategy="BOUNCE_LONG"`, `_maybe_open_bounce_long`), the oversold-WASHOUT dead-cat bounce. Hooks the `BTC_RSI_ADX_CROSS` oversold short-block (`signal==SHORT & btc_rsi≤35`): a short blocked because BTC is washed out → fade into a **real LONG**. **NORMAL long exit** (NOT `_is_flip`); its OWN sleeve so it NEVER routes through `_flip_filters` → the flip-long bear veto does NOT apply (that gate stays correct for the FAN fade; this is the opposite thesis). Self-gates: `bounce_long_enabled` AND tight `bounce_long_btc_cells="25-30:20-25,30-35:15-20"` (BTC RSI×ADX) AND regime ∈ `bounce_long_regimes="HEALTHY_BEAR"` AND per-pair/30min cooldown. Sizes 1×/0.05 (= 1× live obs, same floor-scoping as bull-long); regime-tag bugfix baked in. EVIDENCE (phantom BTC_RSI_ADX_CROSS LONG): N=21, 95% WR, +0.516% avg, **0% SL**, ALL H.BEAR; sub-cells 25-30:20-25 (89%) + 30-35:15-20 (100%). Tables: 🐻↩ Bounce-Long Trade Log + ×BTC-Regime (after Bull-Long log), D11/D12 full-wired. **TIGHT REVERT (phantom + N=21 + counter-trend): disable (`bounce_long_enabled=false`) at live N≥15 if WR≤70% OR avg≤+0.05%; INSTANT review if the wins concentrate in <3 distinct BTC-washout EVENTS (the clustering test the live data finally answers — phantom's 21 are un-dedup-able), or 3 of first 6 SL.** RISK: counter-trend LONG in a bear; the regime gate means it can't bleed outside H.BEAR (just stops firing); the <3-events clustering check is the make-or-break.
- **🔀 FLIP-SHORT pRSI FLOOR — SHIPPED 2026-06-19 (config; operator-directed, DISCIPLINE-OVERRIDE N=21<30) *(staged — pending push)*.** `flip_short_rsi_min=55` (0=off; engine gate in `_flip_filters`, counter `FLIP_SHORT_RSI_MIN`). Block flip-SHORT when entry **pair RSI < 55** — fade quality scales with how overbought the blocked long was. Cross-batch (Jun17/18/19, deduped): RSI<55 = N=21/57%WR/−0.094%/Σ−1.98 (only consistently-negative zone, neg in both recent batches); RSI≥55 = N=78/65%WR/+0.056%/Σ+4.33 (carries ~all the edge); **60-65 = N=24/71%WR/+0.187** (the real sweet spot). **TIGHT REVERT: set →0 if, at N≥15 live flip-shorts post-deploy, the kept (≥55) cohort shows WR≤55% OR avg%≤0.** (Did NOT go to <60 — the flat-but-not-losing 55-60 zone is N=54, too much to cut on this evidence.)
- **🔭 FLIP-SHORT pADX<18 MULTIPLIER — WATCHLIST (NOT shipped; observation next batch).** The one cross-batch-STABLE flip-short edge (WR 70/73/88% Jun17/18/19, pooled N=29/76%WR/+0.080%). **FAILS the locked W→MULT gate: N=29<30 AND avg +0.080<+0.10.** Worse, R:R is **1:2.4** (avg win +0.42 vs avg loss −0.99) and the 5-6 losers are full FLIP_STOP_LOSS straight-to-SL (−0.99 to −1.20%, never armed) → **BE-incompatible, unbounded tail** — a 2× would amplify the worst trades in the bad-regime batch. **GATE TO REVISIT: only at live N≥30 AND avg≥+0.10% AND loss tail bounded (losers start peaking ≥+0.20 before reversing) → then ship 1.5× FIRST (Phase-3 staging), never straight to 2×.** Rank/volume and the 18-25 ADX "soft spot" trims were REJECTED cross-batch (do NOT re-explore — WR holds, only mild avg drag; 18-25 was the PROFIT ENGINE in the Jun17 winning batch).
- **🐂 BULL-LONG 2nd VARIABLE — RESOLVED 2026-06-21: the driver is BTC momentum/extension (30m-RSI Δ≥+12 wins / EMA13-50 gap≥+0.06 loses), NOT pair-ADX/fan. See the obs-arm entry above. ─── (orig watchlist:)** Sleeve net −0.222%/46%WR over N=24, BUT two clean separators this batch: **LEAD = fan ≥ 1.65** (live N=8/88%WR/+0.415%; the two corroborating buckets 1.65-2.0 & 2.0-3.0 are the ONLY ones positive in BOTH live AND the N=106 phantom curve; fan<1.65 = ~25%WR/−0.49). **BACKUP = pair ADX 18-25** (live N=9/67%WR/+0.018 vs outside-18-25 N=15/33%WR/−0.366 — keeping 18-25 flips the sleeve −0.222→+0.018). Range-pos<85 also separates (≥85 = 38%WR vs <85 = 60-100%). **Anti-overfit: per-bucket N is small (5-9); the live 0.85-1.0 = 0%WR is N=5 noise (phantom says +0.27 there). LOCK at live N≥30 in the sleeve before gating `bull_long_fan_max`/adding an ADX window** — then lead with fan≥1.65, ship 1 change at a time.
- **💰 C1 + W2+W1 SHORT lev 1.5→1.0 — SHIPPED 2026-06-19 *(staged — pending push)*.** See Active Pattern Cell Ship rules (de-lever the fat-tail SL, keep inv 2×; caps-for-losers).
- **🟢🔴 DIRECTION FILTER (UI/analytics) — SHIPPED 2026-06-19 *(staged — pending push)*.** New `direction` param on `/api/performance` + dashboard dropdown (All / LONGs / SHORTs), composes with regime + window + flip_source, wired into live + Copy Analytics + Copy Split Report. Rationale: flip-shorts and bull-longs now coexist in the same BTC regime → per-regime tables blended two opposite books; this isolates one side.

- **🔄 PHANTOM FLIP TRACKER — shipped 2026-06-13 (observation-only, operator-requested "fade the block" test).** When an entry is blocked by FAN_RATIO_GATE / **Pair RSI >65** (overbought-long→fade short, added Jun 15: dedup-pool NP=28% — the data's top fade candidate; seeded only when LONG blocked with rsi>momentum_long_rsi_max, NOT oversold-long) / PAIR_TREND_FILTER / LONG_UNMATCHED_ONLY / BTC_RSI_ADX_CROSS (extremes only), a virtual OPPOSITE-direction position is simulated on the live 1s ws feed (base SL −0.70, arm +0.45, trail −0.25, max 45min, de-duped 1 phantom per pair|source per 30min) and persisted to the `phantom_flips` table → **🔄 Phantom Flip Tracker** UI. Tests the operator hypothesis that these block bands are directionally reversible (ENJ fell −1.21% after its long-block). Mirror math (+3-4pp) is a MIRAGE — realized flip sim shows ENJ-flip pays only ~+0.96% (trailed) and HOME-flip hits −0.70 SL (high-ATR loser tail). **READ GATE: at N≥30 per source×flip cell — if ★ flip pays (avg≥+0.10% & WR≥50%), DESIGN a mean-reversion sleeve with its own quick-target exits (do NOT just flip the filter — our short system is cascade-continuation, wrong exit for reversion). If ✗ whipsaws, delete the tracker.** Granularity caveat: 1s ws priced but coarser than tick; realized not max-excursion. Semantics: phantom = blocked by that filter having passed all PRIOR filters (not necessarily all), naked fade not re-checked vs the opposite-direction filter stack — acceptable proxy for these late-stack filters; the 'solely-blocked' refactor was deferred (too invasive for an obs tool). NEVER affects live trading (fail-silent, isolated). **Jun 15: dropped ATR_GAP_LONG source (fade was ✗ whipsaws — N=6, −0.057% avg, 50% SL; chop zone, neither side pays). Short mirror "Pair RSI <25" NOT added — blocked cohort has no NP data + fires rarely.** **Jun 15 DATA-CAPTURE FIX: both the live flip Orders AND the phantom_flips rows now persist the FULL entry-indicator set (RSI/ADX/ATR/fan-ratio gaps 5-8 & 8-13/stretch/range-pos/dist-EMA13/±DI/BTC adx-rsi-slope/regime) — before, flips saved only rsi+adx and phantoms saved nothing, so the pool was un-analyzable. FORWARD-ONLY (existing rows stay NULL). `_flip_entry_fields()` recomputes from the raw indicators at seed/open. Enables cross-batch RSI/ATR/regime analysis once ~4-5 batches accrue.**
- **🔀 FLIP ENTRY SLEEVE — SHIPPED 2026-06-14 (LIVE, operator-directed; promotes the FAN_RATIO_GATE phantom).** `flip_entry_enabled=true`, `flip_entry_sources="FAN_RATIO_GATE:1.0,LONG_UNMATCHED_ONLY:1.0"` (registry now 3-part `SOURCE:size_mult:lev_mult`; lev optional→1.0; a source listed = LIVE BOTH sides; UI has per-source size× + lev× boxes). **🔀 FAN_RATIO_GATE 2× SIZE — SHIPPED then DE-MUXED 2×→1× same day 2026-06-15 (multiplier revert gate fired ✗ HARMFUL).** The 2× ran live N=24: **WR 50%, avg −0.24%, −$912 (Δ$ vs 1× −$456 — the 2× doubled the loss).** De-muxed to 1× per the multiplier gate (operator chose de-mux only, not drop). **⚠️ THE SOURCE ITSELF IS ALSO GATE-BREACHED:** at N≥24 the FAN flip is net-negative *even at 1×* (1× baseline ≈ −$456) → breaches the LOCKED source gate (WR≤55% AND avg≤+0.05% at N≥20). Phantom still shows +0.13%/68% — the live↔phantom gap = **execution slippage** (SL exits hit −0.85% avg not −0.70%, taker-fallback fills + 0.09% roundtrip fees) + a strong-bull regime window (RSI 50-55 shorts 0% WR) + small live N. **Wider ATR-SL REJECTED (operator-requested check, Jun 15): post-exit of the 11 SL trades → median trough −1.87% / mean −3.6% (EVAA −17.6%); a −1.20 floor-capped wide SL saves only 2/11 and deepens 8/11 = manufactures the fat tail. Flip losers are trend-continuation, not noise-wicks — widening bleeds more.** Multiplier effect tracked in the 🔀 Flip Trade Log (Size×/Lev×/Δ$-vs-1×/Mult cols). **NEXT GATE (instant-review): FAN flip stays @1× on probation — if it doesn't recover to net-positive demux by N≥35 OR the execution-slippage drag (live − phantom > 0.25%/trade) persists, DROP the source to phantom-only. Do NOT re-multiply until live demux clears WR≥60% & avg≥+0.10% on N≥30 fresh.** **EXIT/ENTRY TUNING EXHAUSTED (Jun 15, 2-batch N=43 demux): (a) RSI floor REJECTED — RSI≥58 edge REVERSES cross-batch (yesterday RSI 50-55 = 100% WR best; today = 0% WR worst) = regime noise, not signal; do NOT add an RSI gate. (b) Wider ATR-SL REJECTED — saves 2/11 SL trades, deepens 8/11 (losers are trend-continuation, post-exit trough median −1.87%). (c) Tighter TP REJECTED — TP@0.20 = −$266 vs actual −$169 (caps the +0.66% winners that carry the thin edge; higher WR / worse expectancy). The flip is net-negative under EVERY exit variant (best −$142); the problem is live entry edge + slippage, NOT the exit. Only cross-batch-stable differentiator is post-hoc (losers never exceed +0.17% peak = wrong from tick 1). Next real test = a REGIME gate, but needs 4-5 fresh batches (now capturable via the data-capture fix).** **🔀 2nd source LONG_UNMATCHED_ONLY added 2026-06-15 — BELOW-EVIDENCE OPERATOR OVERRIDE @1× (declined 0.5×):** fade matched (C/W) longs → SHORT, same flat exit. Realized phantom only **N≈9, 67% WR, +0.20%** (historical proxy N=271/+0.142pp but proxy overstates). Wired re-entrantly inside `open_position`'s LONG_UNMATCHED block via `_maybe_open_flip(..., isolate=True)` (fresh DB session so the blocked-long txn stays clean). **TIGHTER REVERT GATE (this source): drop `LONG_UNMATCHED_ONLY` from `flip_entry_sources` if at live N≥12 WR≤55% OR avg≤+0.05% OR net-$ negative; INSTANT review if 3 of its first 6 hit FLIP_STOP_LOSS.** Adds a 2nd variable mid-FAN-validation (against "one change at a time" — attribution by `entry_strategy` tag). When FAN_RATIO_GATE blocks an entry, open the OPPOSITE direction as a NAKED entry (block LONG→SHORT, block SHORT→LONG) at base size ×1.0 / 20×, sharing the max-5 book. **Jun-14 PM (EXIT FALLBACK EXECUTED — operator): flips REVERTED to the flat phantom-replica exit `_eval_flip_exit`** (hard SL −0.70 / arm +0.45 / trail 0.25 from peak / 45min) — the exact exit that measured the +0.175% edge. **Why reverted:** the interim "use the normal momentum exit stack" experiment lost money — its ATR-floor-widened trailing gave back 0.25–0.36 on the moderate-peak (~0.5%) reversions, netting **−$243 / 46% WR on N=13** (the wide trail ate the winners; the tight 0.25 trail captures them). Flips now BYPASS the momentum exit stack entirely (monitor loop calls `_eval_flip_exit`, realtime path skips flips). Exit reasons base→`close_position` FLIP_-prefixes them: `FLIP_STOP_LOSS L1` / `FLIP_TRAILING_STOP L1` / `FLIP_NO_EXPANSION`. (The interim-stack code — EMA13/runner is_flip gates in check_exit_conditions — is now dead but harmless; flips never reach it.) Single close funnel `close_position` **FLIP_-prefixes** every exit reason → `FLIP_STOP_LOSS L1` / `FLIP_TRAILING_STOP L1` / `FLIP_RUNNER_TRAIL` etc.; post-exit whitelists + report matchers strip `FLIP_` to recover the base reason. Tagged `entry_strategy="FLIP:FAN_RATIO_GATE"`; shows in ALL momentum tables (operator choice). 🔀 Flip Trade Log scorecard groups by trigger; a flip whose reason does NOT start `FLIP_` = pre-fix bug-exit, excluded from the section verdict. **⭐ ESCALATION (fallback already spent): the flat model IS the exit now. If flips STILL fail the gate under the flat model, the exit is exonerated → drop the source from `flip_entry_sources` (don't keep tweaking the exit).** **Evidence (phantom, in-sample 1 batch): FAN_RATIO→SHORT N=97, 39 pairs, Top-pair 6%, WR 69%, avg +0.175% (GROSS — net-of-fee ~+0.11%), Total +16.93%.** SHORT→LONG side ~zero evidence (phantom N=2, fan blocks only ~3 shorts ever) = idle insurance, tracked separately. Phantom tracker kept running in parallel as live control (compare measured vs realized). All flip code fail-silent + isolated (a flip bug cannot break the momentum bot). **LOCKED REVERT GATE: at live N≥20 (per source×dir, net-of-fee), revert the cell — drop the source from `flip_entry_sources` — if WR≤55% OR avg≤+0.05%. INSTANT kill if Top-pair share ≥60% (one-pair mirage) OR 3 FLIP_STOP_LOSS losses concentrate in 1–2 pairs.** In-sample + gross-of-fees → forward-validate before scaling size or adding sources. **🔀 EXIT SWITCHED TO NORMAL RT STACK — 2026-06-15 PM (operator-directed, REVERSES the Jun-14 flat-model revert).** Flips now exit via `check_realtime_stop_loss` (the momentum realtime stack) with TWO exceptions only: EMA13-cross OFF + short-runner OFF (`_is_flip` gate) → a flip SHORT trails like a LONG. So flips get **ATR-widened SL (base −0.70 → ×`sl_atr_multiplier`1.5 → floor `sl_atr_widen_floor_pct`−1.20)** + normal ATR trailing + **min-profit gate** (`trailing_min_profit_to_fire`0.1, never trail into a loss). Monitor loop now runs flips through the **SAME MAX_HOLD + NO_EXPANSION as normal trades** (`_eval_flip_exit` REMOVED 2026-06-16, operator-directed — was a flip-specific 45min horizon; flips now flow through the shared 180min NO_EXPANSION + BE-peak gate + signal-active reset and the 1200min MAX_HOLD backstop, then skip the momentum exit stack via a flip-skip guard; SL/trail still realtime). Phantom tracker keeps its own 45min `_PFLIP_MAX_MIN` observation horizon (independent). Other RT exits (fast-exit/tick/rsi/signal-lost) are config-OFF so flips get exactly SL+trail. **Rationale:** post-exit-regret showed the flat −0.70 was getting WICKED — 5 of 6 trackable SL trades were peak-FIRST (recovered to +1–5% within 0.7–4.7min, deep trough came 20–40min later); the −0.70 fired at the local extreme then price reversed. The −1.20 ATR floor lets it survive the wick. **Phantom tracker stays on the FLAT model (0.45/0.25/−0.70) = built-in A/B (flat-phantom vs normal-live).** **REVERT GATE (this exit change): if flips don't improve vs the flat-model track record by N≥20 fresh (net-of-fee, per the LOCKED source gate WR≤55%/avg≤+0.05%), OR the ATR-widened −1.20 SL deepens the tail in a TREND-CONTINUATION (bull) regime (last bull batch: wider SL bled to −3.6%), restore the flat `_eval_flip_exit` + the realtime skip (trivial revert).** New reasons may appear: `FLIP_STOP_LOSS_WIDE`, `FLIP_BREAKEVEN_EXIT`. **🔀 FLIP PER-LEVEL ADVANCE — added 2026-06-15 PM (operator request, layered on the exit switch).** Flips were pinned at L1 forever: (a) they skip the monitor's `check_exit_conditions` (where the level advances), and (b) the momentum advance requires `trend_continues` which never fires for a fade (the pair stack is against the short). So the per-level trailing widening was always ×0. FIX: in the realtime path, flips now ratchet `current_tp_level` on PROFIT MILESTONES alone (1 + floor(peak / tp_min); tp_min 0.45 → L2@0.45% / L3@0.90% / L4@1.35% / L5@1.80%, cap 5), decoupled from `trend_continues`, persisted to cache+DB. So the trail widens at L2+ (lets big reversals run) while staying tight at L1. **⚠️ ATTRIBUTION CAVEAT (violates "one change at a time"): this is stacked on the just-shipped ATR-exit switch before that got a clean batch — operator accepted the muddied A/B. If flip results move next batch, can't cleanly attribute ATR-SL vs level-widening. Watch whether the wider L2+ trail gives back on moderate-peak reversions (the known flat-vs-wide failure mode); if flips degrade, revert this FIRST (it's the newer, riskier layer) before touching the ATR exit.** **WATCH on the fresh batch (post-reset): ① does the normal-exit change beat the flat model · ② full phantom accrual (now resets with the data per the Jun-15 reset fix) · ③ pADX-rising sub-rows (🔄 tracker) as a flip entry gate · ④ Entry Quality Score as a possible flip filter — NOTE it's likely INVERTED for a fade (high momentum-quality = strong move = bad to fade) and was non-monotonic this batch (0 ok / 1 worst −$390 / 2 good), so needs cross-batch + likely a flipped threshold before any gate. · ⑤ **🧪 Leftover-Filter Test table (built Jun-15, obs-only, 🔄 phantom section)** — FAN short phantoms split by each SKIPPED downstream filter (short-side: BTC trend/slope, BTC 1h slope, pair trend, breadth, pair ATR, global vol, BTC 1h RSI, quality score) into KEPT vs BLOCKED + N-gated auto-Verdict (★ gate candidate / ✗ inverted / · neutral / ⏳ low N). This directly tests the operator hypothesis "did skipping gates 9–27 cost us the winner/loser separator?" The ★ candidate (if any clears cross-batch) = the regime/trend gate to add to the live flip. Long-only filters (ATR_GAP_LONG, BTC_ACCEL_CHASE_LONG, RSI_SPIKE_GUARD, SPIKE_GUARD) excluded (no short version / not reconstructable).**
- **🔀 FAN flip PAIR-ADX FLOOR — SHIPPED 2026-06-23 @20, RAISED 20→21 same day (`flip_fan_pair_adx_min=21`).** Block FAN flip-SHORT when entry pair ADX < 21 (counter `FLIP_FAN_PAIR_ADX`, engine gate in the FAN-SHORT branch of `_flip_filters`, reads `ind['adx']`). WHY: FAN flips BYPASS the momentum short system's pair-ADX requirement (`Pair ADX Dir: rising` + ADX-Strong>20), so they fire weak-trend fades (pADX 15-19) with no follow-through that chop/gap the 20× SL. The floor restores what momentum already enforces. ORIGINAL @20 EVIDENCE — **3-batch deduped N=89 (J20 pre-reset / J22 20:28 / J23 00:54)**: pADX≥20 = **42/71%WR/+$482** vs pADX<20 = **47/51%WR/−$850** (the entire drain); KEEP>BLOCK + WR-up EVERY batch. Per-pair check PASSED (top-2 pairs 28% < 60% gate → dimension not blacklist). **RAISED 20→21 (commit af29d28) — 4-batch deduped FAN-S re-sim (J20-23, N=92 non-blacklisted), triggered by the BICO −1.22% straight-to-SL gapper (pADX 20.6, peak 0.00):** the pADX **20-21 band = N=6/50%WR/−2.31%** (BICO + HEI never-armed gappers; only 3 tiny offsetting wins) → the 20 floor leaked it. Floor 21 = **36/75%WR/+0.207avg/+7.47**, positive 5/6 dates, vs floor 20 = 42/71%/+0.123/+5.16. **DO NOT go to 22** — the 21-22 band is net-POSITIVE (N=9/+1.99, incl. ORDI +2.80% runner; floor 22 turns J20 −2.8). The full FAN-short loss is 97% never-armed straight-to-SL (entry problem, not exit); after pADX≥21 the residual is pair-concentrated (BICO/HEI/RIF recur) not dimensional (ATR cut REJECTED: non-monotonic + single-batch-J20-dominated + 66% loss in 2 pairs). ⚠️ 20→21 is in-sample, deciding band N=6 marginal (30-50% haircut applied), AND a 2nd finer re-cut on a same-day-shipped dimension (re-overfit risk). Config-only (engine + UI from the @20 ship). **TIGHT REVERT: `flip_fan_pair_adx_min`→20 if 20-21-band FAN shorts come back ≥55% WR on N≥8 fresh; →0 if pADX≥21 FAN flips drop ≤60% WR on N≥15 fresh.** → DECISION_LOG 2026-06-23.
- **🚫 FAN flip-short CHOPPY_FLAT BLOCK — SHIPPED 2026-06-23 (operator discipline-override; `flip_short_regime_block_any_adxd_regimes="CHOPPY_FLAT"`).** Block ALL flip-SHORTS in CHOPPY_FLAT regardless of ADXΔ (was only the ADXΔ<0 slice via `flip_short_regime_block_regimes`; the ADXΔ≥0 slice LEAKED and lost — the 2 ALLO −$192 trades). Universal flip-short gate but FAN is the only live source. Engine: B2 path in `_flip_filters` (reason `FLIP_SHORT_REGIME`, already wired — config-only). **MECHANISM:** a fade needs downward follow-through; CHOP = no trend → never arms → 20× SL whipsaw. **EVIDENCE:** current-stack survivors CHOP = the ONLY negative regime (N=7/43%WR/−$317; raw CHOP N=44/43%/−$1352, ADXΔ≥0 leak N=33/45%/−$1085); H.BEAR/S.BEAR carry the edge (+$881/+$284). **⚠️ DISCIPLINE-OVERRIDE acknowledged:** N=7 survivors ≪ N≥30, AND loss is 83% in 2 pairs (ALLO/PLAY) — our rule says pair-concentration → blacklist, but neither pair clears blacklist individually (ALLO 50% coin-flip w/ +$404 winner; PLAY stale/dormant), so the regime gate is the available lever + the mechanism is sound. **TIGHT REVERT: remove CHOPPY_FLAT from the field if would-be-blocked CHOP FAN-shorts hit ≥50% WR on N≥10 fresh** (watch the phantom + FLIP_SHORT_REGIME counter). Cost: clips the occasional CHOP winner (HU +1.71). → DECISION_LOG 2026-06-23. ─── prior watchlist analysis (now resolved by this ship):
- **🚫 ALLOUSDT BLACKLISTED — SHIPPED 2026-06-24 (operator-directed; resolves the 2026-06-23 ALLO/PLAY watchlist).** The per-pair-concentration lever surfaced by the **BTC-ADX overlap audit** (see refutation below). Cross-batch deduped FAN flip-shorts: ALLO = **9 trades / 6 dates / 33% WR / −4.14%** (the entire ALLO bleed is the flip-short sleeve; its normal longs were net-positive). Under the CURRENT stack (CHOP blocked, pADX≥21) the residue is **0W/3L / −3.45% / 3 dates** — every surviving ALLO flip-short is a full FLIP_STOP_LOSS gap-through (same failure mode as the BICO −$124). Forfeits ~zero current-config upside (its only winners were CHOP, now blocked, or bull-regime longs the book no longer takes). **REVERT GATE: un-blacklist if would-be ALLO flip-shorts run ≥50% WR on N≥5 fresh.** → DECISION_LOG 2026-06-24. *(LAYERUSDT NOT blacklisted — breakeven under current stack: 2W/2L/+0.16%; its losses were all at low BTC ADX / now-disabled BULL_LONG. Watchlist.)*
- **🔭 REFUTED — `flip_fan_btc_adx_min` (FAN flip-short BTC-ADX floor) — NO ship (2026-06-24).** Single-batch (06-24) read flagged BTC ADX ~20-23 as the loser window (vs 25-39 winners). Cross-batch join (deduped, current stack, N=69) **refuted it as a dimension filter**: the BTC-ADX/P&L curve is **non-monotonic** — low <22 negative AND high ≥30 negative, peak in the 22-30 mid-band (a confound, not a single-variable edge; the high-ADX leg is high-WR-but-net-losing = fat-tail, not a block). **Overlap audit is decisive:** the entire "<22 loses" signal is **ALLO+LAYER** — `<22 all pairs = N=7/43%WR/−1.91%` vs `<22 excl ALLO+LAYER = N=4/75%WR/+1.11%`. A <22 block would chop NEAR/APT-type winners to catch losers the ALLO blacklist already removes surgically. The 06-24 "time-window" evidence (late-night losers vs morning winners) was the **same two pairs firing**, not a BTC-ADX effect (LAYER lost at bADX19.7, won at bADX26.5 same day). Lever = pair blacklist (ALLO, shipped above), not BTC ADX. → DECISION_LOG 2026-06-24.
- **🔀 FAN_RATIO_GATE FLIP FILTER SECTION — SHIPPED 2026-06-16 (operator-directed, from the 76-trade batch; in-sample).** Source-namespaced flip-filter layer `_flip_filters(source, ind)` → `(blocked, reason, size_mult, exit_mode)`; each source is an INDEPENDENT branch with its OWN filter TYPES + `flip_<src>_*` config (FAN today; `flip_unmatched_*`/`flip_pairsi_*` are no-op branches awaiting their own data). FAN package: ① **block EMA5 stretch <0.12** (`flip_fan_stretch_min`; thin-fuel, batch N=10/10%WR/−$495) → `FLIP_FAN_STRETCH` · ② **block BTC RSI≥60 & ADX≥30** (`flip_fan_block_btc_rsi`/`_adx`; fade into strong un-exhausted bull, N=19/47%WR/−$416) → `FLIP_FAN_REGIME` · ③ **strpk exit** (`flip_fan_runner_strpk`=true) — FAN flips now exit via the SHORT runner stretch-trail (arm 0.45 / K 0.5, reusing `runner_trail_short_*`), fired in the monitor where ema5 is fresh; realtime tight-trail suppressed once armed (`FLIP_RUNNER_TRAIL`) · ④ **2× mult on BTC RSI 40-45 × ADX≥35 — DROPPED to 1× 2026-06-22** (`flip_fan_mult_rule` `"40-45:35-99:2.0:1.0"`→**`"40-45:35-99:1.0:1.0"`**; was strong-bear mirror of ②, ship-evidence N=10/90%WR/+$308). REASON: first fresh live fire was ✗ — EIGENUSDT −1.206%/**−$209** (2× doubled a −$104 loss); peak **+0.166% FAILS the locked 2× BE-compatibility gate** (≥60% of losses must peak ≥+0.20% so caps bound the tail). Cell kept in observation (`FLIP:FAN_RATIO_GATE×2` row); RE-EARN only at N≥5 fresh fires + Total$>0 + BE-compat. FAN flip itself stays 1× (validated winner +$548 this batch). Wired: `_maybe_open_flip` (veto + size + exit) → `open_position(flip_cell_mult/flip_exit_mode)`; monitor flip-skip block (runner-trail close); realtime suppress. All fail-open + isolated. **In-sample 1 batch: package took the FAN book −$949 → +$1,014 (engine itself −$1,067→ surviving shorts under strpk +$237; +$660 of the gain is the 2× on N=10).** **LOCKED GATES: (filters/strpk) revert the piece if FAN flips don't beat the no-filter track record on N≥20 fresh net-of-fee, OR `actual − shadow_tight < 0` cumulative on N≥8 armed shorts (turn `runner_trail_short`/strpk OFF). (multiplier — BELOW the N≥30 gate, operator override) standard cell verdict: ✗ HARMFUL on N≥5 → set `flip_fan_mult_rule=""` (1×); do NOT trust the +$660 until the cell clears N≥30 AND the FAN source is net-positive demux. ⚠️ filter ② and the multiplier are the SAME regime edge (correlated, not independent) — if regime-alignment is noise next batch, BOTH fail.** Held as watchlist (NOT shipped — confounds/anti-synergistic): fan>X, BTC ADX 30-35, CHOPPY_FLAT, fast-exit.
- **⚠️ DOWNGRADED 2026-06-17 (mechanism-aware re-sim) — ADXΔ<−0.5 is WEAKER on the current stack than raw data showed.** Re-simulating 12-20-07 UNDER the 3 shipped changes (fan≥5 removed + ratchet + N1.0): post-change flip-short book = N=35/77% WR/+$2315. Applying ADXΔ<−0.5 on top: BLOCK N=11/**55% WR**/−$440 · KEEP N=24/88%/+$2754 (Δ +$440). **The BE-ratchet already rescued the ARMED reversers in the <−0.5 cohort (PORTAL −0.13→+0.10 etc.) → 6 of the 11 cut are now WINNERS** (+$248 forfeited to remove −$688). So on the live stack ADXΔ<−0.5 is a 55%-WR net-losing cohort → hits the locked "high-WR-but-net-losing = fix the losers, don't block the cohort" rule. The −$440 residual is entirely **never-armed straight-to-SL** trades (PORTAL/XLM/NEAR/TON/TAO); ADXΔ doesn't separate those at entry (flags as many winners). The raw +$1214/2-batch figure was OVER-CREDITED (pre-ratchet). **Verdict: NOT a clean ship — watchlist.** Real residual = never-armed reversers; likely per-pair (PORTAL & XLM repeat 2× each) not a dimension filter. Still confirm on more batches whether the ADXΔ edge survives the ratchet cross-batch.
- **🔭 SHIP-CANDIDATE (2026-06-17, pre-ratchet read — see DOWNGRADE above) — block flip-SHORTS with ADX-Delta < −0.5 (`flip_adx_delta_min=-0.5`).** Flip shorts BYPASS the momentum-short `Pair ADX Dir S: rising` filter, so they enter on falling/collapsing pair-ADX and get run over. ADXΔ (= entry_adx − entry_adx_prev; pADX "direction" IS its sign, so this is 1D not 2D). **Cut is −0.5, NOT 0** — the −0.5..0 bucket is mildly POSITIVE (+$79/60% WR), so 0 would chop a winner. Combined 2 batches (12-20-07 + 76-ref, N=110): **ADXΔ≥−0.5 KEEP = +$1294/62% WR · ADXΔ<−0.5 BLOCK = −$1214/45% WR.** The ENTIRE flip-short edge lives in ADXΔ≥−0.5; the negative-delta subset drags the sleeve to ~breakeven (+$80). **CLEARS ALL LOCKED GATES:** per-batch consistent (−$644/41% · −$570/46%, net-neg BOTH), per-pair diffuse (22 losers, top-2 pairs only 23% of loss → genuine dimension not pair-blacklist), mechanism (flips skip rising-ADX req). **Caveats before ship:** (a) ~36% of flip-short volume cut — big throughput change; (b) current-batch half is in-sample → haircut; (c) slightly non-monotonic (−1.5..−1.0 less bad than −1.0..−0.5) but whole neg region net-neg; (d) would be the 4th change this session — commit the ratchet/N1.0/fan-block first for clean attribution; (e) LARGELY SUPERSEDES the fan≥5 block + makes the binary-pADX idea obsolete (this is the refined version). Impl = `_flip_filters` block, counter `FLIP_ADX_FALLING`. Also: ASTER is a repeat flip-short loser (2-3×) → separate per-pair-blacklist candidate.
- **📈 BULL_LONG SLEEVE — SHIPPED 2026-06-18; 2026-06-19 → 1× OBSERVATION MODE (operator).** Now LIVE @ 1× size × **~1× actual leverage** (`bull_long_lev_mult` 1.0→**0.05** = 20×→1×, cell-lev floor scoped to 0.05 for this branch; committed **0874e49**). **The N≥15 revert gate TRIPPED (live N=24, 46% WR, −0.222% avg) — operator override: de-risk to 1× and keep collecting to hunt the 2nd variable rather than disable** (see Bull-Long 2nd-variable watch in the 2026-06-19 block above). First build-side LIVE long. `entry_strategy="BULL_LONG"` = a REAL momentum long (NOT a fade, NOT _is_flip → NORMAL long exit). Fires at the fan-gate-BLOCK site (`_maybe_open_bull_long`, fail-silent — **CORRECTED Jun 18: was wrongly on the fan-PASSED branch**) when `bull_long_enabled` AND fan_ratio < `bull_long_fan_max`(**5.0** = upper bound of the traded BLOCKED-long zone; un-blocks longs fan 0.85-5) AND regime ∈ `bull_long_regimes`("HEALTHY_BULL") AND per-pair/30min cooldown; **PRE-EMPTS the flip-short fade** (same blocked-long event triggered both — opposite positions); BYPASSES the downstream long filters; hard risk controls still enforced. The validated population = `PASS:FAN_RATIO_GATE LONG` = the BLOCKED long tracked as a long (the "un-block hunt", fan 0.85+), NOT fan-passed longs. `bull_long_size_mult`/`bull_long_lev_mult` default **1.0** (no amplification). EVIDENCE: PASS:FAN_RATIO_GATE LONG cross-pool H.BULL 79%→94% WR (combined ~52/84%/+0.39), stable S.BULL exclusion (20-22% WR both pools), + 2 live H.BULL longs won. Tables: 📈 Bull-Long Trade Log (live, after Flip Log) + 📈 Bull-Long Curve (phantom PASS+BLOCK virtual longs by fan×regime — the EXPANSION dial: curve axes = config knobs `bull_long_fan_max`/`bull_long_regimes`, widen by config not code). **LOCKED REVERT (phantom + single-window → TIGHT): disable (`bull_long_enabled`=false) at live N≥15 if WR≤55% OR avg≤+0.05% net; INSTANT review if 3 of first 6 SL OR live−phantom gap >0.25%/trade; do NOT raise mult off 1.0 until live WR≥70% & avg≥+0.10% on N≥30.** RISK: bypassing downstream long filters = the real exposure; N≥15 gate is the backstop, regime gate means it can't bleed outside H.BULL (just stops firing).
- **🌀 FAN 3-10 FLIP BAND OPENED — 2026-06-18 (operator).** `flip_fan_spike_max=10.0` + `fan_ratio_block_long="0.85-99.0"` → flips now trade fan **0.85-10.0** (added the 3.0-5.0 gap that NEVER seeded before + the 5-10 band that seeded-then-vetoed); fan **10+ stays blocked** (parabolic gappers). Thesis: the SL-gap kicks in at fan ~13+ (ALLO 13.2/VELVET 28.3 → −1.20 floor) NOT at 5 (ASTER 5.7 → clean −0.69); 3-10 is the clean-exhaustion band. Evidence = the fan 3-5 A/B CONTROL phantom (+0.281%/73% WR/N=19). A flip is born ONLY from a blocked long (`_flip_filters` can't create a seed), so opening the band REQUIRES the `fan_ratio_block_long` extension (negligible normal-long impact, bot is ~flip-only). **LOCKED REVERT (phantom/control + in-sample → TIGHT): revert BOTH (`flip_fan_spike_max`→5, `fan_ratio_block_long`→"0.85-3.00,5.0-99") if fan 3-10 fresh flips show WR≤50% OR avg≤+0.05% on N≥10, OR ≥2 of first 5 gap past −1.0%.** Read = Fan-Ratio Curve live rows + flip log by fan bucket. (Supersedes the prior `flip_fan_spike_max=5.0` ship below.)
- **🔭 MAX-POSITIONS 5→8 — DEFERRED to the cap-skip counter (2026-06-18).** Do NOT raise on the "blocked too many" feeling — that's the WRONG metric (the 1,417 = filter-blocks-during-scans). `equal_split` sizes each trade = balance/max_pos, so 5→8 SHRINKS every bet 37.5% and adds ZERO fills unless the cap actually BINDS (historically ~0.3 concurrent — it didn't). "Trade more" is a SUPPLY problem (fan 3-10 add), not a cap problem. **GATE: one fresh batch, read `_cap_skip_counts` (norm/flip) — raise only if flip cap-skips HIGH and the sleeve net-positive, then 5→6/7 ONE step.** Watch: fan-spike fades cluster (market-wide pumps) so the new supply could start binding — measure, don't assume.
- **🚫 FLIP_FAN_SPIKE — SHIPPED 2026-06-17 (DISCIPLINE-OVERRIDE, N=3); spike threshold RAISED 5→10 on 2026-06-18 (see FAN 3-10 entry above).** `flip_fan_spike_max=10.0` — block ANY flip (all sources, universal at the top of `_flip_filters`) when the pair fan ratio (|EMA5-8 gap|/|EMA8-13 gap|) ≥10 = a violently-accelerating parabolic spike the fade gets run over by (never arms → straight to SL). Counter `FLIP_FAN_SPIKE` (auto via the flip-block path). Evidence: fan≥5 = **N=3, 0% WR, ~−1.0% avg, cross-batch (2 batches, 3 distinct pairs)** — ASTER 5.7 / VELVET 28.3 (12-20-07) + ALLO 13.2 (06-16 76-trade ref). CLEARS the locked ≥3-sample-direction-consistent bar; passes per-pair concentration; fires ~3% (low throughput); zero winners in the zone to chop; mirrors the ALREADY-LIVE `fan_ratio_block_long` 5.0-99. **Threshold is specifically ≥5: the 2-5 band CONTRADICTS cross-batch (12-20-07 +0.48/75% WR vs ref −0.44/33% WR = confound) — do NOT lower toward 2.** D11 wired (config + JSON + engine + UI input + load/save). **TIGHTER-THAN-STANDARD REVERT GATE (N=3 override): re-open (`flip_fan_spike_max`→0 or raise) if fan≥5 flips show ≥40% WR on N≥5 fresh.** (The other never-armed flip-short losers at fan 0.6-2.9 = live-edge question under the FAN_RATIO_GATE / PAIR_RSI_OB source revert gates, N≥20.)
- **🏃 BE-RATCHET (lock) DISABLED — 2026-06-18 (operator-directed). CURRENT runner-exit stack: ATR-floor(N=0.5) ONLY — NO BE-ratchet, NO give-back cap.** `runner_trail_short_be_ratchet_enabled=false`. The +0.10 lock sat ON TOP of the live strpk/ATR-floor trail and over-tightened it. On the 15-42-03 batch's 6 armed lock-exits the lock banked +0.10 each while the no-lock strpk trail (`shadow_strpk`) would have banked **+0.37–0.83** → **0 saved / 6 cut runners, net −2.35pp** once the loser downside is capped at the −1.20 SL (operator's framing: the SL catches the tail, so the lock's only real credit is +0.10 vs −1.20 = +1.30/trade — which the trail BEATS by exiting reversers at ~+0.4 BEFORE any crash). The earlier "small-saves" only beat a NO-trail baseline; with strpk live they're redundant. Full-stack re-sim of 15-42-03: entry filters −$1521→−$33, then +BE-off −$33→**+$85** (the 6 lock-exits re-price +$118). **REVERT GATE (replaces the prior '+0.10 round-trip tracking' gate): re-enable if over N≥8 fresh armed SHORT reversers the no-lock book bleeds past −0.70 vs the `shadow_atr05` no-ratchet control — i.e. armed runners that round-trip now lose MORE than the lock would have saved.** D11 already wired (panel toggle + lock input + load/save); field flipped to false. ⚠️ N=6/one-batch + `shadow_strpk` model (not realized fills) → TIGHT gate.
- **🔭 BTC-30m-RSI-rising threshold — INVESTIGATED, HELD at `>0` (2026-06-18).** Operator asked whether tightening `flip_short_btc30_rise_block_min` from 0 to a higher threshold could spare the yesterday fat-tail winner BR (+$544/+1.81%) while still blocking today's losers. **NO — BR's BTC-30m-RSI Δ is +10.60 (one of the STEEPEST risers), un-sparable below T>+10.6 (which keeps the whole rising cohort).** Threshold sweep: 2-batch net peaks at T≈0.5–1.5 (+$767) and DECLINES monotonically as you tighten (today's losers leak back faster than yesterday's winners recover). Verdict: `>0` is within noise of optimal; BR is a priced-in fat-tail sacrifice for blocking a cohort that's net −$1k+/batch. No change.
- **📊 ENTRY-FUNNEL CAP-COST COUNTER — SHIPPED 2026-06-18 (observability).** `_cap_skip_counts {normal, flip}` (trading_engine.py): increments ONLY at the `open_position` max-pos gate when a fully-qualified signal can't open because the book is full — the REAL cap-cost, distinct from the existing `blocked_at_max` (which counts filter-rejections DURING full-book scans, not trades the cap prevented). Surfaced as a subline on the Blocked-at-max box + BOTH text exports (D12). Read RELATIVE (normal-vs-flip ratio), not as distinct trades (still per-scan while full). **USE: confirms whether the 5-cap binds on the GOOD cohort — expect flip≫normal and good-cohort skips LOW (the disaster-batch cap pressure was the now-entry-filtered bad cohort; filtered good cohort runs ~0.3 concurrent vs the 5-cap → raising positions would capture nothing → do NOT raise max-positions on cap-pressure alone).**
- **🏃 GIVE-BACK CAP DISABLED + N HELD 0.5 — SHIPPED 2026-06-17 PM (operator).** `runner_trail_short_giveback_frac` **0.35→0.0** (the `_sp_frac>0` guard clean-disables → floor = `max(peak − N×ATR, lock)`; lock KEPT enabled, see entry above). WHY: the cap protected bull/chop **bounce-fade** shorts (peak-then-reverse) — but those are now **blocked by FLIP_SHORT_REGIME**, so the cap was guarding a population that no longer trades while **CLIPPING the bear-regime trend runners** the filter leaves us (SKYAI STRONG_BEAR: capped +0.30 close vs **+3.92% post-exit continuation, −0.02 retrace** = continuous trend, ATR-floor would've ridden to ~+3.3). **The BE-ratchet (lock) IS the round-trip backstop** — re-ran VELVET (pk+0.76/ATR1.59) without the cap: floor=max(0.76−0.80, 0.10)=**+0.10**, still flat not −1.20 SL → the cap was redundant tightening. **N HELD at 0.5 (operator asked to bump→1.0; I recommended HOLD):** zero distinguishing evidence (lone runner trended → atr05/10/15 identical), and it'd be a 3rd same-direction loosening (one-change-at-a-time). **ATR shadows NOW LOCK-AWARE** (`max(peak−N×ATR, lock)`) → `atr05`=EXACT live config, `atr10`/`atr15`=the N=1.0/1.5 candidates under the real lock = the clean decision surface. **GATE (decide N with data): bump `runner_trail_short_atr_mult`→1.0 ONLY if `atr10` beats `atr05` cumulatively on N≥8 fresh armed shorts.** **REVERT GATE (cap): re-enable give-back cap (frac→0.35) if an armed bear-regime runner round-trips peak≥+0.45 → ≤0 on N≥3 fresh** (i.e. the lock proves insufficient). D11 wired.
- **🔭 WATCHLIST — DEEP-ANALYZE next batch: did the give-back-cap fix work?** The 2026-06-17-PM give-back cap (frac0.35) + N-revert(0.5) is the 3rd exit iteration this day — it MUST be validated cold on the fresh batch, not assumed. Read from the orders CSV: ① split `FLIP_RUNNER_TRAIL` by `runner_trail_bound` — `cap` rows should now show close ≈ 0.65×peak (NOT ≈ +0.10 like the broken lock rows); `atr` rows = low-ATR winners preserved; `lock` rows = genuine round-trip saves. ② Per armed trade, compare close vs peak — the high-ATR pairs (ATR>1.5) should no longer scratch at BE. ③ Net: did capture rise vs the 14-xx scratch batches? ④ Watch for the OPPOSITE failure (cap too tight → noise-stop: `cap` exit then price keeps going the short's way = left money). ⑤ Confirm N=0.5 didn't re-introduce the strpk early-exit (small freshly-armed peaks tripping). If cap-rows still ≈ BE or winners noise-stop, the fix failed → revisit (peak-% trail without ATR, or frac tune). Whole exit stack is now: ATR-floor(N0.5) ∧ give-back-cap(0.35) ∧ BE-ratchet(lock0.10) — all three interacting; this batch is the clean read on the combination.
- **🏃 BE-RATCHET + N=0.5→1.0 — SHIPPED 2026-06-17 (operator).** *[N PORTION SUPERSEDED 2026-06-17 PM — N reverted/held at 0.5 and the give-back cap was first added then DISABLED; see the give-back-DISABLED entry above for current state. BE-ratchet (lock) is unchanged and still LIVE.]* Fixes "peaked +0.5% then closed negative." ROOT CAUSE (mechanism, not noise): on high-ATR/modest-peak shorts the give-back `N×ATR` EXCEEDS the peak, so the chandelier floor `peak − N×ATR` sits BELOW breakeven → permits a full round-trip into a loss or to the −0.70 SL. Discriminator is **peak/ATR, NOT ATR level** — the 2 highest-ATR trades (ESPORTS 2.63, BR 2.01) were fine because they peaked >1.7; the 5 broken ones (EVAA ATR1.89/pk0.59→−0.36, VELVET ATR1.59/pk0.76→−1.20, PORTAL×2, AERO) all peaked +0.49–0.76. **FIX = BE-ratchet:** once armed, exit floor = `max(peak − N×ATR, be_lock_pct)`, `be_lock=0.10` (≈net-flat after 0.09% fee). Only binds when `peak − N×ATR < lock` (the broken set) → by construction CANNOT touch a runner (PORTAL floor 1.96 unchanged). Config `runner_trail_short_be_ratchet_enabled`=true + `runner_trail_short_be_lock_pct`=0.10; live in the realtime flip-strpk block (fire when `pnl_pct <= eff_floor`, reason tags lock vs atr). **PAIRED: `runner_trail_short_atr_mult` 0.5→1.0** — the ratchet caps the reverser tail, so the looser N is now safe and gives genuine runners room (sim 23 armed: N=1.0+ratchet +20.05% vs N=0.5 +14.02%; ⚠️ +4.5 of the +5 edge is ONE trade, PORTAL 2.70→7.20, + post-exit-continuation inflation — runner-dependent, NOT +5%/day expectation; N=1.0 also costs ~0.1–0.15%/trade on the low-ATR book). Both changes act on DISJOINT sets (ratchet→reversers, N→runners) so attribution stays clean. **`shadow_atr05` (N=0.5, no-ratchet) is the control.** **REVERT GATE (locked): if over the next 3 batches live (N=1.0+ratchet, the Leash `actual` row) does NOT beat the `atr05` shadow cumulatively → revert `atr_mult`→0.5. Separately, if the ratchet ever clamps a trade that would've re-run ≥+0.5% twice → raise be_lock toward 0 / review.** D11 wired (panel toggle + lock input + load/save). Sim: the 5 broken trades go from −0.02/−1.20 to ~+0.10.
- **🏃 ATR-FLOOR TRAIL (CHANDELIER) — SHIPPED 2026-06-16 PM (operator, DISCIPLINE-OVERRIDE).** ROOT CAUSE of strpk early-exits: the `K×peak_stretch` ratio trail collapses to ~0 width on a freshly-armed (tiny) peak → a first bounce trips it before the move develops (AERO closed +0.23% then ran −6.8%; HYPE +0.30%→+2.78%; STG +0.50%→+2.5% — all post-close, shadow-independent via `post_exit_peak_pnl`). FIX: **ATR-floored give-back** — exit only when P&L retraces **> N×entry_atr_pct** from peak (a normal bounce <1 ATR can't trip it; only a real reversal does). Config `runner_trail_short_use_atr`=true (fallback to ratio when false) + `runner_trail_short_atr_mult`=**N=1.0** (robust default — would have held all 3 early-exiters; the bounces were 0.04–0.30 ATR). Live in the realtime flip-strpk block → **applies to ALL flip shorts running strpk** (FAN + PAIR_RSI_OB + LONG_UNMATCHED). Hard SL (−0.70%) still backstops reversers. UI in the 🏃 Runner Stretch-Trail (SHORT) panel (D11: toggle + N, load/save). **SHADOW tuning: `shadow_atr05/10/15_pnl` columns** test N=0.5/1.0/1.5 observation-only — next batch compare them + actual + post_exit_peak to confirm N (0.5 captures more on clean runs but risks mid-trend whipsaw; 1.0 robust; data picks). **REVERT GATE: if actual − shadow_tight < 0 cumulative on N≥8 fresh armed shorts → strpk/ATR-floor net-harmful, turn OFF (same gate as the strpk promotion).** ⚠️ DISCIPLINE-OVERRIDE: shipped live on N=3 prior FAN flips (pre-Fix-A data) — validate on the fresh post-deploy batch; if early exits persist under the ATR-floor, the residual is regime (FAN flips in CHOPPY) not the trail.
- **🏃 STRPK MOVED TO REALTIME TICK RESOLUTION (Fix A) — SHIPPED 2026-06-16 PM (operator).** The flip SHORT runner stretch-trail exit was moved OUT of the 1s monitor (`update_open_positions`) INTO the realtime WS-tick path (`check_realtime_stop_loss`, ~line 8870, right before the tight-trail suppress). Now peak-stretch is tracked in `order_info['runner_peak_stretch']` and the trail (`stretch ≤ K×peak`) is checked **every tick** — matching the leash-shadow — instead of under-tracking the peak and trailing out on a 1-second bounce. Diagnosed from HYPE (live strpk +0.30% vs shadow strpk +2.43% at the same K=0.5 → the gap was sampling resolution, not K; ema5 staleness hits both paths equally so it's not the cause). Same arm 0.45 / K 0.5, same `cached_ema5` the shadow uses; hard SL keeps priority; `_closing_in_progress` guards double-close; peak stamped to `order.runner_peak_stretch` at close for the CSV. Monitor strpk block removed (flips just commit+skip the momentum stack). Fail-open. K=0.3 vs 0.5: on the last batch K=0.5 won (+$286 vs +$276; HYPE 2.43 vs 2.32) — keep 0.5. **⚠️ CAVEAT (the user's catch): the HYPE gap (live runner_peak_stretch 0.172 vs shadow_peak_stretch 0.569) is TOO BIG for 1s-vs-tick sampling — a 3.3× peak gap can't come from sampling resolution. shadow_peak_stretch keeps growing POST-EXIT (HYPE's +2.78% run happened after the live closed), so most of that gap is post-exit continuation, NOT under-sampling. So Fix A likely solved a non-problem; the real issue is the EARLY first-bounce exit → Fix B.**
- **🔬 NEXT-BATCH REVIEW CHECKLIST (post-reset 2026-06-16 ~20:30 — the fresh batch is the first clean one under Fix A + ATR-floor; read from the orders CSV).** ① **ATR-floor: faithfulness THEN N (primary, two steps). LIVE N=0.5 since 2026-06-16 ~20:45 (operator override of the 1.0 conservative start — old-data approx leaned 0.5: +12.6% vs 1.0 +10.1% vs 1.5 +7.7% on 7 armed shorts, but that approx is whipsaw-blind).** Step 1 — FAITHFULNESS (do first): per `FLIP_RUNNER_TRAIL` trade, is `pnl_percentage` (actual, live N=0.5) **≈ `shadow_atr05_pnl`** (shadow at the SAME N=0.5)? ✓ ≈ equal → live implementation is faithful → shadow N-rankings translate to live → Step 2. ✗ live ≪ shadow_atr05 → implementation gap (the strpk sampling disease again) → FIX before tuning. Step 2 — WAS 0.5 RIGHT, or should we LOOSEN? Compare live N=0.5 (actual) vs `shadow_atr10` (N=1.0) vs `shadow_atr15` (N=1.5) vs `post_exit_peak_pnl` (ceiling). **The key risk to look for: did N=0.5 WHIPSAW** — i.e., does `shadow_atr10`/`atr15` capture MORE than actual on trades that ran (meaning 0.5 ejected early on a mid-trend pullback)? If `atr10` net-beats `atr05` → loosen `runner_trail_short_atr_mult` to 1.0. If 0.5 holds up (captures ≥ the looser N) → keep 0.5. (N=1.5 already looks too loose — STG gave back its whole run.) ② **Did the ATR-floor actually stop the early exits?** FAN runner-trails should now show `post_exit_peak − close` SMALL (was +2–6.6% on AERO/HYPE/STG). If still big → residual is regime (FAN flips in CHOPPY), not the trail. ③ **Sampling diagnostic (Fix A retro-confirm, low priority):** `runner_peak_stretch` vs `shadow_peak_stretch_at_close` per trade — ≈ equal confirms it was post-exit not sampling (expected). ④ **strpk/ATR-floor REVERT GATE:** `actual − shadow_tight` cumulative on N≥8 fresh armed shorts; <0 → turn the whole runner-trail OFF. ⑤ **Flip-source gates (N≥20 net-of-fee, per source):** FAN_RATIO_GATE (probation, net-neg at 1× last data), PAIR_RSI_OB (gate WR≤60%/avg≤+0.10%), LONG_UNMATCHED_ONLY (gate WR≤55%/avg≤+0.05%, weakest). ⑥ **LET IT RUN — do NOT reset early.** We've been wiping every ~10 trades; the flip gates are stuck at BUILDING 9/20 and never decide. This batch needs to reach **N≥20 per source** before any verdict.
- **🏃 STRPK EXTENDED TO ALL FLIP SHORTS — SHIPPED 2026-06-16 PM (operator).** New flag `flip_runner_strpk_shorts`=true governs the SHORT runner stretch-trail for the NON-FAN flip short sleeves (`PAIR_RSI_OB`, `LONG_UNMATCHED_ONLY`); FAN keeps using `flip_fan_runner_strpk`. Effective gate (monitor exit + realtime tight-trail suppress + UI 🏃 RUNNER badge): a flip short runs strpk if **FAN & flip_fan_runner_strpk** OR **non-FAN & flip_runner_strpk_shorts**. So with both on = **strpk for ALL flip shorts** → all exit `FLIP_RUNNER_TRAIL` (was: only FAN; PAIR_RSI_OB/UNMATCHED were on the standard 0.25% `FLIP_TRAILING_STOP`). `_flip_filters` non-FAN branch now returns exit_mode="strpk". UI toggle in the flip sleeve panel (D11: define+load+save). Reuses `runner_trail_short_*` (arm 0.45/K0.5). Reversible per-sleeve: turn `flip_runner_strpk_shorts` off → non-FAN flips revert to `FLIP_TRAILING_STOP`. NOTE: changes the exit for PAIR_RSI_OB/UNMATCHED mid-evaluation — their N≥20 revert-gate reads now reflect strpk, not the standard trail.
- **🔀 PAIR_RSI_OB RE-ACTIVATED + FLIP-SOURCE DASHBOARD FILTER — SHIPPED 2026-06-16 PM (operator). LONG_UNMATCHED_ONLY DISABLED 2026-06-17 PM (operator) — `flip_entry_sources="FAN_RATIO_GATE:1.0,PAIR_RSI_OB:1.0"`.** LONG_UNMATCHED_ONLY live-off (live N=0 both batches + phantom N≈8/38%WR/−0.190% ✗ whipsaws = starved & losing, not enough good data); **its phantom seed is DECOUPLED from the registry (always fires) so the tracker keeps accruing for a future re-enable.** RE-ENABLE only if its phantom clears WR≥55% AND net-positive on N≥20. The two live sleeves run @1× base with **no source filters yet** (`_flip_filters` no-op branches = clean baseline for per-source data collection). **NEW "Filter by Flip Source" dropdown** (3rd dashboard filter, beside Macro Trend + Time Window): slices the closed-orders pool by `entry_strategy` (`flip_source` param on `/api/performance` → one `orders` filter at [main.py](main.py) post-regime/window) so **every one of the ~50 tables re-computes for ONE sleeve in isolation** (values: a source name → `startswith("FLIP:<src>")` incl. mult variants · `FLIP` all flips · `MOMENTUM` non-flip · ""/`ALL` off). Wired into loadPerformance + Copy Analytics + Copy Split Report. Workflow: filter to a source → read its tables → encode findings in that source's `_flip_filters` branch. The Flip Trade Log scorecard (always all-sources) stays the cross-source compare. **flip_fan_mult_rule now 4-part `rsi:adx:size:lev`** (`40-45:35-99:2.0:1.0`) — UI mult cell is the 6-field RSI/ADX min-max + Invest/Lev layout like other mult cells; lev flows via `flip_cell_lev_mult`. **REVERT GATES per reactivated source (locked):** drop a source if at **N≥20** fresh net-of-fee it shows WR≤55% OR avg≤+0.05%. Priors: PAIR_RSI_OB phantom N=11/82%WR/+0.405% (promising, gate N≥30); **LONG_UNMATCHED_ONLY phantom N≈8/38%WR/−0.190% ✗ whipsaws — weakest, keep on a tight leash, expect to revert unless real data beats the phantom.** (Lens ⑥ PAIR_RSI_OB live-review RESUMES; lenses still apply to FAN shorts.)
- **🔄 PHANTOM FLIP TRACKER PIVOTED TO LONGs — SHIPPED 2026-06-16 PM (operator).** The tracker now hunts the LONG flip sleeve (fade a BLOCKED SHORT → LONG). **Engine seeds added** at 3 short-block sites (obs-only `_seed_phantom_flip(..., "SHORT", ...)`, forward-accruing from now): `PAIR_ADX_MAX` (exhausted down-move bounce; biggest short-blocker) + `PAIR_RSI_ADX_CROSS` (pair cross mirror) via the `_signal_block_recorder` hook; `BTC_ADX_BLOCK_SHORT` (macro-aligned) at its engine block site. `FAN_RATIO_GATE ⊘SHORT → LONG` already seeded (kept). **Reporting (`_compute_phantom_flip_performance`):** rewrote the source loop to a `source_specs` table — **DROPPED `FAN_RATIO_GATE` SHORT** (real data authoritative now), **SLIMMED `Pair RSI>65` + `LONG_UNMATCHED_ONLY` to top-line** (live sources → DELETE these phantom rows at real N≥30 each), **KEPT `FAN_CONTROL` full** (never-traded control, no real twin; also the shared long-side control). New long sources carry aligned/counter sub-rows. **`BTC_RSI_ADX_CROSS` now per-cell split** (BTC RSI×ADX 5-pt grid, cells N≥3) so a winning sub-cell isn't masked by the blended average (mirrors the fan-ratio curve). All 3 render surfaces iterate rows generically → no render edit needed. Note: `BTC_RSI_ADX_CROSS SHORT` (blended) was N=25/60%/+0.041% = FAILS the flip gate (avg<+0.05%) — the per-cell split is the way to find a shippable sub-cell. The new LONG trackers start at N=0 and need a fresh batch before any read.
  - **🔭 WATCHLIST — STANDING (2026-06-17) → PARTIALLY ADDRESSED by the FLIP-SHORT 2D filter shipped same day (see next bullet); the short-side proactive gate now exists, the long-side bull-mechanism (passthrough tracker) is still accruing. — FLIP sleeve needs a UNIVERSAL regime gate (the bull-failure protection gap).** FLIP/FAN expectancy is REGIME-CONDITIONAL — it's a "short failed counter-trend rallies" edge: pays in bear/down-drift, LOSES in bull (continuation) and flat chop (no downside follow-through). In-sample proof (combined 2 batches): STRONG_BULL −$387 / CHOPPY_FLAT −$343 / vs STRONG_BEAR +$524 / BEAR_EXHAUSTED +$266. **Protection audit:** ✓ per-trade (hard SL −0.70→−1.20 + BE-ratchet = bounded loss) · ✓ source-level (revert gates drop a source at WR≤55%/avg≤+0.05% N≥20 — but REACTIVE, catches bleed after ~20 trades) · ✗ **PROACTIVE regime/entry gate = THE GAP.** Only FAN has one (`FLIP_FAN_REGIME`: block when BTC RSI≥60 AND ADX≥30); **PAIR_RSI_OB + LONG_UNMATCHED flips have NO regime gate**, and flips bypass the macro short-filter stack entirely. Hole confirmed: STRONG_BULL lost −$387 WITH the FAN gate live (non-FAN sources / moderate bulls below RSI60-ADX30 slipped through). **BUILD (when calibratable): generalize FLIP_FAN_REGIME into a universal flip regime-gate across ALL sources** — key on BTC regime label (block/down-size STRONG_BULL, watch HEALTHY_BULL — the data is already sliced by it) and/or BTC trend slope / breadth; likely more sensitive than RSI≥60. **NOT shippable now: bear/range-heavy sample → can't set the bull threshold (would be guessing on a bear sample).** REVIEW EACH BATCH: watch the "Performance by BTC Regime" flip rows; when a BULL window accumulates flip losses at N≥10-15, that's the calibration set → build + tune the gate then. Interim if a strong bull hits first: source revert gates catch it (lagging) + manual kill of flip sources. This is the key all-weather-robustness item for the flip sleeve.
- **🔀 FLIP-SHORT 2D REGIME×ADXΔ FILTER — SHIPPED 2026-06-17 (fulfills the standing universal-regime-gate item above; discipline-override).** `flip_short_regime_block_adxd_max=0.0` + `flip_short_regime_block_regimes="STRONG_BULL,HEALTHY_BULL,CHOPPY_FLAT"`. Block any flip-SHORT (ALL sources) when entry ADXΔ<0 AND BTC regime ∈ bull/chop. Universal block at the top of `_flip_filters` (reason `FLIP_SHORT_REGIME`, rides `_record_filter_block`); UI in the FLIP panel (ADXΔ + regime CSV, empty regimes=OFF). Cross-batch (76+39+11 deduped pool) the BULL/CHOP∧ADXΔ<0 cell = N=38/40%WR/−0.34%/−$1070; 96% of its losers peak <0.45 arm → give-back cap can't save them → entry block not exit. Counterfactual: bear batch −$63 [dormant], bull batch −$611→−$5. Discipline-override (literal NP gate 13%<60%, overridden by arm-saveability). **TIGHT REVERT: re-open the blocked cells if they flip to WR>45% on N≥15 fresh.** **⚠️ WATCHLIST — PER-SOURCE VALIDITY (operator-flagged 2026-06-17): the −$1070 block cell is 97% FAN_RATIO_GATE (N=37/38%WR/−$1091); PAIR_RSI_OB has only N=1 in-cell and it WON (+$21), and is net-positive pool-wide (N=15/80%WR/+$79); LONG_UNMATCHED_ONLY has ZERO data.** Universal application is mechanism-justified (regime hostility to shorts is source-agnostic) but data-unproven for the non-FAN sources — PAIR_RSI_OB's only in-cell sample CONTRADICTS the block. **GATE: as PAIR_RSI_OB + LONG_UNMATCHED accrue, slice the block cell per-source — if PAIR_RSI_OB bull/chop∧ADXΔ<0 shorts stay net-positive OR WR≥55% on N≥10-15 → SCOPE the filter to FAN_RATIO_GATE only (exclude PAIR_RSI_OB; UNMATCHED stays in until it has data). If all sources bleed alike → keep universal.** Same-day twin: passthrough-long tracker (`PASS:*` rows in the Source×BTC-Regime cross-tab, observation-only) for the bull-mechanism / re-enable-normal-longs hunt.
- **🔀 FLIP-LONG REGIME GATE — SHIPPED 2026-06-17 PM (operator-directed; MIRROR of the flip-short 2D gate).** `flip_long_regime_block_adxd_max=99.0` + `flip_long_regime_block_regimes="STRONG_BEAR,HEALTHY_BEAR,CHOPPY_FLAT"`. Block any flip-LONG (a blocked SHORT faded → LONG) when BTC regime ∈ bear/chop. Mechanism: a flip-LONG in a STRONG_BEAR = long-into-the-trend; this batch AAVE+TAO (STRONG_BEAR, RSI~34) both straight to SL = **2/0%WR/−$220** (real Source×Regime table: `FAN_RATIO_GATE LONG · S.BEAR = 2/0%/−1.10/−$220`). **KEY ASYMMETRY vs the short gate: the long losers were ADXΔ-AGNOSTIC** (ADXΔ +1.5, regime was the killer not falling ADX) → `adxd_max=99` = **REGIME-ONLY block** (no ADXΔ cut); lower it later only if a long ADXΔ cell proves out cross-batch. `BEAR_EXHAUSTED` deliberately EXCLUDED (winding-down bear → a mean-reversion long is defensible). Universal block in `_flip_filters` (reason `FLIP_LONG_REGIME`, rides `_record_filter_block`); D11 wired (FLIP panel input + load/save, cyan row). **DISCIPLINE NOTE: shipped on N=2 (operator-directed) — below the N≥15 bar; it's the structural mirror of a validated, shipped filter, so mechanism-justified, but the sample is thin.** **TIGHT REVERT: re-open the blocked bear cells if would-be-blocked flip-LONGs flip to WR>45% on N≥15 fresh.**
- **🔀 FLIP-SHORT B2 (strong-bull any-ADXΔ) + B1 (anti-parabola) — SHIPPED 2026-06-17 PM (operator-directed).** Two flip-SHORT blockers from the PAIR_RSI_OB ESPORTS(−2.25%/0s) + TAC(−1.20%) bull-short losses. **B2** `flip_short_regime_block_any_adxd_regimes="STRONG_BULL"` — block flip-SHORT in STRONG_BULL **regardless of ADXΔ**. Cross-pool STRONG_BULL loses BOTH ADXΔ halves (N=26: ADXΔ<0 −$190/N=11 already-blocked + **ADXΔ≥0 −$531/N=15 was LEAKING** through the ADXΔ<0-only gate). Strong-bull-specific (healthy-bull/chop ADXΔ≥0 ≈ breakeven −$103/N=16 → kept at ADXΔ<0). Catches BOTH ESPORTS+TAC. **B1** `flip_short_stretch_block_max=2.0` — block flip-SHORT when EMA5 stretch% ≥2 (anti-parabola; shorting a vertical blow-off). Pool stretch≥2 = N=2/0%WR (ASTER+ESPORTS), **0 winners removed** (1–2% band 67%WR preserved); regime-agnostic catastrophe guard. Both in `_flip_filters` SHORT-gated (B1 reason `FLIP_SHORT_HISTRETCH`, B2 rides `FLIP_SHORT_REGIME`); D11 wired (FLIP panel orange+rose row + load/save). On the 2 batches: +$454 → +$952, all 3 pool losers removed, 0 winners. **DISCIPLINE: B2 independent edge is MILD (~−$197/13 after removing the 2 in-sample motivators; widens a filter validated on ADXΔ<0); B1 is N≈1 independent but never false-positives.** **TIGHT REVERTS: B2 — re-add the ADXΔ<0 requirement for STRONG_BULL if strong-bull ADXΔ≥0 shorts hit ≥50% WR on N≥10 fresh. B1 — raise the threshold toward 3 (or off) if a stretch∈[2,3] flip-short wins on N≥5 fresh.**
- **🔀 FLIP-SHORT BTC-30m-RSI-RISING BLOCK — SHIPPED 2026-06-18 (operator-directed). The cleanest cross-batch differentiator found.** `flip_short_btc30_rise_block_min=0.0` → block flip-SHORT when `(entry_btc_rsi − entry_btc_rsi_prev6) > 0` (BTC 30m RSI rising). MECHANISM: a FAN flip-short fades a parabolic pump; when BTC's 30m momentum is RISING the whole market bounces → the faded pump squeezes (today's HEALTHY_BULL disaster); when FALLING the pump exhausts → the short pays. **2-batch CONSISTENT (the rare stable signal — most flip differentiators flipped):** FAN-only, BTC-30m-rising = **−$1031** (today −$965, yest −$66) vs falling = **+$811** (today −$33, yest +$844); the win−loss gap was +$910/+$932 both days. Re-sim: TODAY −$998→−$33, YESTERDAY +$778→+$844. Pair-RSI direction by contrast FLIPPED (rising +$668 yest → −$1128 today) = noise; continuous indicators (RSI/ADX/stretch/range) showed NO SL-loss-vs-win separation. Block in `_flip_filters` SHORT-gated (reason `FLIP_SHORT_BTC30_RISE`; added `btc_rsi_prev6` to `_ff_in`); D11 wired (FLIP panel emerald row + load/save). Halves the flip-short count (only short when BTC 30m rolls over) — correct, that's the only condition the edge survives. **DISCIPLINE: 2 windows (below the ≥3 bar) — operator-directed; mechanism-strong + the rare cross-batch-stable signal. CONFIRM/REVERT: if BTC-30m-falling flip-shorts go net-negative OR rising-cohort flips to ≥55% WR on N≥10 fresh → raise the threshold / disable (set min=99).**
- **🔀 PAIR_RSI_OB FLIP SOURCE — DISABLED 2026-06-18 (operator).** Removed from `flip_entry_sources` (now `"FAN_RATIO_GATE:1.0"` only). ✗ HARMFUL cross-batch: this batch N=18/28%WR/−$523 (all HEALTHY_BULL), no source filters, 72% SL-rate. RE-ENABLE only if its phantom (`Pair RSI >65`, still seeding) clears WR≥60% & net-positive on N≥20.
- **🔭 WATCHLIST (2026-06-17, SHARPENED — supersedes the liquidity×ATR framing) — flip-SHORT ATR≥2 × REGIME, a 2D inversion.** ATR>2 ALONE is non-monotonic noise (pooled >2.00 bucket was 100% WR / +$495 on N=4; high ATR is the *best* bucket). The real axis is **BTC regime, and it runs OPPOSITE to the normal flip-short edge**: across the deduped pool, **ATR≥2 flip-shorts WIN in BULL (3W/0L, +$223, 2 batches) and LOSE in STRONG_BEAR**. MECHANISM: a FAN flip shorts a *parabolic pump*; in a strong bear that pump is a violent **counter-trend short-squeeze** that keeps ripping → the short never arms and the high ATR gaps the −0.70 SL to ~−1.2 (AGT 2.1, HUSDT 3.0, ESPORTS 4.0 this batch — all never-armed, all −1.2). In a bull the same pump is an *exhausting overextension* → the fade pays. **⚠️ INTERACTION: FLIP_SHORT_REGIME has this BACKWARDS for ATR≥2** — it blocks bull/chop∧ADXΔ<0 (which would kill the high-ATR bull *winners*, e.g. HUSDT-W/EVAA-W ADXΔ<0) and *exempts* bear (keeping the high-ATR *losers*). **Rule ① SHIPPED 2026-06-17 PM (operator-directed) — cut REFINED ≥2 → ≥3; Rule ② parked.** ① **`flip-SHORT ATR≥3 ∧ bear → BLOCK` — LIVE.** `flip_short_atr_block_min=3.0` + `flip_short_atr_block_regimes="STRONG_BEAR,HEALTHY_BEAR,BEAR_EXHAUSTED"`; block in `_flip_filters` gated to `flip_dir=='SHORT'` (reason `FLIP_SHORT_HIATR`, rides `_record_filter_block`); D11 wired (FLIP panel rose row + load/save). **WHY ≥3 not ≥2:** the cut isn't at 2 — bear flip-shorts by ATR: 1.5 STG-W/PORTAL-W(+$362), 1.9 EVAA-L, **2.0 BR-W(+$272)**, 2.1 AGT-L, 3.0 HUSDT-L, 4.0 ESPORTS-L. The clean 0%-WR cliff is **ATR≥3 (HUSDT+ESPORTS, −$245)**; ATR<2.5 bear shorts are net-POSITIVE (PORTAL/BR/STG) so ≥2 would kill proven winners. Bull high-ATR shorts WIN (regime inversion) → block is bear-only. **DISCIPLINE: N=2/one-window (both ATR≥3 losers from today's bear window) — operator-directed discipline-override, below bar.** **TIGHT REVERT: re-open (raise min toward 4 or drop the regime) if blocked ATR≥3 bear shorts would have ≥40% WR on N≥6 fresh — track via the phantom/passthrough + the LIVE Flip Trades × BTC-Regime table.** Cut may move (≥2.5 vs ≥3 vs ≥4 unresolved on N=2). ② **`carve ATR≥2 OUT of FLIP_SHORT_REGIME (bull side) → UNBLOCK` — PARKED (leave blocked).** The filter's −$691 loser value is **entirely ATR<2** (the ATR≥2 slice of the blocked cell is 2W/0L), so the carve-out would be low-risk — BUT it's un-earnable by a normal gate: **blocked shorts never fire → can't accrue live N → "unblock when WR≥X on N≥Y" is a CATCH-22** (operator-caught). Resolutions are only: (a) **leave permanently blocked [CHOSEN default]** — cohort ≈2–3% of flips, forfeits ~2 frozen-pool winners (HUSDT-W +0.32, EVAA-W +1.65) = negligible, validated filter intact; (b) **unblock-now + a measurable REVERT gate** (re-block if ATR≥2 bull∧ADXΔ<0 ≤40% WR on N≥6 once firing) — only if the operator wants the upside, rests on N=2–3; (c) phantom-instrument the blocked shorts (overkill for 3%). NOT pursuing unless operator opts for (b). The BLOCK candidate ① is the live one. TRACK ① via the LIVE Flip Trades × BTC-Regime table + per-trade ATR×regime×armed?×stop-overshoot; it ships only when the INDEPENDENT (multi-window) N clears — live cost of waiting is tiny. (Original SKYAI liquidity×ATR note retired: AGT/HUSDT were mid-cap $175–227M, so liquidity is NOT the discriminator — regime is.)
- **🔭 WATCHLIST (2026-06-17) — fan-ratio curve: do NOT widen the FAN dead-zone; if anything NARROW the chop middle.** Phantom FAN-RATIO CURVE shows the two strong UNBLOCKED buckets (fan 0.00-0.85 +0.46, fan 3.00-5.00 +0.68) — tempting to add to `fan_ratio_block_long` so we flip them, **but those are the DELIBERATELY-PRESERVED long-edge zones (Jun-10: fan 0.6-0.85 longs +$499, fan 3-5 longs +$102 — that's WHY they're excluded).** The phantom short-fade paying there = both-sides-chop confound, NOT a bad long → flipping them sacrifices a REALIZED long edge for a PHANTOM short. DON'T add them. Conversely the currently-flipped 0.85-3.00 zone is the WEAKEST (+0.05..+0.17, 1.35-1.65 = −0.095 NEGATIVE = pure-chop where neither long nor flip-short works) → the only directional hint is to SKIP the flip in ~1.35-2.00 (block the long, don't open the short), i.e. NARROW not widen. fan≥5 already handled (FLIP_FAN_SPIKE). ALL phantom / N=4-8 per bucket / single regime / observation-only → NOTHING shippable; re-examine the narrow-1.35-2.00 idea only at much higher N. FAN_CONTROL SHORT (+0.517, N=18; pADX-rising sub-row 9/9/+0.792) confirms the pADX-rising fade edge but is also phantom.
- **🔭 WATCHLIST (2026-06-17) — flip-short MULTIPLIER candidate: overbought-fade (entry RSI ≥ 65).** Combined 2 batches (12-20-07 + 76-ref): flip-shorts with entry RSI 65-80 = **N=10, 90% WR, +0.271% avg, +$233** — the only clean winner *signature* in the scan, and mechanistically sound (fading an overbought spike). Overlaps the already-live `PAIR_RSI_OB` source. **NOT a ship: N=10 ≪ 30 multiplier gate (need N≥30/WR≥70%/avg≥+0.10%/Total$>0).** Accrue toward N≥30, then per-pair concentration check + RE-SIM UNDER THE NEW EXIT (N1.0+ratchet) before sizing — a multiplier amplifies the exit's output, and the pre-change P&L over/under-credits it. Other thin winner-signatures noted (don't ship): low range-position 0-50% (N=6/100%/+0.65%), ADXΔ≥0×RSI50-55 (N=8/+0.32%). **TRAP (do NOT multiply): range-pos 90-100% = 70% WR but net-NEGATIVE (−$49) = high-WR-but-losing fat-tail cohort.** No multiplier action now — flip sleeve is ~breakeven overall (N=110/56%/+$80); stabilize the just-shipped exit/entry changes first.
- **🔭 WATCHLIST (2026-06-17) — generalize the BE-ratchet to the NORMAL realtime trailing (active, decoupled breakeven floor).** Root cause of VELVET-type round-trips (armed peak +0.76 → −1.20 SL): the realtime trailing pullback trigger is ATR-FLOORED (`_effective_pullback = max(base, entry_atr × trailing_atr_multiplier=0.5)`), so on a high-ATR pair (VELVET ATR 1.59%) the give-back needed to ARM the trail is huge → it never crosses while in profit (`trailing_first_pullback_pnl_pct = −1.094` = first crossing was already at −1.09%) → the ATR-widened hard SL is the only exit. **`trailing_min_profit_to_fire`=0.1 does NOT cover this** — verified at [trading_engine.py:9127](services/trading_engine.py:9127) it's a SUPPRESSOR (cancels an already-triggered trailing close below 0.1, flips-only), the OPPOSITE of a floor — it removes exits below 0.1, never creates one, and it's COUPLED to the ATR-pullback trigger that didn't fire in time (on VELVET it even suppressed the −1.09 close → SL took it to −1.20). **PROPOSAL: an active, DECOUPLED BE-ratchet on the realtime path** — once `running_peak ≥ be_arm (0.45)`, force a close at `pnl ≤ be_lock (0.10)` regardless of the ATR-pullback (`reason TRAILING_BE_RATCHET`). Fixed P&L level → immune to the high-ATR scaling problem; direction-agnostic (covers flip-longs AND the short side as a backstop). VELVET: would exit ~+0.10 vs −1.20 (saves ~+1.30); shadow_tight (0.25 P&L give-back) +0.51 confirms a fixed floor works. Config: `trailing_be_ratchet_enabled` + `trailing_be_arm_pct=0.45` + `trailing_be_lock_pct=0.10`. **SCOPE decision pending: flips-only (lower blast radius, where the gap is seen) vs all trailing trades.** Caveat: caps a dip-then-rerun at the lock (bounded — banks a small win; lower the lock for more room). Note: this is the GENERAL version of the SHORT runner BE-ratchet already shipped (which is ATR-floor-specific + short-only).
  - **🔭 WATCHLIST (2026-06-17, VERIFIED) — flip-LONGs have NO runner-trail/ratchet (exit gap, long-side twin of the short fix).** The runner-trail + BE-ratchet are `runner_trail_short_*` = SHORT-ONLY → never execute on a LONG. FLIP_FAN_SPIKE is universal but both this batch's flip-longs have fan≈1.0 (<5) → not blocked. **So BOTH flip-longs (HYPE, VELVET) are 100% UNTOUCHED by all 3 shipped changes (verified per-trade).** VELVET = armed peak +0.76 → round-tripped to the −1.20 hard SL (FLIP_STOP_LOSS L2); shadow says a trail saves ~+1.70 (tight +0.51 / strpk +0.49 vs −1.20). **The flip-long 0/2 record is PARTLY an exit artifact** — entries reached +0.37/+0.76 peaks then gave it all back; VELVET alone flips loser→winner just from a trail (HYPE peak +0.37 never armed = true entry-edge loss). **NOT building now: live flip-long sleeve is N=2 and is the `FAN⊘SHORT→LONG` idle-insurance source (0/2, ~zero phantom edge).** TRIGGER to build the long-side trail+ratchet = a phantom LONG cell promoting to live (build it TOGETHER with turning the source on). Note: for VELVET the TIGHT/strpk trail beat the ATR-floor (atr05 −0.06, atr10 −0.70 — ATR give-back too wide on a 0.76 peak), so flip-longs likely want a tighter trail than the shorts' ATR-floor — TBD on data.
  - **🔭 WATCHLIST (added 2026-06-17, operator) — mirror the 3 live short filters onto the FLIP-LONG sleeve.** When the flip-LONG trackers accrue data, slice flip-LONG WR/avg by these 3 dimensions (reflections of what we shipped for shorts off the prior batch) and promote to a flip-LONG entry block only at the standard gate (N≥30, WR/avg evidence, cross-batch, per-pair concentration check). Mirror math = reflect RSI around 50, flip the gap sign, keep stretch/ADX (direction-agnostic): ① **Stretch floor — flip-LONG `stretch < 0.12` → block candidate** (long stretch = (price−ema5)/price·100; low extension = no momentum behind the fade, same threshold as the short block). ② **BTC regime — `BTC RSI ≤ 40 AND BTC ADX ≥ 30` → block candidate** (mirror of the short's RSI≥60 & ADX≥30; don't fade-LONG into a strong/trending BEAR). ③ **Pair trend gap — `pair_trend_long_gap_min`: block flip-LONG when pair (EMA13−EMA50)/EMA50 ≤ 0** (mirror of `pair_trend_short_gap_max≥0`; don't long a coin still below its own 4h trend). All three are OBSERVATION-ONLY hypotheses for now — do NOT wire any as a live block until the flip-LONG data clears the gate; check whether the loser zone is 1–2 pairs (→ pair blacklist) before adding a dimensional filter.
- **🔀 3rd flip source PAIR_RSI_OB added 2026-06-16 — BELOW-EVIDENCE OPERATOR OVERRIDE @1× (DISABLED same day — see narrowing note above; phantom keeps tracking).** Fade an overbought-long (rsi>`momentum_long_rsi_max`=65) PAIR_RSI_RANGE block → SHORT, registry key `PAIR_RSI_OB` (mirrors the phantom cell "Pair RSI >65"). `flip_entry_sources="…,PAIR_RSI_OB:1.0"`. **Evidence: phantom N=11, 82% WR, +0.405% avg, +4.45% total, SL-rate 18%, 8 pairs, top-pair 18% — the best fade cell to date** (beats FAN's phantom on every axis). **OVERRIDE ACKNOWLEDGED: locked phantom-promotion gate is N≥30; we shipped at 11 (in-sample, 1 batch).** Wired in Phase-3 of `scan_and_trade`: the overbought block fires inside `get_signal` (Phase-1, signal→NO_TRADE), so a per-pair `rsi_ob_flip` marker rides the `_collected` row → Phase-3 calls `_maybe_open_flip(db, pair, "LONG", "PAIR_RSI_OB", …)`. Shows in the 🔀 Flip Trade Log as `PAIR_RSI_OB ⊘LONG` (source-agnostic grouping). **⚠️ TWO KNOWN RISKS at ship (operator accepted):** ① phantom≠live — FAN proved the gap (phantom +0.175 → live +0.014, now ✗ revert); PAIR_RSI_OB's bigger cushion + low 18% SL-rate *should* survive the slippage but unproven. ② EXIT MISMATCH — it rides the current ATR-widened realtime stack, which the methodology + the −$243/46% experiment showed GIVES BACK moderate-peak reversions (PAIR_RSI_OB AvgPk +0.78 = exactly that profile); the +0.405% was measured on the FLAT quick-target phantom model, not this exit. **TIGHT REVERT GATE (this source, tighter than standard): drop `PAIR_RSI_OB` from `flip_entry_sources` if at live N≥12 WR≤60% OR avg≤+0.10% OR net-$ negative; INSTANT review if 3 of its first 6 hit FLIP_STOP_LOSS, OR if it caps a winner that would've made ≥+0.5% twice.** Phantom "Pair RSI >65" stays on the flat model = built-in A/B (flat-phantom vs ATR-live). If it fails the gate, FIRST swap to a flat/quick-target exit before dropping (the exit, not the entry, is the prime suspect).
- **📋 NEXT-BATCH FLIP REVIEW CHECKLIST (updated 2026-06-15 → 5 lenses; run on the next FULL batch).** ① **🧪 Leftover-Filter Test — PRIMARY:** which skipped downstream filter separates flip winners from losers (★ gate candidate). ② **🔄 Phantom Tracker:** regime (aligned/counter) + pADX sub-rows — does the edge clear execution friction at N. ③ **Entry Conditions by Outcome:** cross-validate the separator from ① / flag confounds (e.g. is "block C-only" == "block counter-trend"). ④ **🧬 4-Cohort + Pattern C/W trackers:** does a loser-signature (esp. **C7**) or winner-signature cleanly tag flip outcomes. ⑤ **🚦 Fast-Exit Counterfactual (FLIP) — REVIEW (added Jun-15):** does the +X%/2min fast-take on fades hold cross-batch — track fires, $Δ vs no-fast-exit, and whether any *winner* gets capped (fast-pop=fakeout thesis: losers pop <2min, winners build 6-40min in; in-sample 1 batch +$284 @0.20%/2min, 2-3 fires). Pre-committed: build/ship the flip fast-exit only if the +$ edge holds AND ≤1 winner-cap on N≥10 fires; else stays phantom-only. ⑥ **🔀 PAIR_RSI_OB live flip (shipped Jun-16, @1×) — REVIEW vs its tight revert gate:** track the live `PAIR_RSI_OB ⊘LONG` row in the 🔀 Flip Trade Log — at live N≥12 drop the source if WR≤60% OR avg≤+0.10% OR net-$ negative; instant review if 3 of first 6 hit FLIP_STOP_LOSS or it caps a ≥+0.5% winner twice. Compare live (ATR exit) vs the phantom "Pair RSI >65" cell (flat exit) = the built-in A/B; if it fails, swap to a quick-target exit BEFORE dropping (exit is the prime suspect, not the entry). ⑦ **🌡️ BTC 1h × 5m RSI direction cross-tab — FALLING/FALLING cell for FLIP shorts (NEW lens, not in the Leftover-Filter Test):** the live `BTC_1H_5M_RSI_DIR_GATE` only blocks the **RR** (Rising/Rising) cell for NORMAL shorts, and flips fire upstream so they bypass it entirely. Hypothesis to test on flip shorts: does **FF** (BTC 1h RSI Falling × 5m RSI Falling) separate flip winners from losers? **BATCH 1 (Jun-16, FLIP shorts): FF = N=7, 14% WR, −$318, −0.46% avg — the SOLE loser cell** (RR +0.06% / RF +0.36% / FR +0.26% all positive). This INVERTS the cross-tab's "aligned wins" header (FF is 'aligned' for a short — BTC falling both TFs — yet worst), consistent with an exhausted/over-extended down-move that bounces and runs over the fade. Meets the gate's WR+sign already but N=7/1-batch in-sample → **counts as batch 1 of ≥2.** **GATE: if FF cleanly tags flip-short LOSERS (≤40% WR AND net-negative) on N≥10 fresh flip shorts across ≥2 batches → ship a flip-specific FF block** (a downstream gate the flip currently skips). Until then observe only — the FLIP cross-tab split is captured via `entry_btc_rsi_1h`/`_prev` + 5m rsi. ⑧ **🏃 Leash Shadow: Exit Capture by Direction (SHORT flips) + short-FLIP post-exit — should flip shorts adopt the normal-short Runner Stretch-Trail?** Normal shorts run the LIVE Runner Stretch-Trail (`runner_trail_short_enabled`, Jun-12 shadow-strpk, N=20 discipline-override), but flips have **short-runner OFF** (a flip SHORT trails like a LONG). Evaluate the 🏃 Leash Shadow table on the flip-short cohort + the short-FLIP post-exit-regret to decide whether to extend strpk to flips. **BATCH (Jun-16, N=10 armed flip shorts): strpk03 +$149 · strpk +$107 · strpk_signed +$113 · wide +$101 · tierA +$59 (★ helps); tight +$176 but ⚠ SIM-OFF (unreliable); stren +$6 / tierB +$35 (marginal)** — i.e. a stretch-peak trail would have captured MORE than the current tight ATR trail. **⚠️ TENSION — two reasons this may NOT transfer:** (a) the normal-short runner-trail's mechanism is "ride through the first micro-bounce of a capitulation CASCADE" (C1/W2 continuation); flip shorts are FADES of a *rising* pair (opposite character) — the cascade logic may not apply. (b) The earlier flat-model experiment DISABLED the wide trail for flips precisely because it "ate the winners" on moderate-peak (~0.5%) flip reversions (−$243/N=13). The Leash-Shadow ★ may just reflect this batch's flip shorts happening to keep falling. **GATE: enable the flip-short runner-trail only if strpk/strpk03 holds ★ helps with +$Δ on N≥20 armed flip shorts across ≥2 batches AND the short-FLIP post-exit-regret confirms peaks are LEFT ON THE TABLE (not given back) AND it beats the FLAT-phantom A/B; else keep short-runner OFF for flips.** Honor line-86 sanity: the strpk leash row must ≈ actual once live (persistent gap = sim divergence). ⑨ **📐 Performance by EMA5 Stretch — for FLIP shorts:** does the pair's entry EMA5 stretch separate flip winners from losers? **NOTE the read is likely INVERTED vs a momentum long** (a flip SHORT fades an overextended *rising* pair → HIGHER upside stretch = more overextended = *more reversion fuel* = should be the BETTER fade; for a normal long high stretch = chasing = bad). Same inversion logic as lens ④ (Entry Quality Score). **BATCH (Jun-16, bearish flip shorts): 0.08-0.12% = 0%/−0.72% (N=1) · 0.20-0.25% = +1.07% (N=1) · 0.25-0.30% = 75%/+0.16% (N=4) · >0.30% = 75%/+0.04% (N=8)** — directionally the low-stretch single loser fits "thin fuel = bad fade," but the top bucket (>0.30) is the largest yet weakest avg, so NON-MONOTONIC + tiny N = no clean signal yet. **GATE: if a stretch band cleanly + monotonically separates flip-short winners/losers on N≥10/band across ≥2 batches → flip stretch rule (block low-stretch "thin-fuel" fades, and/or size up high-stretch); else observe.** Captured via `entry_ema5_stretch` on flip orders. ⑩ **📊 Performance by EMA Fan Acceleration (5-8 vs 8-13) — for FLIP shorts:** which fan-ratio bucket within the block band separates flip winners from losers? **SELF-REFERENTIAL caveat: the FAN flip only fires because fan ratio is in the DZ block band (0.85-3.0), so this is a SUB-SEGMENTATION of the trigger itself** — and it's the LIVE analog of the phantom FAN-RATIO CURVE (cross-check the two). **BATCH (Jun-16, bearish flip shorts): 0.85-1.00 = 50%/−0.16% (N=2) · 1.00-1.35 = 100%/+0.54% (N=3) · 1.35-1.65 = 100%/+0.25% (N=4) · 1.65-2.00 = 50%/+0.51% (N=2) · 2.00-3.00 = 33%/−0.66% (N=3)** → winners concentrate MID-band (1.00-1.65), losers at the UPPER flank (2.00-3.00 = the clear loser). Non-monotonic (winners-in-middle) + tiny N/bucket = noise-grade for now. **GATE: if the 2.00-3.00 (and/or 0.85-1.00) flank holds net-negative on N≥10/band across ≥2 batches AND agrees with the phantom FAN-RATIO CURVE → NARROW the flip's fan band to exclude the loser flank (a fan sub-gate ON the flip, not a new filter); else observe.** Captured via `entry_ema_gap_5_8`/`entry_ema_gap_8_13` (ratio). ⑪ **🔁 STAGED RE-ENABLE of LONG_UNMATCHED_ONLY + PAIR_RSI_OB (parked Jun-16 to isolate FAN) — sequencing + result tracking.** Both were dropped from `flip_entry_sources` to give FAN_RATIO_GATE a clean optimization window; they keep accruing in the phantom tracker meanwhile (cells "Pair RSI >65" / "LONG_UNMATCHED_ONLY" = the live↔phantom A/B baseline to size the execution drag at re-enable). **RE-ENABLE TRIGGER:** once FAN reaches a decision (kept / dropped / sub-gated via lenses ⑦–⑩), re-add the parked sources **ONE AT A TIME** (one-change-at-a-time attribution — PAIR_RSI_OB FIRST, it has the stronger phantom: N=11/82%/+0.405%), and only if that source's phantom is STILL ★ at re-enable time. **TRACK each via its 🔀 Flip Trade Log row against its LOCKED revert gate:** `PAIR_RSI_OB ⊘LONG` → drop if live N≥12 WR≤60% OR avg≤+0.10% OR net-$ neg (instant on 3-of-first-6 FLIP_STOP_LOSS or 2 winner-caps); `LONG_UNMATCHED_ONLY ⊘LONG` → drop if live N≥12 WR≤55% OR avg≤+0.05% OR net-$ neg (instant on 3-of-first-6 FLIP_STOP_LOSS). Compare each re-enabled live cell vs its parked-phantom cell (did the +$ edge survive execution?). ⑫ **✖️ SIZE-MULTIPLIER CANDIDATE — flip shorts in BTC RSI 40-45 × BTC ADX ≥35 (Pattern-W treatment).** **BATCH (Jun-16): N=10, 90% WR, +0.30% avg, +$307.63** — passes the W-multiplier bar on WR/avg/Total$ but **NOT on N (gate N≥30; at 10)**. Mechanism = "aligned fade in a strong bear" (BTC RSI low + strong ADX downtrend = shorting with the macro) → likely the SAME regime-alignment edge as lens ② (cross-check they don't double-count). **DO NOT SHIP NOW (below gate + two blockers):** (a) a flip multiplier already fired ✗ HARMFUL — FAN 2× = −$912 by amplifying the fat-tail losses; (b) we just narrowed the sleeve to DE-RISK FAN, so adding leverage back contradicts that. **GATE (locked, Pattern-W): ship 1.5× (Phase-3 staging) ONLY when N≥30 AND WR≥70% AND avg≥+0.10% AND Total$>0 AND the FAN source ITSELF is net-positive demux (never multiply a subset of a net-losing source) AND BE-compat (≥60% of the cell's losses peak ≥+0.20% so caps bound the tail). Step to 2.0× only after +50 trades. Multiplier verdict gate (≥5 fresh fires): ★ WORKING keep · ⚠ DRAG drop to 1.5× · ✗ HARMFUL revert 1.0×.** Until all five conditions clear, track only (Multiplier Cell Performance — SHORTs / Flip Trade Log Size×/Δ$-vs-1× cols). **Decision rule (all 12): a separator must hold 2+ batches AND clear friction → add gate (or, for ⑫, a multiplier); nothing holds across 3 batches → retire the live flip source.**
- **🔀⚠️ FLIP CHAIN-POSITION BUG — WATCHLIST (identified 2026-06-15, NOT fixed; decision deferred to data).**
  **CORRECT DESIGN (the intent):** the flip should fade ONLY entries that *passed every filter and would have actually traded* but for the fan dead-zone — i.e. genuine "would-have-opened" longs (fan 0.85-3.00 / 5.0-99) and shorts (fan 1.00-1.90). For that, `FAN_RATIO_GATE` must be the **LAST** gate in the chain, so a fan-blocked entry is guaranteed to have cleared all other filters.
  **ACTUAL DESIGN (what's coded):** `FAN_RATIO_GATE` fires at **gate #8 of ~27** in `scan_and_trade` (line ~6771), and the flip opens *right there* — BEFORE ~19 downstream gates (`BTC_TREND_FILTER`, `BTC_SLOPE_*`, `BTC_1H_*`, `BTC_ACCEL_CHASE_LONG`, `PAIR_TREND_FILTER`, `VOL_GATE`, `BREADTH_GATE`, PAIR_ATR…). So flips fade a **contaminated SUPERSET**: entries that passed only the first 8 filters, NOT all of them. A fan-blocked long that would have failed a downstream gate (e.g. NEAR — a counter-trend bullish stack in a HEALTHY_BEAR that `BTC_TREND_FILTER`/`PAIR_TREND_FILTER` would likely have killed) still triggers a flip today. (Does NOT change which entries *trade* — fan-band entries are always blocked either way — only which ones *flip*; the phantom seeds at the same wrong point, so its +0.13% edge is on the superset too.)
  **COUNTERINTUITIVE TWIST (why not fixed blindly):** the downstream gates block *counter-trend* entries, so the "junk" longs they'd block are exactly the ones whose fade (SHORT) is **macro-aligned** (short with a bear) — and those won this batch (NEAR +0.40% pk, FARTCOIN +0.63% pk, N=2 = noise). The passed-all-filters longs are in *supportive* regimes → their fades are counter-trend-into-the-trend (the cohort steamrolled in STRONG_BULL). So fixing blindly might drop the WINNERS and keep the losers.
  **DECISION GATE (resolve with data, target N≥30 clean flips): deeply evaluate flip WINNERS vs LOSERS — primarily by REGIME ALIGNMENT (macro-aligned fade vs counter-trend fade, using captured `entry_macro_trend`/`entry_btc_regime`) and the Entry-Conditions-by-Outcome separators.** ① If a CLEAR loser-filter emerges (e.g. counter-trend fades lose, aligned win) → **KEEP the current mid-chain design and ADD that filter** to remove the losers (a regime-gate, NOT a chain move). ② If NO clean separator emerges → **revert to the CORRECT design: move `FAN_RATIO_GATE` + the flip trigger + the phantom seed to the END of the filter chain** (after `BREADTH_GATE`, before `open_position`), so flips only fire on genuine passed-all-filters entries — and re-validate the edge on the clean cohort. Until resolved: keep mid-chain (more volume = faster N for the split); cost is low (flip on 1× probation, net-negative regardless).
- **🔀🧪 REGIME-ALIGNMENT FINDING + FAN_CONTROL / FAN-RATIO-CURVE EXPERIMENT — shipped 2026-06-15 (phantom/observation-only, forward-only).** **THE FINDING (mechanism-backed, from the 2-batch flip split):** flip winners vs losers separate cleanly on **macro regime alignment, NOT the FAN signal.** A fade that aligns with the BTC EMA20 slope wins (aligned: ~88% WR / +$126 in-sample); a counter-trend fade loses (short into a RISING BTC: ~50% WR / −$120). Smoking gun = FARTCOIN: same pair, won its fade in a bear window, lost the SAME fade twice in a bull window. **Open question this experiment answers:** is the fan dead-zone (0.85-3.00/5.0-99 long, 1.00-1.90 short) actually *doing* anything, or is it incidental to regime alignment? **Two probes added:** ① **FAN_CONTROL phantom** — a virtual fade seeded when an entry's fan ratio is OUTSIDE the dead-zone (i.e. the entry PASSES the fan filter); it's the A/B control for FAN_RATIO_GATE — if FAN_CONTROL fades pay the SAME as FAN_RATIO_GATE fades (once regime-matched), the fan band earns nothing and the real signal is pure regime. Seeded in the fan gate's `for-else` branch (loop completes with no band match → fan outside dead-zone), fail-silent, never affects live. ② **FAN-RATIO CURVE** — a new `🔀 Phantom Flip` sub-table (`#phantom-fan-curve-body`, both text exports) that pools FAN_RATIO_GATE + FAN_CONTROL phantoms and buckets by fan ratio (0-0.85 / 0.85-1.0 / 1.0-1.35 / 1.35-1.65 / 1.65-2.0 / 2.0-3.0 / 3.0-5.0 / 5.0-99) × regime alignment (all / aligned / counter), flagging which buckets are in the dead-zone (DZ). Goal: find WHERE the aligned edge peaks across fan ratio → **define the best fan range for fades** (refines or replaces the dead-zone for the flip sleeve). **READ at N≥30 pooled fades/bucket: if the aligned edge is flat across ALL fan buckets (DZ and non-DZ alike) → the fan band is incidental; design the live flip sleeve as a pure REGIME-ALIGNMENT gate (fade only when macro-aligned, any fan). If the aligned edge concentrates in specific fan buckets → tighten the flip's fan range to those. Either way the live regime-alignment gate (deferred above) gets its fan window from this curve.** Friction wall still applies: phantom is frictionless, live pays ~0.27%/trade → +0.30% promotion bar on any bucket before it ships live.
- **Book-short fan<0.6 (Jun 13, OBSERVATION-only — NOT a block candidate).** Recent-era (May27-Jun13) book shorts with fan_ratio<0.6 = N=11, 55% WR, **−$327 demux** (loses even at 1×). BUT loss is fat-tail R:R: JTO −160, UNI −109, RENDER −98, BTC −65 (BTC now no-trade) vs 5+ winners +$17..+$84. **Fails the block test (55% WR → blocking kills winners; per locked "high-WR-but-net-losing = fix sizing not block").** Fan filter otherwise well-calibrated for both current cohorts (long block 0.85-3.00 kills the 1.7-3.0 spike zone −$441; short block 1.00-1.90 kills the 1.0-1.7 zone 14%WR −$403). **PROMOTE to filter candidate ONLY if fan<0.6 shorts degrade ≤40% WR on N≥20 fresh, OR a 2D sub-signature isolates the fat losers. Default: no action.**
- **🏃 RUNNER STRETCH-TRAIL SHORT — SHIPPED Jun 12 (DISCIPLINE-OVERRIDE, N=20<30; operator: "results too conclusive").** `runner_trail_short_enabled=true`, arm peak **0.45** (= leash ACT, matches the measured sim — do NOT raise), **no ATR gate** (atr_min=0; book shorts enter at ATR 0.4-1.0), K=0.5 (shadow strpk). Once a SHORT peaks ≥0.45: tight trailing suppressed AND **the EMA13-short cross records a PHANTOM instead of closing** (`[EMA13_RUNNER_SUPPRESS]`; the measured uplift comes from riding through the first cross) — exit = stretch ≤ 0.5× peak stretch (close reason RUNNER_TRAIL) or hard SL backstop. Evidence: shadow strpk on current-stack book shorts Δ+5.06pp/+$996 (N=20, 13/7); recent era (Jun 9+) **8/0 better, +$979**; current batch +$1,022 on 6 armed shorts. Mechanism: C1/W2 capitulation cascades continue after the first micro-bounce that used to close us. LONG side untouched (all leashes ≤ actual — trail already optimal; long runner-trail stays OFF). Known minor sim-divergence: live peak-stretch tracks from entry vs shadow's post-arm — slightly conservative. **TIGHTER-THAN-STANDARD REVERT GATES: ① shadow_tight (still running) = the old-policy proxy — if cumulative (actual − shadow_tight) < 0 on N≥8 fresh armed shorts → revert to OFF. ② Instant review if 2 armed shorts convert peaks ≥+0.45 into hard-SL losses (giveback tail). ③ Sanity: the strpk leash row in the 🏃 Exit-Capture table must ≈ actual once live; persistent gap = implementation diverges from sim → investigate before anything else.**
- **Pair universe 50→75 REVERTED to 50 same day (Jun 12).** The expansion audit ranked the RAW Binance list; the bot's eligibility filters (180-day new-listing excludes ~47 of the highest-volume pairs + alpha-subtype) run BEFORE the top-N cut, so eligible rank-75 reached **$24M** (and even eligible rank-50 dips to ~$32M) — squarely in the long-hostile (39% WR) + liquidity-cap-pinched tier. Operator caught it live; reverted via UI + git same day. **KEPT: `entry_pair_rank` column + 🏅 Performance by Pair Rank table** (now buckets 1-10…41-50 + a `>50` catch-all) — first-ever visibility into rank structure inside the Top 50. **Boundary exploration CLOSED same evening — all four candidates rejected by data (see DECISION_LOG 06-12 evening): 75-universe (reverted) · 180→90-day listing filter (admits 1 hyper-volatile pair; age never was the protective mechanism — burns were OLD pairs) · pair_atr_max_short (zero trades to block in 525 shorts; entries structurally exclude high-ATR shorts) · $40M volume floor (1% of entries, net −$16 — bottom-of-universe pairs rarely pass the stack). Do NOT re-litigate without new data. Carried forward: live-mode gap-through tail → de-lever phase of scaling roadmap.**
- **W2+W1 SHORT cell refinement (Jun 10):** multiplier moved from plain W2 to the W2+W1 combo. Evidence: W2+W1 = +$1,480/79% WR (N=14, demux +$590); W2-alone = −$123 demux (N=16); W2+W6-no-W1 = −$207 demux. **READ GATE (N≥5 fresh W2+W1 fires): ✗ HARMFUL (Total$<0) → demote combo to 1×; also watch the now-1× W2-alone cohort — if it surprises ≥70% WR + positive on N≥10 fresh, re-add a W2 rule.**
- **BTC ADX 20-25 × RSI 60-70 LONG multiplier (Jun 10, WATCHLIST — NOT shipped):** the only multiplier-candidate axis that survived the unmatched-long deep-dive under the guard stack. Current-era survivors: N=6, 83% WR, +$199 demux (5 dates); all-time survivors BTC ADX 20-25: N=124, 55% WR, +$90 (only positive band with real N, but fails WR/avg promote gates; flanks 0-20 and 25-30 negative both eras). Equals the existing-but-shadowed BTC-level cell `60-65×22-25`. **PROMOTE GATE: activate (e.g. re-point the UNMATCHED LONG rule or un-shadow the BTC cell) if fresh unmatched-long survivors in BTC ADX 20-25 × RSI 60-70 hit WR≥70% AND Total$>0 on N≥20.** Until then the long book multiplier question stays governed by the UNMATCHED-LONG gate above.
- **Global Vol filter (Jun 2):** revert a SHORT side if would-be-blocked SHORTs ≥55% WR on N≥10 fresh · drop LONG floor→0 if it clips ≥3 runner winners (peak≥+3%)/batch with no offsetting saves. Watch VOL_GATE / VOL_GATE_MAX_SHORT counters fire.
- **ATR-LOW Fixed TP +0.25% LONG (Jun 5):** profit-lock on the no-runner cohort. **Watch the `ATR_FIXED_TP L1` row in Post-Exit Regret:** high avg Post-Peak (cohort kept running after the cap) → 0.25% too tight, raise/disable; Post-Peak ≈ 0 → lock is correct. No N-gate yet — accumulate ≥10 fires first.
- **gvol capitulation override REMOVED, SHORT (Jun 5):** high-GV shorts now always blocked (when Max-S cap active). **RE-ENABLE the override only if would-be-passed capitulation shorts (GV>Max-S AND BTC RSI<30 & slope<0) show ≥55% WR on N≥6 fresh.** No-op while Max-S cap-driven blocking is the binding piece.
- **ETH no-trade / track-only (Jun 5):** added for recurring short-squeeze (−$230 this batch + −$230 prior). **Re-evaluate (return to tradeable) only if ETH — either side — would-have-won ≥55% WR on N≥8 fresh while track-only.** Watch PAIR_NO_TRADE + the ETH would-be record.
- **Chase Filter LONG — RE-ENABLED 2026-06-05 (Jun-4 A/B resolved).** Kept ON: blocks the LONG-loss clusters (13 longs / −$853 on the batch). **KEEP-ON unless would-be-blocked LONGs (BTC-accel-since-last-LONG-in-30min) show ≥50% WR on N≥10 fresh** (= chase is a false signal → pursue C-level filters instead). Watch BTC_ACCEL_CHASE_LONG counter.
- **BTC RSI×ADX Cross-Filter simplification (Jun 5, WATCHLIST — observation, NOT shipped):** proposed = ① delete dead **rule 3** (RSI 60-65×ADX27-30 — never fires, first-match-wins claims 60-65 at rule 2) + ② replace the 5 RSI×ADX rules with two **RSI-only** blocks `RSI 50-55→block` and `RSI 60-100→block` (**drop the ADX axis** — confirmed it does NOT separate within any band: 50-55 = −394/−551/−196 across ADX, 65-70 = −1564/−1251/−829). Net surface: LONG allowed only **RSI 40-50 + 55-60** (all ADX 18-40); blocked 50-55 & ≥60. **Rationale:** ADX-*strength* ≠ BTC *direction* → the filter never measured the right axis (barely separates, −0.194 vs −0.174); it blocks ≥70 overbought yet ALLOWS 65-70 (same chase mechanism, worse outcome). Band sign-consistency (FULL vs last-4): 40-50 +both · 50-55 −both · 55-60 FLIPS · 60-65 −both · **65-70 −both (−$3,644 / −$1,643, biggest LONG loser, currently wide-open)** · ≥70 −. Last-4 impact: **kills 89 longs** (60-65 41/−$802 + 65-70 48/−$1,643 = **−$2,445**); 0 measurable adds (55-60-full opening is forward-only). **SHIP GATE (next batch): ship `60-100→block` IF 60-65 AND 65-70 longs are net-negative AGAIN (3rd-window confirmation).** Keep 55-60 allowed but do NOT bank it (non-stationary; FULL −$733 vs +$524 recent). **Post-ship revert: re-open 60-65 if would-be-blocked 60-65 longs ≥50% WR on N≥10 fresh.**
- **BTC 1h Slope MIN floor `-0.60` SHORT (Jun 3, below-gate ship):** evidence = BTC 1h slope < −0.60 SHORT = **0W/4L** (SEI, XRP, BTC, JTO) across 2 sessions; not covered by existing filters (these had rising pair-ADX + BTC ADX outside the 24-30 kill-zone). N=4 / 2-3 correlated events = below the N≥30 bar → tighter gate. **Revert `btc_1h_slope_min_short`→0 if would-be-blocked (slope<−0.60) SHORTs show ≥50% WR on N≥6 fresh, OR if BTC_1H_SLOPE_MIN_GATE blocks a would-be-winner SHORT on 3+ separate sessions.** Re-confirm at next ≥30-trade checkpoint. LONG floor stays DISABLED (its loser zone is the FLAT band -0.10..0, structural N=60/6-dates — a separate band-filter, not yet built).
- **Pair ADX Direction = `rising` (Jun 2, below-gate re-activation):** evidence = falling-ADX entries 1W/9L cross-batch+today (LONG 1/8, SHORT 0/2), all samples 06-02 + prior May 28 A/B (`rising` beat `both`). **Revert a side to `both` if would-be-blocked (falling-ADX) entries on that side show ≥50% WR on N≥6 fresh.** Also: if PAIR_ADX_DIR blocks >15% of that side's attempts (vs ~1% historical base rate) → regime shift, re-examine. Watch PAIR_ADX_DIR counter.
- **ADX Δ filter (Jun 2):** SHORT would-be-blocked ≥50% WR on N≥6 → blank SHORT rule / re-disable · LONG ≥55% WR on N≥10 → blank LONG rule. WATCHLIST: filter is **blind to FALLING ADX** (signed match, only catches rising/climax). If a batch bleeds on sharply-falling-ADX entries → add a signed negative-range rule (parser needs signed lo-hi fix first).
- **PAIR_35-40_30-35 SHORT demote:** re-promote 1.5×→2.0× only if N≥5 fresh AND ≥70% WR. Any single fresh Δ$≤−$60 → keep 1.0× indefinitely.
- **UNMATCHED LONG 2× — ✗-HARMFUL GATE TRIPPED Jun 10, OPERATOR OVERRIDE (kept at 2×).** Fresh post-reset fires N=7, 14% WR, −$1,092 as-sized / −$546 demux; robust to the new filter set (excluding the 2 trades the 50-55 rule now blocks: N=5, −$658 — still trips). Operator explicitly declined the demote (second time, post-trip). **Transparency note: this is a formal gate violation kept on operator authority.** Replacement tracking gate (tighter): **demote to 1.0× WITHOUT FURTHER DEBATE if the next read (N≥5 additional fresh fires under the Jun-10 guard set) is ALSO net-negative demux** — the guards now fence the worst entries; if the cohort still loses clean of them, the multiplier has no defense left. Re-earn standard: ≥70% WR + positive on N≥5.
- **Extension multiplier LONG demote (Jun 4):** RESTORE 2× only if `Ext0.4-0.6_L` reaches **N≥15 fresh AND Total$>0** (current stack). Watch the Extension Multiplier Performance table — now fires at 1.0× but stays tagged, so the cohort's true 1× edge is observable before any re-promote.
- **C1 SHORT combination review (Jun 4, WATCHLIST — observation only, NOT shipped):** track ALL C1 SHORT signatures next batch — **full C+W** (not just C; report N, WR, Tot$ AND de-mux 1×). 7-pool baseline (BE-off, beW=0): **C1+C6 N=3 0W/3L −$667 (de-mux −$333, 2 NP) = TOXIC** · C1-only N=9 78% +$48 · C1+C3 N=2 100% +$202 · C1+C2 N=2 100% +$441 = all WINNERS. **Driver is C6, NOT any W:** C1·has-C6 = 0% WR/−$667 vs C1·no-C6 = 85% WR/+$691 (razor-clean); the W2 split is muddy (78% vs 57%) and its negativity is itself C6-contaminated. C1+C6 loses under every W overlay (W1, W1+W2, W1+W2+W6); C1-no-C6 wins under every W overlay → block target is **C1+C6, W-agnostic**. (C1-alone unreliable — BE-inflated historically, N=1 BE-off.) **GATES: (1) C1+C6 SHORT block — ✅ RESOLVED MOOT Jun 10:** overlap audit showed ALL 8 historical C1+C6 shorts (N=8, 50% WR, −$174 demux) would already be blocked by the current stack (SHORT cross-filter + `btc_rsi_1h_min_short=35` — every one entered at BTC RSI 22-35 / 1h-RSI <35). The combo can no longer enter; no pattern-level block needed; the C1 3× cell now effectively applies only to the clean cohort (C1-alone = N=35, 77% WR, +$154 demux — KEEP). **(2) still armed: any winner C1 combo (C1-only / C1+C2 / C1+C3) that flips net-negative on N≥5 fresh → review/demote its multiplier.** **▸ C1+C3 flagged (Jun 6)** — track C1+C3 SHORT under gate (2); also keep reconfirming C1-alone (still the clean winner).
- **LONG ATR-floor (Jun 7, INVESTIGATED — REJECTED):** hypothesis (from a clean 06-07 N=4 split) that ATR<~0.5 longs are DOA-prone → block them. **Cross-batch (9-pool+batch, N=187) it does NOT hold:** DOA-rate is non-monotonic — *peaks at ATR 0.6-0.8 (44%)*, and the <0.3 band is the *lowest* DOA below 1.1 (24%); both sides of a 0.5/0.6 floor lose; the 0.5-0.6 band is the *least-bad* below 1.1 so a floor blocks the wrong band; and the live fix-TP already cuts ATR<0.5 from −$1,572→−$514. The only winning cohort was **ATR≥1.1** (+$798 demux, 70% WR) — note Jun 10: that cohort did NOT survive the guard-stack re-sim (zone survivors 36% WR / −$328; ATR multiplier dimension removed). A real floor would be at 1.1 = near-total long shutdown (reactive-throttle question, not an ATR tweak). **No floor. Don't re-propose.**
- **EMA13-cross-LONG → PHANTOM-CF live experiment (Jun 7, SHIPPED as measurement, was model-REJECTED).** The 9-pool model said disabling is net-negative (−$65 at SLWide −1.00, coin-flip 22/22 per-trade — cross protects reversing longs from the wider stop). Rather than argue the model, operator chose to **measure it live, zero-blind-risk**: `ema13_cross_exit_long_enabled=false` → LONGs ride to their real exit, the cross is recorded as a **phantom** (would-have-exited pnl). The **🔀 EMA13 Cross Disabled-Direction CF** table compares phantom (cut) vs actual (held) per LONG. **GATE: read the CF table at N≥20 fresh LONG phantom-fires — if ⚠ KEEP cross (cross beats held, net-$ negative) → re-enable `ema13_cross_exit_long_enabled=true`; if ★ DISABLE wins (held beats cut) → keep off and the model was wrong.** Expected (per model) to read ⚠ KEEP, but measuring removes the doubt.
- **Pattern-cell engine GENERALIZED (Jun 8).** `pattern_cell_rules` now matches `UNMATCHED` (no C & no W), AND-combos (`C1+C6`, `C7+W2` — mixed resolves to C side), and a `block:true` action (skip entry → counter `PATTERN_CELL_BLOCK`), alongside the existing single-code multiplier/fixed-exit treatment. One config list = block | cap | multiply for any C/W/combo × L/S. **Jun 10: `!` negation added** — `W6+!W1` = W6 matched AND W1 NOT matched (all-negated patterns refused; side comes from positive parts; C-blocks-W priority means a W-side negation rule never fires on C-matched trades).
- **W6+!W1 SHORT → BLOCK (Jun 10).** Cohort = "Mature BTC Bear tag (BTC ADX≥32) with NO pair-level momentum confirmation (no W1), no C" — macro-only shorts on coins with no downtrend of their own. Mechanism-aware re-sim under TODAY'S stack (cross-filter + 1h-RSI≥35 guard): the only winner (PHA +$169) is already blocked by the 1h guard; survivors = **N=4, 0% WR, −$303, across 3 dates** (clears the ≥3-sample direction-consistent bar). The W6 SHORT 2× rule was DELETED in the same ship (cell cohort −$37 demux; its winners W6+W1 keep the multiplier automatically via the new W2+W1 rule — every W6 short is also a W2 short). **REVERT GATE: remove the block if PATTERN_CELL_BLOCK(W6+!W1) blocks >10% of SHORT attempts (over-block / regime shift), OR re-open for a 1-batch measurement window at the next ≥30-trade review if short throughput thins.**
- **BTC cross-filter SHORT `30-35:30`→`30-35:30-32` (Jun 9, DISCIPLINE-OVERRIDE).** Closes the climax-oversold leak: RSI 30-35 shorts now require BTC ADX **30-32** (was ≥30), blocking the **ADX≥32 + RSI 30-35** corner = the W6 "mature bear" zone that's actually exhaustion → bounce. Evidence: 2×2 on W2 shorts — only the `ADX≥32 × RSI<35` cell loses (N=22, 45% WR, −$399 demux / −$881 as-sized); the other 3 cells win (ADX<32×RSI<35 +$151/80%, ADX<32×RSI35-45 +$655/78%, **ADX≥32×RSI35-45 +$179/100%** — so W6 NOT retired, just gated). All 22 blocked are RSI 30-35; ADX 30-32 winner band (+$43/69% WR) preserved. Impact: short book −$172→+$709 as-sized (+$881) / demux +$399; ~1.7 blocks/active-short-day. **BELOW GATE** (N=22<30, WR 45%>40%) = override; cell is −EV even at 1× (−$399 demux) so chose block over de-mux. **REVERT GATE: re-open the cell (`30-35:30-32`→`30-35:30`) if RSI 30-35 × ADX>32 shorts hit ≥50% WR on N≥8 fresh** (track via the would-be-blocked record / BTC_RSI_ADX_CROSS counter). Driver = climax (ADX≥32) AND oversold (RSI<35) interaction, not either alone.
- **UNMATCHED SHORT → BLOCKED (Jun 8, OPERATOR DISCIPLINE-OVERRIDE).** `{UNMATCHED, SHORT, block:true}` live. **Fails the locked Pattern-C→FILTER gate 3/4** (N=23<30, WR 43%>40%, NP 26%<60%; only Avg −0.222% passes) — shipped on operator call over my fix-SL recommendation. **Cost accepted:** forfeits the cohort's ~7-runners-per-23 upside, AND the block makes the cohort UNMEASURABLE (can't see its WR while blocked). Overlaps the re-added BTC cross-filter (which already blocks the RSI 30-35 squeeze unmatched-shorts); this additionally removes the RSI 38-42 residual + unmatched shorts in any other zone. **TIGHTER-THAN-STANDARD REVERT GATE: (1) re-open (block→false) for a 1-batch measurement window at the next ≥30-trade review — if the cohort shows ≥45% WR OR net-positive demux on N≥10 fresh, remove the block (or downgrade to `fixed_sl_pct:-0.50` cap, the originally-recommended treatment); (2) remove immediately if PATTERN_CELL_BLOCK blocks >15% of SHORT attempts (over-block) OR short throughput thins past operator comfort.** (UNMATCHED LONG stays 2× multiplier — separate watchlist item.)
- **Trailing Min-Profit Gate → PHANTOM-CF read gate (Jun 8, SHIPPED w/ instrument).** The gate (`trailing_min_profit_to_fire=+0.10`) suppresses high-ATR L1 trailing whipsaws (peak−pullback < +0.10 → dormant, rides hard SL, re-arms). No new exit reason — suppressed trades vanish from negative TRAILING_STOP L1 and exit via TRAILING_STOP L2-5 (recovery) or STOP_LOSS_WIDE (dump). Measurement = phantom marker `phantom_trail_suppress_pnl` (records the would-have-cut pnl% the FIRST time the gate blocks a fire) + the **🛡️ Trailing Min-Profit Gate — Suppressed-Fire CF** table (phantom cut vs actual held exit, per direction). Backing CF evidence pre-ship: 16/16 suppressed L1 trades recovered to ≥+0.25% (suppress+ride +$190→+$1,697, Δ +$1,506). **READ GATE at N≥20 fresh suppressed-fires: ⚠ REVERT (cut beats held, avg Δ≤−0.05pp OR Total$<0) → set `trailing_min_profit_to_fire`→−99 (disable); ★ SUPPRESS wins (held beats cut) → keep +0.10.** Watch the CF table verdict.
- **UNMATCHED-LONG `65-70:28` cross-rule (Jun 10, WATCHLIST — DOWNGRADED to weak suspect, NOT re-added).** Raw band scan flagged 65-70 as the last negative unmatched band, but the proper two-window check demotes it: the cell's big loss (all-longs −$1,879) is **overwhelmingly MATCHED longs — already blocked** by keep-only-unmatched; the unmatched-only residue = **N=11, 55% WR, only −$122 demux, and the per-date sign FLIPS every single date** (05-26 −255 · 05-28 +96 · 06-02 −140 · 06-03 +67 · 06-05 −158 · 06-08 +52 = textbook multi-date inconsistency → noise). Last-6: N=2. Contrast ×ADX≥28 = 83% WR +$92 (N=6). **GATE kept as tripwire only (expect it never trips): re-add `65-70:28` if unmatched 65-70×ADX<28 longs confirm net-negative on N≥6 fresh post-reset.** (55-60 +$348 and 60-65 +$368/86% WR — healthy, leave open.)
- **EMA-Gap-Expanding RELAXATION → prev2_only A/B (Jun 8, SHIPPED live w/ tag).** `ema_gap_expanding_mode = prev2_only` (was strict 'both'). The gap-expanding filter was the **#1 entry blocker (31% of all blocks, 8,684)** — strict 'both' requires the EMA5-EMA13 gap to beat prev1 AND prev2 (a fresh 3-bar expansion high), rejecting healthy stairstep continuations. prev2_only drops the prev1 check (tolerates a 1-candle pause). Rationale to relax: it's belt-and-suspenders with the NO_EXPANSION *exit* (which already culls non-developing trades) and it's the highest-yield throughput lever. **Measurement (real trades, not phantom — these entries open):** each admitted entry tagged `entry_gap_expand_marginal` = True if it passed prev2 but would have FAILED prev1 (the newly-allowed cohort). The **🪟 Gap-Expand Relaxation** table splits MARGINAL vs STRICT × dir (WR, Avg%, Total$ demux). **READ GATE at N≥20 fresh MARGINAL: ★ keep prev2_only if MARGINAL WR ≥ STRICT AND Total$>0; ⚠ revert mode→'both' if MARGINAL net-negative OR WR ≫10pp below STRICT.** **ABORT EARLY (real $ at risk): revert to 'both' if MARGINAL WR≤30% OR Total$<−$150 on N≥10.** Live measure beats theory — can't validate a blocked cohort while it's blocked.
- **SHORT-side ATR separation (Jun 6, OBSERVATION — likely noise, do NOT act):** hypothesis that entry ATR separates SHORT winners/losers (a partial-batch read suggested ATR≥0.5 wins / <0.5 loses). **Cross-batch it does NOT hold** — on the 8-pool the relationship is INVERTED (low-ATR is the better short cohort) and non-monotonic (money in the 0.4-0.7 middle, losers at both extremes), and the per-date sign flips ~half the dates. Treat as **non-stationary noise**; the durable short separators remain pattern-based (C1-alone winner, C1-combos contaminated) + the proven entry filters, NOT ATR. **GATE: only revisit if ATR≥0.5 > ATR<0.5 on Total$ across ≥4 of the next 5 batches (it won't — logged so we don't re-litigate).**
- **SLWide widen −0.70→−1.00% (`signal_active_sl`, SHIPPED 2026-06-06):** widens ONLY the signal-active stop (STOP_LOSS_WIDE); STOP_LOSS (signal-dead) stays −0.70. Level chosen from a proper sweep (real `signal_active_sl`×ATR-widen×−1.20-floor mechanism): NET Δ is **monotonic — wider = better to the −1.20 floor** (9-pool: −0.90 +$861 · −1.00 +$939 · −1.10 +$1,459 · −1.20 +$1,532). −1.20 is EV-max but its marginal gains rest on deep-trough (−1.0/−1.1%) survivors (proxy-soft) + the fattest leveraged crash tail. **−1.00 = the middle / risk-managed pick** (operator-chosen): ~60% of the −1.20 EV, short of the deepest-trough trades. Driven by signal-active stops where the thesis was still valid (~28-38% reverse). Chose the base-SL lever (not the ATR mult) because the reversers are LOW-ATR (ATR-widen doesn't engage). **REVERT GATE: revert `signal_active_sl`→−0.70 if (a) net-negative on STOP_LOSS_WIDE survive-vs-deepen over N≥30 fresh, OR (b) a single correlated crash window adds ≥−$300 of deepened STOP_LOSS_WIDE loss vs the −0.70 baseline.** Watch the deepen tail on 2-3× cells (leveraged). Symmetric L/S — split into `_long`/`_short` only if directions diverge (last-4: SHORT > LONG, both positive).
- **SLWide STAGE-UP −1.00→−1.20 (Jun 6, WATCHLIST — review NEXT BATCH):** the level sweep showed NET Δ monotonic to the −1.20 floor (EV-max; 9-pool −1.00 +$939 → −1.20 +$1,532), but the −1.00→−1.20 gain rests on **deep-trough (−1.0/−1.1%) survivors (proxy-soft) + the fattest leveraged crash tail**. **STAGE UP to −1.20 (in one step, or −1.00→−1.10→−1.20) IF: on next batch, STOP_LOSS_WIDE survive-vs-deepen stays net-positive at −1.00 AND no crash window flipped it AND the would-be −1.10/−1.20 survivors (trough between −1.00 and −1.20 then reversed) actually materialize ≥ the deepen cost.** Do NOT jump straight to −1.20 on this batch — validate −1.00 first.
- **C1-SHORT fixed-SL widen 0.70→0.80% (Jun 6, WATCHLIST — observation, NOT shipped):** candidate to widen the C1-SHORT `fixed_sl` from −0.70% to −0.80%. Mechanism only helps the narrow band of C1-shorts whose post-SL **trough stays above −0.80% then reverses** (stopped one tick before the bounce); trades that plunge below −0.80% first are just deepened, and the extra 0.10% is amplified by the 2–3× C1-SHORT multiplier on every non-reversal tail (survivorship risk — we only see the reversers). **TRACK: for each C1-SHORT that hits PATTERN_FIXED_SL, log whether `post_exit_trough_pnl` stayed > −0.80% (a −0.80 SL would SAVE it) or went below (deepens).** **GATE: ship 0.80 only if "save" cases are a clear majority on N≥6–8 fresh PATTERN_FIXED_SL C1-shorts.** **Cross-batch CF tally (Jun 10, all 6 historical PATTERN_FIXED_SL shorts): 2 SAVE / 3 DEEPEN / 1 unknown — gate FAILS (saves are minority).** Saves: UNI 06-06 (trough −0.77 → rode to **+2.68**, Δ+3.41pp) + 1000PEPE 06-10 (−0.78 → −0.31, Δ+0.39pp). Deepens: SUI/PENGU/DASH (troughs −1.46 to −2.89, each pays exactly −0.10×3×≈−$20). $ CF net +$750 BUT +$770 of it is the single UNI monster = 1-sample fat tail, do NOT ship on it. **Structural note: payoff is asymmetric (deepen cost capped at −0.10pp; save upside unbounded) — so −0.80 could be +EV even at minority save-rate IF monster recoveries recur. Keep the majority gate; revisit shape only if a 2nd monster save appears (would be N=2 evidence the tail is real).**
- **C7 LONG-alone / no-W (Jun 4, WATCHLIST — observation only, NOT shipped):** C7 = Pair Countertrend Bounce; a LONG on a countertrend bounce with no W (trend) confirmation = buying a falling knife. Recent (7-pool+batch, BE-off), **C7 LONG with no W: N=3, 0W/3L, ≈−$265** (1000LUNC, ONDO, HOME — HOME this batch). Cross-ref: UI C-signature "C7" LONG row (W-ignored) = N=5, 20% WR, −$245. Definition matters — the *block target* is **C7 present AND no W match** (don't block C7 trades that carry trend confirmation). N=3 ≪ gate. **GATE: ship a LONG entry-block on "C7-match AND w_any=False" only if it holds ≤30% WR on N≥8 fresh.** Until then observe; do nothing.
- **C6 LONG (Jun 4, OBSERVATION-tracking — NOT a loser, do NOT block):** recent (7-pool+batch, BE-off) C6 LONG = N=7, 4W/3L, **57% WR**, Avg ≈−0.002% (flat), Tot −$46.6 but **de-multiplied +$18.9** (positive). The as-sized loss is entirely 2× amplification on RENDER (−172) + FET (−117); RENDER's 2× = the extension mult already demoted Jun 4, so the main amplifier is handled. C6 LONG always fires **no-W** (macro-over-extended, no trend confirmation) yet still de-mux positive → no action. **WATCH-FOR-DETERIORATION GATE: only consider a cap/block if C6 LONG turns net-negative DE-MULTIPLIED with ≤35% WR on N≥10 fresh.** Track the Pattern-C Combination Tracker "C6 LONG" row.
- **W2 LONG (Jun 4, OBSERVATION):** W2 = "Macro tailwind" is a SHORT pattern (7-pool SHORT N=55, **76% WR, +$634**, multiplied 3× & winning — DO NOT touch). On LONGS it's baseline **1×** and NOT a winner: 7-pool N=14, 36% WR, **≈−$47 (breakeven)**; collapsed this batch N=5, **0W/5L, −$337, 4 NP (DOA)**. The batch collapse overlaps the C6/Neither DOA bleed (AAVE/SOL/XRP/1000PEPE) → likely regime, not a standalone W2-long edge. **GATE: consider a LONG entry-block on W2 only if it holds ≤35% WR on N≥10 fresh AND net-negative (it's 1×, so block is the only lever).** Until then observe.
- **W3 LONG & SHORT (Jun 4, OBSERVATION — too thin, no verdict):** W3 = "Energetic volatility" has no statistical weight anywhere: LONG 7-pool N=3 33% −$96 / batch N=1 −$129 (XPL, DOA); SHORT 7-pool N=2 0% −$232 / batch N=0. Mildly negative both sides but N=1–3. **NO gate — accumulate to N≥8 per side before any verdict.** Track in the Pattern-W Combination Tracker.
- **Runner Stretch-Trail — DISABLED 2026-06-06 (was the Jun-1 ship).** Live result was a wash (N=6, net +$30 vs tight, big peakers faded). Re-enable (`runner_trail_enabled`=true) only if a true IDU-class monster runner (peak >2-3%) appears and the stretch-trail would have clearly out-captured tight — the mechanism only pays on rare monsters, none seen in 6 fires. UI toggle now exposed.
- **BTC_60-65_22-25 LONG 3× eff:** keep if N≥5 & WR≥70% & Total$+ · WR 55-70% → drop lev to 2.0× inv-only · WR≤55% → drop both. Any single leveraged Δ$≤−$60 → drop lev immediately.

## Metrics tracked
**Avg P&L %** (leverage-invariant — the comparison metric) · Daily Compound Return · per-cell verdict (Multiplier / Pattern-Cell Ship tables) · Filter Blocks counters · Gross gauge (% + $ tooltip) · entry/exit slippage · Pattern C/W trackers (observation-only).

## Known risks
- **Gross 30× tail**: a −3.3% correlated gap ≈ −100% equity (per-position SLs are primary protection; gross cap is flash-crash insurance).
- **Multiplier amplification on fat-tail losers**: C6/C1/W2 cohorts are 62-74% WR *winners* net-dragged by a few 3×-amplified losers → fix is surgical sizing on the specific losers, NOT blocking the cohort ("high-WR-but-net-losing" trap).
- **LONG book structurally weak** (negative in most cross-batch dimensions); LONG vol floor occasionally clips high-ATR runners.

## Do not change for now
- **BE stays OFF** (proven not to help; runner-trail + trailing is the current exit thesis (FAST_EXIT OFF too)).
- **SHORT volume rescue stays $0** (mirroring LONG $50M would un-block dead-tape SHORTs like SEI).
- **Don't add more entry filters reflexively** — 15+ already, diminishing returns. Exit-side + sizing is the active lever.

## Next ~1-week forward-test focus
1. **Liquidity-sizing live validation** — the Go-Live Watch signals (Gross gauge, GROSS_CAP_SKIP, ① slippage, REDEPLOY_OPEN=0).
2. **Global Vol filter** firing correctly + not clipping runners (VOL_GATE counters).
3. **ADX Δ filter** + the recent BTC RSI/ADX cross-filter openings holding.
4. Combined **Avg P&L %** vs the recent −$1,941/207-trade baseline; SHORT-side drag (dead-tape, C1/W2 amplified losers).
5. **Reset partition:** after the user's reset, analyze only `opened_at ≥ reset timestamp` (don't pool pre-reset trades).
